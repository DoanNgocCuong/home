```python
[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiokafka==0.12.0",
    "dependency-injector==4.48.2",
    "fastapi==0.117.1",
    "ipykernel>=6.30.1",
    "langchain==0.3.27",
    "langchain-openai==0.3.33",
    "langfuse==3.5.0",
    "markitdown==0.1.3",
    "pillow==11.3.0",
    "pydantic-settings==2.10.1",
    "redis==6.4.0",
    "uvicorn==0.36.0",
]

[tool.setuptools.packages.find]
include = ["app*"]
exclude = ["CKP*", "temp*", "deploy*", "studio*", "tests*", "utils*", "base-image*", "scripts*"]

```

---

```python
pip install -e .        # Editable install
# hoặc
pip install .           # Normal install
```

---
