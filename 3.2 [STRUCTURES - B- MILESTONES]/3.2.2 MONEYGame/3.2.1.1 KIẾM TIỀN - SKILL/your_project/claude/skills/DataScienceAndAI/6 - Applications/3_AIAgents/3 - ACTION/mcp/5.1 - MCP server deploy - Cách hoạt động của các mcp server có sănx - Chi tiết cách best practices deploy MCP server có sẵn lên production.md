```bash
Để deploy **official MCP servers** (npx/uvx) lên production:

1. **KHÔNG THỂ** dùng trực tiếp (chỉ có STDIO transport)
    
2. **PHẢI** convert sang HTTP bằng 1 trong 3 cách:
    
    - ⭐ **MCP Proxy** (sparfenyuk) - Khuyến nghị cho 90% cases
    - 🚀 **Ray Serve** - Cho enterprise scale
    - 🐳 **Docker Gateway** - Cho Docker users
3. **Best practice đầy đủ** như custom servers:
    
    - Security (auth, rate limit, SSL)
    - Monitoring (logs, metrics, alerts)
    - Performance (resource limits, caching)
    - Reliability (health checks, backup)
```


Đúng, về bản chất là **đúng** – đa số mấy server trong config đó **đã được “đóng gói & deploy sẵn” thành công cụ runnable**, nên Claude/Cursor chỉ việc spawn process là chạy ngay, bạn không phải tự build.

## 1. Những server trong config này đang chạy kiểu gì?

Nhìn vào cấu hình:

- Các server dùng `npx`:
    
    - `@modelcontextprotocol/server-sequential-thinking`
        
    - `@modelcontextprotocol/server-memory`
        
    - `chrome-devtools-mcp`, `mcp-excalidraw-server`, `@modelcontextprotocol/server-github`, v.v.  
        → Đây là **npm packages trên registry**, mỗi package **đã chứa sẵn MCP server** (Node/TypeScript) và `npx` chỉ việc:
        
    - tải về (nếu chưa có),
        
    - chạy command line entrypoint,
        
    - nói chuyện với Claude/Cursor qua **STDIO**.
        
- Các server dùng `uvx`:
    
    - `mcp-rubber-duck`, `mcp-server-git`, `mcp-server-filesystem`, `mcp-jupyter`, `mcp-server-sqlite`, `mcp-server-aws`, `mcp-server-gcp`, `mcp-server-kubernetes`, `mcp-server-docker`, v.v.  
        → Đây là **Python MCP servers đã publish lên PyPI / uvx registry** (hoặc script entry), `uvx` lo:
        
    - install đúng package + version,
        
    - chạy MCP server theo đúng entrypoint,
        
    - giao tiếp cũng qua **STDIO**.
        
- Một số server custom:
    
    - `vnstock`: `"command": "python", "args": ["vnstock_mcp_run_fixed.py"]` → Đây là MCP server **do bạn tự viết**, chạy local.
        
    - `GitKraken`: chạy exe của GitKraken dưới mode `mcp` → GitLens team đã implement MCP endpoint trong binary đó.
        

=> Tất cả các entry trong JSON này đều là **“cách để client (Claude/Cursor) khởi chạy MCP server dưới dạng process local”**, không phải bạn phải deploy HTTP service từ trước.

## 2. Nó “deploy sẵn” ở đâu?

Có 2 kiểu:

1. **Server do bên thứ ba build và publish**:
    
    - Kiểu `npx @modelcontextprotocol/server-xxx` hoặc `uvx mcp-server-xxx`.
        
    - Họ đã:
        
        - Viết MCP server (Node/Python),
            
        - Đóng gói thành npm / PyPI package,
            
        - Publish lên registry public.
            
    - Bạn chỉ cần có `node+npx` hoặc `uvx+python`, là **Claude/Cursor có thể tải và chạy ngay**, không cần tự deploy HTTP.
        
2. **Server tự viết, chạy local**:
    
    - Như `vnstock`, hoặc bất kỳ script Python nào bạn tự code.
        
    - “Deploy” ở đây chỉ là: file `.py` nằm trong máy bạn, Cursor/Claude spawn process `python vnstock_mcp_run_fixed.py` → server chạy qua STDIO.
        

=> Nói nôm na:

- **Đúng, mấy MCP server đó đã ở trạng thái “runnable package” / “deploy local binary” rồi**, nên khi nhúng vào Claude/Cursor:
    
    - Client spawn process,
        
    - Gắn STDIO stream,
        
    - MCP protocol chạy luôn → bạn thấy “cắm vào là dùng được”.
        

## 3. Khác gì với remote MCP server kiểu HTTP/Docker?

- Ở case JSON bạn gửi:
    
    - **Transport = STDIO** (trừ khi package đó hỗ trợ HTTP bên trong, nhưng Cursor/Claude đang launch nó như process local).
        
    - Không cần URL, không cần cổng, không cần Docker, không cần cloud.
        
- Ở case **remote MCP server**:
    
    - Bạn phải deploy MCP server (Python/Node/whatever) lên:
        
        - Docker container / Docker Compose,
            
        - Azure Functions,
            
        - EC2 / VPS / Cloud Run,…
            
    - Client config:
        
        json
        
        `{   "mcpServers": {    "parent-grade-mcp": {      "type": "http",      "url": "https://your-mcp-server.com/mcp",      "headers": {        "x-api-key": "..."      }    }  } }`
        
    - Lúc này **Claude/Cursor không spawn process**, mà **gọi qua HTTP**.
        

## 4. Kết luận ngắn gọn

- **Đúng**:
    
    - Các MCP server như `server-sequential-thinking`, `server-memory`, `mcp-server-git`, `mcp-server-docker`,… trong config đó **đã được build & đóng gói sẵn** (npm/PyPI/binary).
        
    - Khi bạn thêm vào config Claude/Cursor, client **chỉ cần chạy command tương ứng là server hoạt động ngay**, nên bạn cảm giác “cắm là chạy”.
        
- **Nhưng**:
    
    - Chúng **chạy local dưới dạng process STDIO**, chứ không phải bạn đang nói chuyện với 1 HTTP service deploy đâu đó (trừ khi bạn cố tình dùng `type: "http"` + URL).
        

Nếu muốn, có thể chuyển một trong các server kiểu `python vnstock_mcp_run_fixed.py` thành **Dockerized HTTP MCP service**, rồi mình viết cho bạn 1 `docker-compose.yml` + config `type: "http"` tương ứng để so sánh 2 mode side-by-side.

1. [https://www.perplexity.ai/search/step-by-step-trien-khai-mcp-se-QUHplDvCT_WtfTAYDlA7_w](https://www.perplexity.ai/search/step-by-step-trien-khai-mcp-se-QUHplDvCT_WtfTAYDlA7_w)



---

# Đóng docker compose 


Đang tìm hiểu các cách triển khai MCP server:


[6 tools called]


Dựa trên code và docs, đây là phân tích MECE:

## Cách triển khai hiện tại

Đang dùng: Docker Compose + HTTP Transport (streamable-http)
- MCP server chạy trong container riêng
- Transport: HTTP/streamable-http
- Kết nối: App container → MCP container qua HTTP

---

## Phân tích MECE: Các cách triển khai MCP Server

### 1. Theo Transport Protocol

#### A. STDIO Transport (như Cursor)
- Cách hoạt động: Process-based, app spawn process của MCP server
- Code đã có: `ExternalMCPServerWrapper` hỗ trợ STDIO
- Ưu điểm:
  - Đơn giản, không cần network
  - Phù hợp single-user/local
  - Cursor/Claude Desktop dùng cách này
- Nhược điểm:
  - Khó scale multi-user
  - Process management phức tạp
  - Không tốt cho production multi-user

#### B. HTTP Transport (đang dùng)
- Cách hoạt động: HTTP/streamable-http, MCP server là HTTP service
- Đang dùng: `HTTPMCPServerWrapper`
- Ưu điểm:
  - Stateless, scale tốt
  - Multi-user support
  - Phù hợp production/SaaS
- Nhược điểm:
  - Cần network infrastructure
  - Phức tạp hơn STDIO

---

### 2. Theo Deployment Method

#### A. Docker Compose (đang dùng)
```
docker compose -f docker-compose-mcp.yml up -d
```
- MCP server trong container riêng
- Tách biệt với app container
- Dễ quản lý lifecycle

#### B. Process-based (như Cursor)
```python
ExternalMCPServerWrapper(
    name="drive",
    command="npx",
    args=["-y", "@modelcontextprotocol/server-drive"]
)
```
- App spawn process của MCP server
- Không cần Docker
- Phù hợp development/local

#### C. Kubernetes (production scale)
- Deploy MCP server như Kubernetes service
- Auto-scaling, service discovery
- Phù hợp production lớn

---

### 3. Theo Integration Type

#### A. External Server (đang dùng)
- MCP server chạy riêng biệt
- App connect qua HTTP
- Ưu điểm: Tách biệt, dễ scale, dễ maintain

#### B. Internal Server
- MCP server là part của app
- Code đã có: `InternalDriveMCPServer`
- Ưu điểm: Đơn giản, không cần network
- Nhược điểm: Tight coupling, khó scale riêng

---

## So sánh với Cursor

| Aspect      | Cursor (thường dùng)     | Bạn (đang dùng)      |
| ----------- | ------------------------ | -------------------- |
| Transport   | STDIO                    | HTTP/streamable-http |
| Deployment  | Process-based            | Docker Compose       |
| Integration | ExternalMCPServerWrapper | HTTPMCPServerWrapper |
| Multi-user  | ❌ Khó                    | ✅ Tốt                |
| Production  | ⚠️ Hạn chế               | ✅ Phù hợp            |
| Complexity  | ✅ Đơn giản               | ⚠️ Phức tạp hơn      |

---

## Kết luận

Hiện tại dùng Docker Compose + HTTP Transport phù hợp cho production/multi-user.

Các cách khác (MECE):
1. STDIO + Process-based (như Cursor) - cho development
2. HTTP + Docker Compose (đang dùng) - cho production
3. HTTP + Kubernetes - cho scale lớn
4. Internal server - đơn giản nhưng ít linh hoạt

Bạn có muốn thêm support STDIO transport như Cursor không, hay giữ nguyên HTTP?


---


Tuyệt vời! Bây giờ tôi có đủ thông tin để tạo phân tích MECE đầy đủ. Để mình tạo một tài liệu chi tiết về các loại MCP server và best practices cho từng loại:

---

# 📊 PHÂN TÍCH MECE: CÁC LOẠI MCP SERVER & BEST PRACTICES PRODUCTION

## 1️⃣ CÁC LOẠI MCP SERVER (MECE Framework)

### A. Theo Nguồn Gốc (Origin)

#### 🏢 **Official Servers** (Do Anthropic/Tổ chức chính thức phát triển)

- **Danh sách phổ biến:**
    
    - `@modelcontextprotocol/server-filesystem` - Truy cập file system
    - `@modelcontextprotocol/server-memory` - Memory context
    - `@modelcontextprotocol/server-github` - GitHub integration
    - `@modelcontextprotocol/server-gdrive` - Google Drive
    - `@modelcontextprotocol/server-sqlite` - SQLite database
- **Đặc điểm:**
    
    - ✅ Được test kỹ lưỡng
    - ✅ Documentation đầy đủ
    - ✅ Security audit chính thức
    - ⚠️ Chỉ hỗ trợ STDIO transport mặc định
    - ⚠️ Hạn chế customization

#### 👥 **Community Servers** (Do cộng đồng phát triển)

- **Danh sách phổ biến 2026:**
    
    - `K2view` - Enterprise data fabric
    - `Vectara` - Semantic search & RAG
    - `Zapier` - 6,000+ app automations
    - `Notion` - Workspace integration
    - `Supabase` - Serverless Postgres
    - `Docker Hub` - Container management
    - `Slack`, `Discord` - Communication platforms
- **Đặc điểm:**
    
    - ✅ Đa dạng tính năng
    - ✅ Cập nhật thường xuyên
    - ⚠️ Quality không đồng nhất
    - ⚠️ Cần verify security

#### 🛠️ **Custom Servers** (Tự phát triển)

- **Ví dụ:** Server vnstock của bạn, parent-grade MCP
- **Đặc điểm:**
    - ✅ Tùy chỉnh hoàn toàn
    - ✅ Hỗ trợ HTTP + STDIO
    - ⚠️ Phải tự maintain
    - ⚠️ Trách nhiệm security thuộc về bạn

---

### B. Theo Ngôn Ngữ Lập Trình (Language)

#### 🟨 **JavaScript/TypeScript Servers**

- **Công cụ chạy:** `npx`
- **Package manager:** npm
- **Ví dụ:**
    
    ```bash
    npx -y @modelcontextprotocol/server-memory
    npx -y @modelcontextprotocol/server-github
    ```
    

#### 🐍 **Python Servers**

- **Công cụ chạy:** `uvx` hoặc `uv`
- **Package manager:** PyPI
- **Ví dụ:**
    
    ```bash
    uvx mcp-server-sqlite
    uvx mcp-server-fetch
    ```
    

#### 🦀 **Native Binary Servers** (Go, Rust, C++)

- **Công cụ chạy:** Direct executable
- **Ví dụ:**
    
    ```bash
    /usr/local/bin/mcp-custom-server --config server.json
    ```
    

---

### C. Theo Transport Protocol (Communication)

#### 📡 **STDIO Transport** (Standard Input/Output)

- **Đặc điểm:**
    
    - Process-based communication
    - Local execution only
    - Không cần network configuration
    - Client spawn server process trực tiếp
- **Config mẫu:**
    
    ```json
    {
      "mcpServers": {
        "filesystem": {
          "command": "npx",
          "args": ["-y", "@modelcontextprotocol/server-filesystem"]
        }
      }
    }
    ```
    

#### 🌐 **HTTP/SSE Transport**

- **Đặc điểm:**
    
    - Network-based communication
    - Remote execution
    - Cần authentication
    - Scale được nhiều user
- **Config mẫu:**
    
    ```json
    {
      "mcpServers": {
        "remote-server": {
          "transport": "sse",
          "url": "https://mcp.example.com/sse",
          "headers": {
            "Authorization": "Bearer token"
          }
        }
      }
    }
    ```
    

---

### D. Theo Deployment Method (Triển khai)

#### 💻 **Local Process-Based** (npx/uvx)

- Chạy trực tiếp trên máy local
- Cursor, Claude Desktop thường dùng

#### 🐳 **Docker Container**

- Chạy trong container
- Isolated environment
- Production-ready

#### ☸️ **Kubernetes/Cloud**

- Deploy lên cluster
- Auto-scaling
- Enterprise-grade

---

## 2️⃣ BEST PRACTICES PRODUCTION CHO TỪNG LOẠI

### 📌 A. OFFICIAL SERVERS (npx/uvx)

#### ✅ **Khi nào dùng?**

- ✅ Development/Testing local
- ✅ Personal projects
- ✅ Rapid prototyping
- ❌ **KHÔNG dùng cho production multi-user**

#### 🛡️ **Best Practices**

##### 1. **Hạn chế quyền truy cập**

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/safe/path/only"  // Giới hạn path
      ]
    }
  }
}
```

##### 2. **Chạy với non-root user**

```bash
# Tạo user riêng cho MCP
sudo useradd -m -u 1001 mcp
sudo -u mcp npx -y @modelcontextprotocol/server-filesystem
```

##### 3. **Monitor logs**

```bash
# Cursor/Claude tự log, nhưng nên enable debug mode
export DEBUG=mcp:*
```

##### 4. **Verify package integrity**

```bash
# Check npm package trước khi chạy
npm view @modelcontextprotocol/server-filesystem
```

##### 5. **Update thường xuyên**

```bash
# Xóa cache để luôn lấy version mới
npx clear-npx-cache
```

#### ⚠️ **Security Checklist**

- [ ] Giới hạn filesystem access paths
- [ ] Chạy với non-root user
- [ ] Không expose sensitive env vars
- [ ] Review package source code trên GitHub
- [ ] Enable audit logging

---

### 📌 B. CUSTOM SERVERS (Docker HTTP) - **FULL PRODUCTION**

#### ✅ **Khi nào dùng?**

- ✅ Production multi-user
- ✅ SaaS applications
- ✅ Enterprise deployments
- ✅ Khi cần custom logic

#### 🏗️ **BEST PRACTICES ĐẦY ĐỦ**

---

#### **1. 🔐 SECURITY (Quan trọng nhất)**

##### A. Authentication & Authorization

```python
# main.py
from fastapi import FastAPI, Security, HTTPException
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials

app = FastAPI()
security = HTTPBearer()

API_KEYS = {"valid-key-123": {"user": "admin", "rate_limit": 1000}}

async def verify_token(credentials: HTTPAuthorizationCredentials = Security(security)):
    if credentials.credentials not in API_KEYS:
        raise HTTPException(status_code=403, detail="Invalid API key")
    return API_KEYS[credentials.credentials]
```

##### B. Rate Limiting

```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.post("/mcp/tools")
@limiter.limit("100/minute")
async def call_tool():
    pass
```

##### C. Input Validation

```python
from pydantic import BaseModel, validator, Field

class ToolRequest(BaseModel):
    tool_name: str = Field(..., regex="^[a-z_]+$")  # Chỉ cho phép lowercase + underscore
    arguments: dict
    
    @validator('tool_name')
    def validate_tool(cls, v):
        allowed = ['get_stock_data', 'get_grade']
        if v not in allowed:
            raise ValueError(f"Tool {v} not allowed")
        return v
```

##### D. Security Headers

```python
from fastapi.middleware.trustedhost import TrustedHostMiddleware
from fastapi.middleware.cors import CORSMiddleware

# CORS strict
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://your-app.com"],  # KHÔNG dùng "*"
    allow_credentials=True,
    allow_methods=["POST"],
    allow_headers=["Content-Type", "Authorization"],
)

# Trusted hosts
app.add_middleware(
    TrustedHostMiddleware,
    allowed_hosts=["yourdomain.com", "*.yourdomain.com"]
)
```

---

#### **2. 📊 MONITORING & LOGGING**

##### A. Structured Logging

```python
import structlog
from datetime import datetime

logger = structlog.get_logger()

@app.middleware("http")
async def log_requests(request: Request, call_next):
    start_time = datetime.utcnow()
    response = await call_next(request)
    duration = (datetime.utcnow() - start_time).total_seconds()
    
    logger.info(
        "mcp_request",
        method=request.method,
        path=request.url.path,
        status=response.status_code,
        duration_ms=duration * 1000,
        user_id=request.state.user_id if hasattr(request.state, 'user_id') else None
    )
    return response
```

##### B. Health Checks

```python
@app.get("/health")
async def health_check():
    checks = {
        "status": "healthy",
        "version": "1.0.0",
        "timestamp": datetime.utcnow().isoformat(),
        "checks": {
            "database": await check_database(),
            "redis": await check_redis(),
            "filesystem": check_filesystem(),
        }
    }
    
    # Return 503 nếu có service unhealthy
    if any(not v for v in checks["checks"].values()):
        return JSONResponse(status_code=503, content=checks)
    
    return checks

async def check_database():
    try:
        await db.execute("SELECT 1")
        return True
    except:
        return False
```

##### C. Metrics (Prometheus)

```python
from prometheus_client import Counter, Histogram, generate_latest
from fastapi.responses import Response

# Metrics
request_count = Counter(
    'mcp_requests_total',
    'Total MCP requests',
    ['tool', 'status']
)

request_latency = Histogram(
    'mcp_request_duration_seconds',
    'MCP request duration',
    ['tool']
)

@app.get("/metrics")
async def metrics():
    return Response(
        content=generate_latest(),
        media_type="text/plain"
    )
```

---

#### **3. 🚀 PERFORMANCE & SCALABILITY**

##### A. Resource Limits (Docker Compose)

```yaml
# docker-compose.prod.yml
version: '3.8'

services:
  mcp-server:
    image: your-mcp-server:latest
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G
      replicas: 3  # Multiple instances
    restart: unless-stopped
```

##### B. Connection Pooling

```python
from httpx import AsyncClient, Limits

# Reuse connections
http_client = AsyncClient(
    timeout=30.0,
    limits=Limits(
        max_connections=100,
        max_keepalive_connections=20
    )
)
```

##### C. Caching

```python
from functools import lru_cache
import redis.asyncio as redis

# Redis cache
redis_client = redis.from_url("redis://redis:6379")

async def get_cached(key: str):
    cached = await redis_client.get(key)
    if cached:
        return json.loads(cached)
    return None

async def set_cached(key: str, value: dict, ttl: int = 3600):
    await redis_client.setex(key, ttl, json.dumps(value))
```

##### D. Async Processing

```python
import asyncio

@app.post("/mcp/tools/batch")
async def batch_process(requests: list[ToolRequest]):
    # Process parallel thay vì sequential
    tasks = [process_tool(req) for req in requests]
    results = await asyncio.gather(*tasks, return_exceptions=True)
    return results
```

---

#### **4. 🔄 RELIABILITY & ERROR HANDLING**

##### A. Graceful Shutdown

```python
import signal
import sys

def handle_shutdown(signum, frame):
    logger.info("Graceful shutdown initiated")
    
    # Close connections
    asyncio.run(http_client.aclose())
    asyncio.run(redis_client.close())
    
    # Save state if needed
    save_state()
    
    sys.exit(0)

signal.signal(signal.SIGTERM, handle_shutdown)
signal.signal(signal.SIGINT, handle_shutdown)
```

##### B. Retry Logic

```python
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=4, max=10),
    reraise=True
)
async def call_external_api(url: str):
    response = await http_client.get(url)
    response.raise_for_status()
    return response.json()
```

##### C. Circuit Breaker

```python
from circuitbreaker import circuit

@circuit(failure_threshold=5, recovery_timeout=60)
async def risky_operation():
    # Stop calling nếu fail nhiều lần
    pass
```

##### D. Error Handling Middleware

```python
@app.exception_handler(Exception)
async def global_exception_handler(request: Request, exc: Exception):
    logger.error(
        "unhandled_exception",
        error=str(exc),
        path=request.url.path,
        exc_info=True
    )
    
    return JSONResponse(
        status_code=500,
        content={
            "error": "Internal server error",
            "request_id": request.state.request_id
        }
    )
```

---

#### **5. 📦 DEPLOYMENT**

##### A. Multi-Stage Dockerfile

```dockerfile
# Dockerfile.production
FROM python:3.11-slim AS builder

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Production stage
FROM python:3.11-slim

WORKDIR /app

# Copy dependencies
COPY --from=builder /usr/local/lib/python3.11/site-packages /usr/local/lib/python3.11/site-packages

# Copy code
COPY . .

# Non-root user (CRITICAL)
RUN useradd -m -u 1001 mcp && \
    chown -R mcp:mcp /app
USER mcp

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
  CMD curl -f http://localhost:8000/health || exit 1

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]
```

##### B. Docker Compose Production

```yaml
# docker-compose.prod.yml
version: '3.8'

services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: mcp-server:${VERSION:-latest}
    
    environment:
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
      - DATABASE_URL=${DATABASE_URL}
    
    env_file:
      - .env.production
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
      replicas: 2
    
    # Logging
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    
    # Restart policy
    restart: unless-stopped
    
    # Security
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
    
    networks:
      - mcp-network

  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      mcp-server:
        condition: service_healthy
    networks:
      - mcp-network

  # Redis cache
  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis-data:/data
    networks:
      - mcp-network

  # Prometheus monitoring
  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    networks:
      - mcp-network

  # Grafana dashboards
  grafana:
    image: grafana/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
    volumes:
      - grafana-data:/var/lib/grafana
    ports:
      - "3000:3000"
    networks:
      - mcp-network

volumes:
  redis-data:
  prometheus-data:
  grafana-data:

networks:
  mcp-network:
    driver: bridge
```

##### C. Nginx Configuration

```nginx
# nginx.conf
upstream mcp_backend {
    least_conn;
    server mcp-server:8000 max_fails=3 fail_timeout=30s;
}

server {
    listen 443 ssl http2;
    server_name mcp.yourdomain.com;

    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=mcp_limit:10m rate=10r/s;
    limit_req zone=mcp_limit burst=20 nodelay;

    location / {
        proxy_pass http://mcp_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    location /health {
        access_log off;
        proxy_pass http://mcp_backend/health;
    }
}
```

---

#### **6. 🧪 TESTING & CI/CD**

##### A. Integration Tests

```python
# tests/test_integration.py
import pytest
from httpx import AsyncClient

@pytest.mark.asyncio
async def test_health_check():
    async with AsyncClient(app=app, base_url="http://test") as client:
        response = await client.get("/health")
        assert response.status_code == 200
        assert response.json()["status"] == "healthy"

@pytest.mark.asyncio
async def test_mcp_tool_requires_auth():
    async with AsyncClient(app=app, base_url="http://test") as client:
        # Without auth
        response = await client.post("/mcp/tools")
        assert response.status_code == 403
        
        # With valid auth
        headers = {"Authorization": "Bearer valid-key-123"}
        response = await client.post(
            "/mcp/tools",
            json={"tool_name": "get_stock_data", "arguments": {}},
            headers=headers
        )
        assert response.status_code == 200
```

##### B. Load Testing

```javascript
// k6-load-test.js
import http from 'k6/http';
import { check, sleep } from 'k6';

export const options = {
  vus: 50,  // 50 virtual users
  duration: '30s',
};

export default function () {
  const res = http.post(
    'https://mcp.yourdomain.com/mcp/tools',
    JSON.stringify({
      tool_name: 'get_stock_data',
      arguments: { symbol: 'VNM' }
    }),
    {
      headers: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer test-key',
      },
    }
  );
  
  check(res, {
    'status is 200': (r) => r.status === 200,
    'response time < 500ms': (r) => r.timings.duration < 500,
  });
  
  sleep(1);
}
```

##### C. GitHub Actions CI/CD

```yaml
# .github/workflows/deploy.yml
name: Deploy MCP Server

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run tests
        run: |
          docker compose -f docker-compose.test.yml up --abort-on-container-exit
      
      - name: Security scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'image'
          image-ref: 'mcp-server:latest'

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Build and push
        run: |
          docker build -t mcp-server:${{ github.sha }} .
          docker push mcp-server:${{ github.sha }}
      
      - name: Deploy to production
        run: |
          ssh production "docker compose pull && docker compose up -d"
```

---

#### **7. 🎯 PRODUCTION CHECKLIST**

##### Pre-Deployment

- [ ] Environment variables in `.env.production`
- [ ] Secrets management (AWS Secrets Manager/Vault)
- [ ] SSL certificates configured
- [ ] Database migrations tested
- [ ] Backup strategy defined
- [ ] Disaster recovery plan documented

##### Security

- [ ] API authentication enabled
- [ ] Rate limiting configured (100 req/min/user)
- [ ] Input validation on all endpoints
- [ ] CORS restricted to known domains
- [ ] Security headers (HSTS, CSP, X-Frame-Options)
- [ ] Dependency vulnerabilities scanned
- [ ] Non-root Docker user (UID 1001)
- [ ] Read-only filesystem where possible

##### Monitoring

- [ ] Structured logging enabled (JSON format)
- [ ] Health checks configured (30s interval)
- [ ] Metrics exposed (Prometheus `/metrics`)
- [ ] Alerts configured (PagerDuty/Slack)
- [ ] Error tracking (Sentry/DataDog)
- [ ] Dashboard created (Grafana)

##### Performance

- [ ] Resource limits set (CPU: 2.0, Mem: 2G)
- [ ] Auto-scaling rules defined
- [ ] CDN configured (if serving static assets)
- [ ] Database indexes optimized
- [ ] Caching strategy implemented (Redis)
- [ ] Connection pooling configured

##### Reliability

- [ ] Multi-replica deployment (≥2 replicas)
- [ ] Graceful shutdown implemented
- [ ] Circuit breakers for external calls
- [ ] Retry logic with exponential backoff
- [ ] Backup & restore tested monthly
- [ ] Load testing completed (50+ concurrent users)

##### Documentation

- [ ] API documentation (OpenAPI/Swagger)
- [ ] Deployment runbook
- [ ] Troubleshooting guide
- [ ] Rollback procedures
- [ ] On-call playbook

---

#### **8. 📖 DEPLOYMENT COMMANDS**

```bash
# Structure
production/
├── docker-compose.prod.yml
├── .env.production (gitignored)
├── nginx.conf
├── prometheus.yml
├── grafana/dashboards/
├── scripts/
│   ├── deploy.sh
│   ├── backup.sh
│   ├── rollback.sh
│   └── health-check.sh
└── monitoring/alerts.yml

# Deploy
./scripts/deploy.sh production v1.2.0

# Monitor
docker compose -f docker-compose.prod.yml logs -f mcp-server
curl https://mcp.yourdomain.com/health
curl https://mcp.yourdomain.com/metrics

# Backup
./scripts/backup.sh

# Rollback if needed
./scripts/rollback.sh v1.1.0
```

---

## 3️⃣ SO SÁNH TỔNG QUAN

|Tiêu chí|Official Servers (npx/uvx)|Custom Servers (Docker HTTP)|
|---|---|---|
|**Transport**|STDIO only|HTTP/SSE + STDIO|
|**Deployment**|Local process|Docker/K8s|
|**Multi-user**|❌ Khó (1 process/user)|✅ Shared service|
|**Security**|⚠️ Full host access|✅ Sandboxed|
|**Production**|❌ Chỉ dev/testing|✅ Production-ready|
|**Monitoring**|⚠️ Hạn chế|✅ Full observability|
|**Scalability**|❌ Không scale|✅ Horizontal scaling|
|**Maintenance**|✅ Không cần|⚠️ Phải tự maintain|
|**Best Practices**|5 điểm cơ bản|8 điểm đầy đủ|
|**Cost**|✅ Free|⚠️ Infrastructure cost|

---

## 4️⃣ DECISION FRAMEWORK

### ❓ Dùng Official Servers (npx/uvx) khi:

- ✅ Personal projects
- ✅ Development/Testing local
- ✅ Rapid prototyping
- ✅ Không xử lý sensitive data
- ✅ Single user (chỉ bạn dùng)

### ❓ Dùng Custom Docker Servers khi:

- ✅ Production deployment
- ✅ Multi-user application
- ✅ SaaS platform
- ✅ Cần custom business logic
- ✅ Security requirements cao
- ✅ Cần monitoring/scaling

---

## 5️⃣ CÔNG CỤ NÊN DÙNG

### Security

- **Snyk** - Dependency vulnerability scanning
- **Trivy** - Container security scanning
- **Vault** - Secrets management

### Monitoring

- **Sentry** - Error tracking
- **Prometheus + Grafana** - Metrics & dashboards
- **DataDog** - All-in-one observability
- **ELK Stack** - Log aggregation

### Load Testing

- **k6** - Modern load testing
- **Locust** - Python-based load testing
- **Apache JMeter** - Traditional load testing

### CI/CD

- **GitHub Actions** - Integrated với GitHub
- **GitLab CI** - Full DevOps platform
- **Jenkins** - Self-hosted automation

---

## 🎓 KẾT LUẬN

1. **Official servers** (npx/uvx): Dùng cho dev/testing, áp dụng **5 best practices cơ bản** về security
2. **Custom servers** (Docker): Dùng cho production, áp dụng **ĐẦY ĐỦ 8 NHÓM best practices**:
    - 🔐 Security (authentication, rate limiting, validation)
    - 📊 Monitoring (logging, health checks, metrics)
    - 🚀 Performance (resource limits, caching, async)
    - 🔄 Reliability (graceful shutdown, retry, circuit breaker)
    - 📦 Deployment (Docker, multi-stage build, CI/CD)
    - 🧪 Testing (integration, load testing)
    - 🎯 Checklist (40+ items pre-deployment)
    - 📖 Documentation (runbooks, playbooks)

**Câu trả lời trực tiếp:**

- **CÓ**, với official servers (npx/uvx) bạn vẫn cần best practices, nhưng **chỉ 5 điểm cơ bản** về security
- **BẮT BUỘC** phải áp dụng **ĐẦY ĐỦ 8 NHÓM best practices** nếu bạn deploy custom server lên production bằng Docker HTTP

Bạn có muốn tôi tạo **production-ready template** cho setup cụ thể của bạn không? Bao gồm tất cả file config + scripts + monitoring stack đầy đủ!


---
# Chi tiết cách best practices deploy MCP server có sẵn lên production



Hoàn hảo! Bây giờ tôi đã có đủ thông tin để trả lời đầy đủ câu hỏi. Để tôi tạo hướng dẫn chi tiết:

---

# 🎯 CÁCH DEPLOY OFFICIAL/PRE-BUILT MCP SERVERS LÊN PRODUCTION

Bạn hỏi **đúng trọng tâm**! Câu trả lời trước **CHƯA NÓI RÕ** điều này. Đây là **3 CÁCH CHÍNH** để deploy các MCP server có sẵn (official/community) lên production:

---

## 🔥 VẤNĐỀ CỐT LÕI

**Official MCP servers (npx/uvx) CHỈ HỖ TRỢ STDIO transport** → Không thể dùng trực tiếp cho production multi-user.

**Giải pháp:** Phải **CONVERT STDIO → HTTP/SSE** bằng 1 trong 3 cách sau:

---

## 📋 3 CÁCH CONVERT STDIO → HTTP PRODUCTION

### **Cách 1: MCP Proxy/Wrapper** ⭐ **KHUYẾN NGHỊ**

#### A. Sử dụng `mcp-proxy` (sparfenyuk)

**Nguyên lý:** Wrap STDIO server trong HTTP/Streamable HTTP gateway

```bash
# Install
uv tool install mcp-proxy

# Run STDIO server qua HTTP
mcp-proxy --port=8080 --host=0.0.0.0 npx -y @modelcontextprotocol/server-filesystem /data

# Multiple servers
mcp-proxy --port=8080 \
  --named-server filesystem 'npx -y @modelcontextprotocol/server-filesystem /data' \
  --named-server github 'npx -y @modelcontextprotocol/server-github'
```

**Docker Compose Production:**

```yaml
# docker-compose-mcp-proxy.yml
version: '3.8'

services:
  # 🔥 MCP Proxy Gateway
  mcp-proxy:
    image: ghcr.io/sparfenyuk/mcp-proxy:latest
    container_name: mcp-proxy
    ports:
      - "8080:8080"
    command: >
      --port=8080 
      --host=0.0.0.0
      --pass-environment
      --allow-origin "*"
      --named-server-config /config/servers.json
    volumes:
      - ./servers.json:/config/servers.json:ro
      - ./data:/data:ro  # Mount data cho filesystem server
    environment:
      - GITHUB_PERSONAL_ACCESS_TOKEN=${GITHUB_TOKEN}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/status"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    networks:
      - mcp-network

  # Nginx reverse proxy (optional nhưng nên có)
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      mcp-proxy:
        condition: service_healthy
    networks:
      - mcp-network

networks:
  mcp-network:
    driver: bridge
```

**servers.json config:**

```json
{
  "mcpServers": {
    "filesystem": {
      "enabled": true,
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/data"],
      "transportType": "stdio"
    },
    "github": {
      "enabled": true,
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "transportType": "stdio"
    },
    "memory": {
      "enabled": true,
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "transportType": "stdio"
    },
    "sqlite": {
      "enabled": true,
      "command": "uvx",
      "args": ["mcp-server-sqlite", "--db-path", "/data/database.db"],
      "transportType": "stdio"
    }
  }
}
```

**Client config (Cursor/Claude):**

```json
{
  "mcpServers": {
    "remote-filesystem": {
      "transport": "streamablehttp",
      "url": "https://your-domain.com/servers/filesystem/",
      "headers": {
        "Authorization": "Bearer your-token"
      }
    },
    "remote-github": {
      "transport": "streamablehttp",
      "url": "https://your-domain.com/servers/github/"
    }
  }
}
```

#### **✅ Ưu điểm Cách 1:**

- ✅ Đơn giản nhất, không cần code
- ✅ Support NHIỀU servers trong 1 proxy
- ✅ Compatible với MỌI STDIO servers
- ✅ Active maintenance (GitHub: sparfenyuk/mcp-proxy)
- ✅ Hỗ trợ OAuth2, SSL, CORS

#### **⚠️ Nhược điểm:**

- ⚠️ Thêm 1 layer proxy (latency nhỏ)
- ⚠️ Cần install Node.js/Python trong container

---

### **Cách 2: Ray Serve (Anyscale)** 🚀 **CHO SCALE LỚN**

**Nguyên lý:** Wrap STDIO Docker image thành HTTP service với auto-scaling

```python
# deploy_mcp_ray.py
from ray import serve
import subprocess
import json

@serve.deployment
class MCPStdioProxy:
    def __init__(self, command: list[str]):
        self.command = command
    
    async def __call__(self, request):
        # Spawn STDIO process
        process = subprocess.Popen(
            self.command,
            stdin=subprocess.PIPE,
            stdout=subprocess.PIPE,
            stderr=subprocess.PIPE
        )
        
        # Forward request to STDIO
        mcp_request = await request.json()
        process.stdin.write(json.dumps(mcp_request).encode())
        process.stdin.flush()
        
        # Get response
        response = process.stdout.readline().decode()
        return json.loads(response)

# Deploy
filesystem_server = MCPStdioProxy.bind([
    "npx", "-y", "@modelcontextprotocol/server-filesystem", "/data"
])

serve.run(filesystem_server, route_prefix="/mcp/filesystem")
```

**Deploy lên Anyscale:**

```bash
# Upload code
anyscale workspace upload deploy_mcp_ray.py

# Deploy với scaling
anyscale service deploy \
  --name mcp-filesystem \
  --num-replicas 3 \
  --ray-serve-config ray_serve_config.yaml
```

#### **✅ Ưu điểm Cách 2:**

- ✅ Auto-scaling tốt nhất
- ✅ GPU support (nếu cần)
- ✅ Load balancing built-in
- ✅ Monitoring dashboard

#### **⚠️ Nhược điểm:**

- ⚠️ Phức tạp hơn
- ⚠️ Chi phí cao (Anyscale platform)
- ⚠️ Overkill cho small-medium projects

---

### **Cách 3: Docker MCP Gateway** 🐳 **OFFICIAL DOCKER**

**Nguyên lý:** Docker’s official MCP solution

```yaml
# docker-compose-mcp-gateway.yml
version: '3.8'

services:
  # Docker MCP Gateway
  mcp-gateway:
    image: docker/mcp-gateway:latest
    ports:
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./mcp-catalog.json:/config/catalog.json
    environment:
      - MCP_CATALOG_PATH=/config/catalog.json
    restart: unless-stopped

  # Official MCP servers as containers
  mcp-filesystem:
    image: modelcontextprotocol/server-filesystem:latest
    volumes:
      - ./data:/data:ro
    environment:
      - ALLOWED_PATHS=/data

  mcp-github:
    image: modelcontextprotocol/server-github:latest
    environment:
      - GITHUB_PERSONAL_ACCESS_TOKEN=${GITHUB_TOKEN}
```

**mcp-catalog.json:**

```json
{
  "servers": {
    "filesystem": {
      "container": "mcp-filesystem",
      "enabled": true
    },
    "github": {
      "container": "mcp-github",
      "enabled": true
    }
  }
}
```

#### **✅ Ưu điểm Cách 3:**

- ✅ Official Docker solution
- ✅ Docker Desktop integration
- ✅ Secure secrets management
- ✅ Container isolation

#### **⚠️ Nhược điểm:**

- ⚠️ Còn mới (2025), chưa stable hoàn toàn
- ⚠️ Ít documentation
- ⚠️ Requires Docker Engine

---

## 📊 SO SÁNH 3 CÁCH

|Tiêu chí|MCP Proxy|Ray Serve|Docker Gateway|
|---|---|---|---|
|**Độ phức tạp**|⭐⭐ Đơn giản|⭐⭐⭐⭐⭐ Phức tạp|⭐⭐⭐ Trung bình|
|**Setup time**|10 phút|2-3 giờ|30 phút|
|**Multi-server**|✅ Excellent|✅ Good|✅ Good|
|**Auto-scaling**|❌ Manual|✅ Tự động|⚠️ Giới hạn|
|**Cost**|$ Rẻ|$$$ Đắt|$$ Vừa|
|**Production-ready**|✅ Sẵn sàng|✅ Enterprise|⚠️ Beta|
|**Best for**|SMB, Startups|Enterprise scale|Docker users|

---

## 🎯 KHUYẾN NGHỊ THEO USE CASE

### 🥇 **Cho hầu hết mọi người: MCP Proxy (Cách 1)**

**Khi nào dùng:**

- ✅ Startup/SMB
- ✅ < 1000 concurrent users
- ✅ Budget limited
- ✅ Cần deploy nhanh
- ✅ Multiple official servers

**Deploy script:**

```bash
#!/bin/bash
# deploy-mcp-proxy.sh

# 1. Install dependencies
uv tool install mcp-proxy

# 2. Create config
cat > servers.json << 'EOF'
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/data"]
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
EOF

# 3. Start with Docker Compose
docker compose -f docker-compose-mcp-proxy.yml up -d

# 4. Verify
curl http://localhost:8080/status
curl http://localhost:8080/servers/filesystem/health
```

---

### 🥈 **Cho enterprise scale: Ray Serve (Cách 2)**

**Khi nào dùng:**

- ✅ > 10,000 concurrent users
- ✅ Cần GPU
- ✅ Budget lớn
- ✅ Team DevOps có kinh nghiệm

---

### 🥉 **Cho Docker enthusiasts: Docker Gateway (Cách 3)**

**Khi nào dùng:**

- ✅ Đã dùng Docker Desktop
- ✅ All-in-one solution
- ✅ Chấp nhận beta software

---

## 🔐 PRODUCTION BEST PRACTICES (Áp dụng cho cả 3 cách)

### 1. **Security**

```yaml
# Thêm authentication middleware
services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx-auth.conf:/etc/nginx/nginx.conf:ro
```

**nginx-auth.conf:**

```nginx
server {
    listen 443 ssl;
    
    # API key authentication
    location / {
        if ($http_x_api_key != "your-secret-key") {
            return 403;
        }
        proxy_pass http://mcp-proxy:8080;
    }
    
    # Rate limiting
    limit_req_zone $binary_remote_addr zone=mcp:10m rate=10r/s;
    limit_req zone=mcp burst=20 nodelay;
}
```

### 2. **Monitoring**

```yaml
# Thêm vào docker-compose
services:
  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
```

**prometheus.yml:**

```yaml
scrape_configs:
  - job_name: 'mcp-proxy'
    static_configs:
      - targets: ['mcp-proxy:8080']
    metrics_path: '/metrics'
```

### 3. **Health Checks**

```python
# Thêm health check endpoint cho mcp-proxy
@app.get("/health")
async def health_check():
    checks = {}
    for server_name in servers:
        try:
            # Test server connection
            response = await test_server(server_name)
            checks[server_name] = "healthy" if response else "unhealthy"
        except:
            checks[server_name] = "down"
    
    all_healthy = all(v == "healthy" for v in checks.values())
    status_code = 200 if all_healthy else 503
    
    return JSONResponse(
        status_code=status_code,
        content={"status": "healthy" if all_healthy else "degraded", "checks": checks}
    )
```

### 4. **Backup & Recovery**

```bash
# backup-mcp-data.sh
#!/bin/bash

# Backup config
tar -czf mcp-backup-$(date +%Y%m%d).tar.gz \
  servers.json \
  docker-compose-mcp-proxy.yml \
  nginx.conf \
  .env.production

# Upload to S3
aws s3 cp mcp-backup-*.tar.gz s3://your-backup-bucket/
```

---

## 🚀 COMPLETE PRODUCTION TEMPLATE

Tôi tạo template đầy đủ cho bạn:

```bash
# Directory structure
mcp-production/
├── docker-compose-mcp-proxy.yml  # Main compose file
├── servers.json                   # MCP servers config
├── nginx.conf                     # Reverse proxy
├── prometheus.yml                 # Monitoring
├── .env.production                # Secrets (gitignored)
├── scripts/
│   ├── deploy.sh                  # Deploy script
│   ├── backup.sh                  # Backup script
│   ├── rollback.sh                # Rollback script
│   └── health-check.sh            # Health check script
└── ssl/
    ├── cert.pem
    └── key.pem
```

**Full docker-compose-mcp-proxy.yml:**

```yaml
version: '3.8'

services:
  mcp-proxy:
    image: ghcr.io/sparfenyuk/mcp-proxy:latest
    container_name: mcp-proxy
    command: >
      --port=8080 
      --host=0.0.0.0
      --pass-environment
      --allow-origin "*"
      --named-server-config /config/servers.json
    volumes:
      - ./servers.json:/config/servers.json:ro
      - ./data:/data:ro
    environment:
      - GITHUB_PERSONAL_ACCESS_TOKEN=${GITHUB_TOKEN}
      - LOG_LEVEL=INFO
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
    networks:
      - mcp-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
    depends_on:
      mcp-proxy:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - mcp-network

  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"
    restart: unless-stopped
    networks:
      - mcp-network

  grafana:
    image: grafana/grafana
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - mcp-network

volumes:
  prometheus-data:
  grafana-data:

networks:
  mcp-network:
    driver: bridge
```

**scripts/deploy.sh:**

```bash
#!/bin/bash
set -e

VERSION=${1:-latest}

echo "🚀 Deploying MCP Proxy v${VERSION}..."

# 1. Backup current
./scripts/backup.sh

# 2. Pull latest images
docker compose -f docker-compose-mcp-proxy.yml pull

# 3. Deploy
docker compose -f docker-compose-mcp-proxy.yml up -d

# 4. Wait for health
echo "⏳ Waiting for services..."
sleep 10

# 5. Health check
./scripts/health-check.sh

echo "✅ Deployment completed!"
```

---

## 📝 TÓM TẮT

**Câu trả lời ngắn gọn:**

Để deploy **official MCP servers** (npx/uvx) lên production:

1. **KHÔNG THỂ** dùng trực tiếp (chỉ có STDIO transport)
    
2. **PHẢI** convert sang HTTP bằng 1 trong 3 cách:
    
    - ⭐ **MCP Proxy** (sparfenyuk) - Khuyến nghị cho 90% cases
    - 🚀 **Ray Serve** - Cho enterprise scale
    - 🐳 **Docker Gateway** - Cho Docker users
3. **Best practice đầy đủ** như custom servers:
    
    - Security (auth, rate limit, SSL)
    - Monitoring (logs, metrics, alerts)
    - Performance (resource limits, caching)
    - Reliability (health checks, backup)

**Template đầy đủ:** Tôi đã tạo complete production setup phía trên!

Bạn muốn tôi tạo **working example cụ thể** cho setup của bạn không? (với filesystem + github + sqlite servers chẳng hạn?)


----


Đang đánh giá setup hiện tại so với best practices production:


[3 tools called]


# Đánh giá setup hiện tại

## Tóm tắt

Điểm: 5/10 — Đạt mức development/testing, chưa đủ cho production.

---

## Điểm mạnh

1. HTTP transport (streamable-http) — phù hợp multi-user
2. Stateless mode — dễ scale
3. OAuth 2.1 support — multi-user authentication
4. Docker Compose — dễ quản lý
5. Network isolation — Docker network riêng
6. Restart policy — tự động restart khi crash

---

## Thiếu sót so với best practices

### 1. Security (3/8)

| Tiêu chí | Trạng thái | Mô tả |
|----------|------------|-------|
| Authentication ở MCP level | ❌ Thiếu | Chỉ có OAuth ở app level, MCP server không có API key/auth |
| Rate limiting ở MCP level | ❌ Thiếu | Chỉ có ở app level, không có ở MCP server |
| Input validation | ❌ Thiếu | Không validate request từ app |
| Security headers | ❌ Thiếu | Không có CORS, HSTS, CSP |
| Non-root user | ❌ Thiếu | Dockerfile chạy root (security risk) |
| Secrets management | ⚠️ Cơ bản | Dùng env vars, chưa có Vault/secrets manager |
| SSL/TLS | ❌ Thiếu | Không có HTTPS |
| API key protection | ❌ Thiếu | MCP server expose trực tiếp, không có gateway |

### 2. Monitoring & Logging (1/6)

| Tiêu chí | Trạng thái | Mô tả |
|----------|------------|-------|
| Health checks | ❌ Disabled | Đã comment out trong docker-compose |
| Prometheus metrics | ❌ Thiếu | Không expose `/metrics` |
| Grafana dashboards | ❌ Thiếu | Không có visualization |
| Structured logging | ❌ Thiếu | Chỉ có LOG_LEVEL, không có JSON logs |
| Error tracking | ❌ Thiếu | Không có Sentry/DataDog |
| Request logging | ⚠️ Cơ bản | Có logs nhưng không structured |

### 3. Performance & Scalability (2/6)

| Tiêu chí | Trạng thái | Mô tả |
|----------|------------|-------|
| Resource limits | ❌ Thiếu | Không có CPU/memory limits |
| Caching | ❌ Thiếu | Không có Redis cache cho MCP |
| Connection pooling | ❌ Thiếu | Không config connection reuse |
| Auto-scaling | ❌ Thiếu | Không có horizontal scaling |
| Load balancing | ❌ Thiếu | Không có Nginx/LB |
| Async processing | ✅ OK | Stateless mode hỗ trợ async |

### 4. Reliability (2/6)

| Tiêu chí | Trạng thái | Mô tả |
|----------|------------|-------|
| Graceful shutdown | ❌ Thiếu | Không handle SIGTERM |
| Retry logic | ❌ Thiếu | Không có retry cho failed requests |
| Circuit breaker | ❌ Thiếu | Không có circuit breaker |
| Error handling | ❌ Thiếu | Không có global error handler |
| Backup strategy | ❌ Thiếu | Không có backup plan |
| Restart policy | ✅ OK | Có `unless-stopped` |

### 5. Deployment (2/8)

| Tiêu chí | Trạng thái | Mô tả |
|----------|------------|-------|
| Multi-stage Dockerfile | ❌ Thiếu | Dockerfile đơn giản, không optimize |
| Nginx reverse proxy | ❌ Thiếu | Expose trực tiếp port 30024 |
| SSL/TLS | ❌ Thiếu | Không có HTTPS |
| CI/CD pipeline | ❌ Thiếu | Không có automated deployment |
| Version tagging | ❌ Thiếu | Không tag Docker images |
| Rollback strategy | ❌ Thiếu | Không có rollback plan |
| Blue-green deployment | ❌ Thiếu | Không có zero-downtime deploy |
| Docker Compose | ✅ OK | Có docker-compose config |

### 6. Testing (0/3)

| Tiêu chí | Trạng thái | Mô tả |
|----------|------------|-------|
| Integration tests | ❌ Thiếu | Không có tests cho MCP integration |
| Load testing | ❌ Thiếu | Không có k6/Locust tests |
| Health check tests | ❌ Thiếu | Healthcheck bị disable |

---

## Checklist nhanh

### Critical (phải có cho production)

- [ ] Enable health checks
- [ ] Thêm resource limits (CPU/memory)
- [ ] Non-root user trong Dockerfile
- [ ] Nginx reverse proxy với SSL
- [ ] API authentication ở MCP level
- [ ] Rate limiting ở MCP level
- [ ] Structured logging (JSON)
- [ ] Prometheus metrics

### Important (nên có)

- [ ] Multi-stage Dockerfile
- [ ] Graceful shutdown
- [ ] Retry logic với exponential backoff
- [ ] Error tracking (Sentry)
- [ ] Backup strategy
- [ ] CI/CD pipeline
- [ ] Load testing

### Nice to have

- [ ] Grafana dashboards
- [ ] Circuit breaker
- [ ] Blue-green deployment
- [ ] Auto-scaling config

---

## So sánh với best practices

| Nhóm | Best Practices | Setup hiện tại | Điểm |
|------|---------------|----------------|------|
| Security | 8 điểm | 3 điểm | 37.5% |
| Monitoring | 6 điểm | 1 điểm | 16.7% |
| Performance | 6 điểm | 2 điểm | 33.3% |
| Reliability | 6 điểm | 2 điểm | 33.3% |
| Deployment | 8 điểm | 2 điểm | 25% |
| Testing | 3 điểm | 0 điểm | 0% |
| Tổng | 37 điểm | 10 điểm | 27% |

---

## Kết luận

- Đạt: Development/testing, multi-user support, Docker setup cơ bản.
- Chưa đạt: Security, monitoring, performance optimization, reliability, production deployment.

Khuyến nghị:
- Nếu đang development/testing: OK, có thể tiếp tục.
- Nếu production: Cần bổ sung ít nhất các mục Critical trước khi deploy.

---

## Template production-ready

Bạn có muốn tôi tạo:
1. Production-ready docker-compose với đầy đủ best practices
2. Dockerfile cải tiến (multi-stage, non-root user)
3. Nginx config với SSL
4. Prometheus + Grafana setup
5. Health checks và monitoring scripts

Bạn muốn tôi tạo template nào trước?


---
<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Báo Cáo Kỹ Thuật: Tích Hợp LangChain/LangGraph với MCP Server Bên Ngoài

## Tóm Tắt Điều Hành

Báo cáo này phân tích chiến lược tích hợp LangChain và LangGraph với MCP servers bên ngoài trong môi trường production, tập trung vào kiến trúc an toàn, lựa chọn transport protocol, và patterns triển khai thực tế. Phân tích đề xuất framework ra quyết định dựa trên yêu cầu về latency, security, và scalability, với implementation examples cho fintech AI agents.

***

## 1. Hệ Sinh Thái MCP Server Bên Ngoài

### 1.1 Phân Loại và Ví Dụ Thực Tế

Hệ sinh thái MCP server bên ngoài phát triển mạnh mẽ với hơn 200+ implementations, được phân loại theo use case:[^1][^2]


| Category | Ví Dụ Nổi Bật | Transport | Authentication | Use Case Fintech |
| :-- | :-- | :-- | :-- | :-- |
| **Cloud Infrastructure** | AWS, GCP, Azure MCP | HTTP/SSE | OAuth 2.0 | Infrastructure management |
| **Databases** | PostgreSQL, ClickHouse, Couchbase | Stdio/HTTP | API Key | Analytics \& reporting |
| **Financial APIs** | Mercado Pago, Stripe MCP | HTTP | API Key | Payment processing |
| **Observability** | Datadog, Cloudflare MCP | HTTP | Bearer Token | Monitoring \& alerting |
| **Business Tools** | Salesforce, Notion, Zapier | HTTP | OAuth | CRM \& workflow automation |

**Các server production-ready**:[^3][^2]

- **Mercado Pago MCP**: `https://mcp.mercadopago.com/mcp` - Payment processing với API key authentication
- **AWS MCP**: `npx @awslabs/mcp` - AWS resource management qua SSO
- **ClickHouse MCP**: `mcp-clickhouse` - High-performance analytics queries
- **Datadog MCP**: Observability data integration cho AI-driven incident response


### 1.2 Discovery và Registry

Hai cơ chế discovery chính:[^4]

- **Static Configuration**: Định nghĩa server URLs và credentials trong config file
- **Dynamic Service Discovery**: Sử dụng service registry (Consul, Eureka) cho auto-scaling deployments

```python
# Static configuration pattern
mcp_servers = {
    "finance_api": {
        "server_url": "https://mcp.financeprovider.com/sse",
        "headers": {"Authorization": "Bearer ${FINANCE_API_KEY}"}
    },
    "internal_db": {
        "command": "python",
        "args": ["mcp_postgres_server.py"],
        "transport": "stdio"
    }
}
```


***

## 2. Kiến Trúc Tích Hợp với LangChain/LangGraph

### 2.1 Multi-Server MCP Client Pattern

LangChain cung cấp `MultiServerMCPClient` để quản lý concurrent connections:[^5]

```python
from langchain_mcp_adapters.client import MultiServerMCPClient
from langgraph.prebuilt import create_react_agent
import asyncio

async def create_fintech_agent():
    # Khởi tạo client với multiple external servers
    async with MultiServerMCPClient({
        "market_data": {
            "transport": "http",
            "server_url": "https://mcp.marketdata.com/sse",
            "headers": {"X-API-Key": os.getenv("MARKET_DATA_KEY")}
        },
        "risk_engine": {
            "transport": "stdio",
            "command": "docker",
            "args": ["run", "--rm", "risk-mcp-server:latest"]
        },
        "compliance_api": {
            "transport": "http",
            "server_url": "https://mcp.compliance.gov/api",
            "headers": {"Authorization": f"Bearer {os.getenv('COMPLIANCE_TOKEN')}"}
        }
    }) as client:
        
        # Auto-discover và convert tools
        tools = await client.get_tools()
        
        # Tạo LangGraph agent với checkpointing
        agent = create_react_agent(
            model="gpt-4o",
            tools=tools,
            checkpointer=MemorySaver()
        )
        
        return agent

# Sử dụng trong async context
agent = asyncio.run(create_fintech_agent())
```

**Lợi ích kiến trúc**:[^4]

- **Connection Pooling**: Tái sử dụng connections giữa các tool calls
- **Error Isolation**: Lỗi ở một server không ảnh hưởng others
- **Credential Management**: Secrets không expose trong agent code


### 2.2 Stateful Orchestration với External Tools

LangGraph cho phép orchestration phức tạp với external MCP tools:[^6]

```python
from langgraph.graph import StateGraph, START, END
from typing import TypedDict, Annotated
from langgraph.checkpoint.memory import MemorySaver

class TradingState(TypedDict):
    order_params: dict
    risk_check: dict
    compliance_approval: bool
    execution_result: Optional[dict]
    error_count: int

# Node implementations gọi external MCP tools
async def risk_check_node(state: TradingState, config: RunnableConfig):
    tools = config["configurable"]["mcp_tools"]
    
    # Gọi external risk engine
    risk_result = await tools["calculate_risk"].ainvoke({
        "portfolio_id": state["order_params"]["portfolio_id"],
        "order_value": state["order_params"]["value"]
    })
    
    return {"risk_check": risk_result, "error_count": 0}

async def compliance_node(state: TradingState, config: RunnableConfig):
    if state["risk_check"]["risk_score"] > 0.8:
        tools = config["configurable"]["mcp_tools"]
        
        # Gọi external compliance API
        approval = await tools["request_approval"].ainvoke({
            "risk_score": state["risk_check"]["risk_score"],
            "trader_id": config["configurable"]["user_id"]
        })
        
        return {"compliance_approval": approval["approved"]}
    
    return {"compliance_approval": True}

# Conditional routing dựa trên external tool responses
def route_execution(state: TradingState) -> str:
    if not state["compliance_approval"]:
        return "human_review"
    elif state["risk_check"]["risk_score"] > 0.9:
        return "reject_order"
    else:
        return "execute_order"

# Build graph
builder = StateGraph(TradingState)
builder.add_node("risk_check", risk_check_node)
builder.add_node("compliance", compliance_node)
builder.add_node("execute", execution_node)
builder.add_node("human_review", human_review_node)

builder.add_edge(START, "risk_check")
builder.add_edge("risk_check", "compliance")
builder.add_conditional_edges("compliance", route_execution)
builder.add_edge("execute", END)
builder.add_edge("human_review", END)

# Compile với checkpointing
graph = builder.compile(checkpointer=MemorySaver())
```


***

## 3. Transport Protocol: Lựa Chọn và Trade-offs

### 3.1 So Sánh Chi Tiết Transport Options

| Transport | Latency | Remote Access | Multi-Client | Security Model | Use Case Phù Hợp |
| :-- | :-- | :-- | :-- | :-- | :-- |
| **Stdio** | <1ms | ❌ No | ❌ No | Process isolation | Local CLI tools, containers |
| **HTTP** | 10-50ms | ✅ Yes | ✅ Yes | API keys, OAuth | Cloud APIs, microservices |
| **SSE** | 20-100ms | ✅ Yes | ✅ Yes | Bearer tokens | Real-time streaming |
| **StreamableHTTP** | 10-30ms | ✅ Yes | ✅ Yes | mTLS, JWT | Production web services |

**Performance Benchmark**:[^7]

- **In-Memory**: 500x nhanh hơn stdio (chỉ cho same-process)
- **Stdio**: 10,000+ ops/sec, phù hợp cho local tools
- **HTTP**: 100-1,000 ops/sec, phụ thuộc network conditions


### 3.2 Decision Framework

```python
def select_transport(requirements: dict) -> str:
    """
    Framework chọn transport dựa trên yêu cầu
    """
    if requirements.get("local_only") and requirements.get("low_latency"):
        return "stdio"  # Local database connectors, CLI tools
    
    elif requirements.get("real_time") and requirements.get("streaming"):
        return "sse"  # Market data streaming, log aggregation
    
    elif requirements.get("web_access") and requirements.get("scalable"):
        return "streamable_http"  # Cloud APIs, multi-tenant services
    
    elif requirements.get("simple_web"):
        return "http"  # REST-like MCP servers
    
    else:
        return "streamable_http"  # Default for production
```

**Ví dụ áp dụng**:[^8]

- **Stdio**: `mcp-postgres-server` chạy trong Docker container, agent spawn process
- **StreamableHTTP**: `https://mcp.financeapi.com/sse` cho global market data access
- **HTTP**: `https://mcp.compliance.gov/api` cho regulatory reporting APIs

***

## 4. Security Framework cho External MCP

### 4.1 Multi-Layer Security Architecture

Security implementation phải bao gồm 5 lớp:[^9][^6]

```
┌─────────────────────────────────────────────────────────────┐
│                    Layer 5: Application Security            │
│                    (Input validation, Rate limiting)        │
├─────────────────────────────────────────────────────────────┤
│                    Layer 4: Transport Security              │
│                    (mTLS, WAF, API Gateway)                 │
├─────────────────────────────────────────────────────────────┤
│                    Layer 3: Authentication                  │
│                    (OAuth 2.0, JWT, API Keys)               │
├─────────────────────────────────────────────────────────────┤
│                    Layer 2: Network Isolation               │
│                    (VPC, Private Subnets, IP Whitelisting)  │
├─────────────────────────────────────────────────────────────┤
│                    Layer 1: Secrets Management              │
│                    (HashiCorp Vault, AWS Secrets Manager)   │
└─────────────────────────────────────────────────────────────┘
```


### 4.2 Authentication Patterns

**Pattern 1: API Key Rotation**

```python
from langchain_mcp_adapters.client import MultiServerMCPClient
import boto3

async def get_rotated_api_key(service_name: str) -> str:
    """Lấy API key từ Secrets Manager với auto-rotation"""
    secrets_client = boto3.client('secretsmanager')
    response = secrets_client.get_secret_value(SecretId=f"mcp/{service_name}")
    return json.loads(response['SecretString'])['api_key']

# Sử dụng dynamic key fetching
client = MultiServerMCPClient({
    "market_data": {
        "transport": "http",
        "server_url": "https://mcp.marketdata.com/sse",
        "headers": lambda: {"X-API-Key": await get_rotated_api_key("market_data")}
    }
})
```

**Pattern 2: OAuth 2.0 Flow**

```python
from authlib.integrations.httpx_client import OAuth2Client

async def get_oauth_token() -> str:
    """Lấy OAuth token với refresh logic"""
    client = OAuth2Client(
        client_id=os.getenv("MCP_CLIENT_ID"),
        client_secret=os.getenv("MCP_CLIENT_SECRET"),
        token_endpoint="https://auth.provider.com/oauth/token"
    )
    token = await client.fetch_token()
    return token["access_token"]

# MCP client với OAuth
client = MultiServerMCPClient({
    "compliance_api": {
        "transport": "http",
        "server_url": "https://mcp.compliance.gov/api",
        "headers": lambda: {"Authorization": f"Bearer {await get_oauth_token()}"}
    }
})
```

**Pattern 3: mTLS cho Internal Services**

```python
# MCP server config với mTLS
client = MultiServerMCPClient({
    "internal_risk_engine": {
        "transport": "http",
        "server_url": "https://risk-engine.internal:8443/mcp",
        "ssl_cert": "/certs/client.crt",
        "ssl_key": "/certs/client.key",
        "ca_cert": "/certs/ca.crt"
    }
})
```


### 4.3 Network Security Best Practices

**Micro-segmentation**:[^6]

- Deploy MCP servers trong private subnets không có internet access
- Sử dụng VPC endpoints cho AWS services
- Implement IP whitelisting tại security group level

**API Gateway Pattern**:

```
[LangGraph Agent] → [API Gateway] → [WAF] → [MCP Server]
                      (Rate limit)    (DDoS protection)
```


***

## 5. Production Deployment Strategies

### 5.1 Container Orchestration với Kubernetes

**Deployment YAML**:[^10][^11]

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-finance-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mcp-finance
  template:
    metadata:
      labels:
        app: mcp-finance
    spec:
      containers:
      - name: mcp-server
        image: mcp-finance:latest
        ports:
        - containerPort: 8080
        env:
        - name: API_KEY
          valueFrom:
            secretKeyRef:
              name: mcp-secrets
              key: finance-api-key
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mcp-secrets
              key: database-url
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: mcp-finance-service
spec:
  selector:
    app: mcp-finance
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
```

**Horizontal Pod Autoscaler**:[^12]

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mcp-finance-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mcp-finance-server
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Pods
    pods:
      metric:
        name: http_requests_per_second
      target:
        type: AverageValue
        averageValue: "1000"
```


### 5.2 CI/CD Pipeline

**GitHub Actions Workflow**:[^12]

```yaml
name: Deploy MCP Server

on:
  push:
    branches: [main]
    paths:
      - 'mcp-servers/finance/**'

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install -r requirements-dev.txt
    
    - name: Run security scan
      run: |
        bandit -r mcp_servers/ -f json -o bandit-report.json
    
    - name: Run tests
      run: |
        pytest tests/ --cov=mcp_servers --cov-report=xml
    
    - name: Build Docker image
      run: |
        docker build -t mcp-finance:${{ github.sha }} \
                     -t mcp-finance:latest .
    
    - name: Push to registry
      run: |
        echo ${{ secrets.DOCKER_PASSWORD }} |
<span style="display:none">[^13][^14][^15][^16][^17][^18][^19][^20][^21]</span>

<div align="center">⁂</div>

[^1]: https://mcpservers.org
[^2]: https://github.com/punkpeye/awesome-mcp-servers
[^3]: https://github.com/jaw9c/awesome-remote-mcp-servers
[^4]: https://latenode.com/blog/ai-frameworks-technical-infrastructure/langgraph-multi-agent-orchestration/langgraph-mcp-integration-complete-model-context-protocol-setup-guide-working-examples-2025
[^5]: https://changelog.langchain.com/announcements/mcp-adapters-for-langchain-and-langgraph
[^6]: https://sparkco.ai/blog/enterprise-mcp-security-best-practices-an-in-depth-guide
[^7]: https://www.reddit.com/r/mcp/comments/1kssu2g/mcp_server_transports_inmemory_vs_httpsse_vs_stdio/
[^8]: https://mcpcat.io/guides/comparing-stdio-sse-streamablehttp/
[^9]: https://modelcontextprotocol.io/specification/draft/basic/security_best_practices
[^10]: https://milvus.io/ai-quick-reference/whats-the-best-way-to-deploy-an-model-context-protocol-mcp-server-to-production
[^11]: https://github.com/BigUncle/MCP-Server-Unified-Deployment
[^12]: https://collabnix.com/how-to-use-mcp-in-production-a-practical-guide/
[^13]: https://modelcontextprotocol.io/examples
[^14]: https://www.k2view.com/blog/awesome-mcp-servers
[^15]: https://docs.databricks.com/aws/en/generative-ai/mcp/external-mcp
[^16]: https://www.youtube.com/watch?v=hblrscBO2x0
[^17]: https://mcp-framework.com/docs/Transports/stdio-transport/
[^18]: https://obot.ai/resources/learning-center/mcp-servers/
[^19]: https://docs.opensearch.org/latest/vector-search/ai-search/agentic-search/mcp-server/
[^20]: https://generect.com/blog/langgraph-mcp/
[^21]: https://modelcontextprotocol.io/specification/2025-03-26/basic/transports```


---
<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# 📚 Hướng Dẫn Chi Tiết: Triển Khai MCP Server Bên Ngoài vào Production

Dựa trên code của bạn (LangChain + LangGraph + MCP), tôi sẽ cung cấp **step-by-step implementation guide** từ local development đến production.

***

## 📋 Table of Contents

1. [Kiến Trúc Tích Hợp MCP Bên Ngoài](#1-ki%E1%BA%BFn-tr%C3%BAc-t%C3%ADch-h%E1%BB%A3p-mcp-b%C3%AAn-ngo%C3%A0i)
2. [Setup Local Development](#2-setup-local-development)
3. [Triển Khai MCP Server Bên Ngoài](#3-tri%E1%BB%83n-khai-mcp-server-b%C3%AAn-ngo%C3%A0i)
4. [Integration với Existing Code](#4-integration-v%E1%BB%9Bi-existing-code)
5. [Production Deployment](#5-production-deployment)
6. [Monitoring \& Debugging](#6-monitoring--debugging)

***

## 1. Kiến Trúc Tích Hợp MCP Bên Ngoài

### 1.1 Topology Hiện Tại vs Sau Khi Integrate

**Current Architecture (Local + In-Process)**:

```
┌─────────────────────────────────────┐
│    FastAPI Application              │
├─────────────────────────────────────┤
│  ┌──────────────────────────────┐   │
│  │ MultiAgentOrchestrator       │   │
│  │  ├── PlannerAgent            │   │
│  │  ├── RouterAgent             │   │
│  │  ├── ExecutorAgent           │   │
│  │  └── SynthesizerAgent        │   │
│  └──────────────────────────────┘   │
│           ▼                          │
│  ┌──────────────────────────────┐   │
│  │ MCP Tool Adapters (Local)    │   │
│  │ ├── BrowserMCP (Playwright)  │   │
│  │ ├── MathMCP                  │   │
│  │ ├── DriveMCP (OAuth)         │   │
│  │ └── YoutubeMCP               │   │
│  └──────────────────────────────┘   │
│           ▼                          │
│  ┌──────────────────────────────┐   │
│  │ Redis Pub/Sub (MessageBus)   │   │
│  └──────────────────────────────┘   │
└─────────────────────────────────────┘
```

**After Integration (Hybrid - Local + External)**:

```
┌────────────────────────────────────────────────────────────────┐
│                   FastAPI Application                          │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ LangGraph Orchestrator (New)                            │ │
│  │  ├── StateGraph with checkpointing                      │ │
│  │  └── Conditional edge routing                          │ │
│  └──────────────────────────────────────────────────────────┘ │
│                           ▼                                    │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ MultiServerMCPClient (NEW - Central Hub)                │ │
│  │  ├── Internal MCP Servers (Stdio)                       │ │
│  │  │   ├── BrowserMCP                                     │ │
│  │  │   ├── MathMCP                                        │ │
│  │  │   └── DriveMCP                                       │ │
│  │  │                                                       │ │
│  │  └── External MCP Servers (HTTP)                        │ │
│  │      ├── FinanceAPI MCP (Remote)                        │ │
│  │      ├── ComplianceAPI MCP (Remote)                     │ │
│  │      └── DataWarehouse MCP (Remote)                     │ │
│  └──────────────────────────────────────────────────────────┘ │
│                           ▼                                    │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │           LangChain Agents & Tools                      │  │
│  │  ├── create_react_agent()                              │  │
│  │  └── Tool Executor with interceptors                   │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                │
└────────────────────────────────────────────────────────────────┘
         ▲                              ▲                 ▲
         │ (Stdio)                      │ (HTTP)         │ (HTTP)
         │                              │                │
    ┌────┴───────┐            ┌────────┴──────┐    ┌────┴─────────┐
    │  Internal   │            │   External    │    │  External    │
    │   MCP       │            │  FinanceAPI   │    │ Compliance   │
    │  Servers    │            │     MCP       │    │     MCP      │
    └─────────────┘            └───────────────┘    └──────────────┘
    (Same Process)          (Remote Services)    (Remote Services)
```


### 1.2 Communication Protocols

| Layer | Protocol | Topology | Use Case |
| :-- | :-- | :-- | :-- |
| **Internal MCPs** | Stdio (JSON-RPC) | Subprocess | Fast, co-located tools |
| **External MCPs** | Streamable HTTP | Network | Scalable remote services |
| **Tools ↔ Agent** | Python async/await | In-process | Tool discovery \& execution |
| **State Management** | LangGraph Checkpointer | Database (optional) | Persistence \& recovery |


***

## 2. Setup Local Development

### 2.1 Install Dependencies

```bash
# Update pyproject.toml
cat >> pyproject.toml << 'EOF'

# Existing dependencies
langchain = ">=0.3.27"
langgraph = ">=0.6.8"
langchain-openai = ">=0.3.33"

# NEW: MCP Adapters (the key integration library)
langchain-mcp-adapters = ">=0.5.0"

# FastMCP for creating MCP servers
fastmcp = ">=0.5.0"

# For external HTTP MCP connections
httpx = ">=0.27.0"

# For async context management
aiohttp = ">=3.10.0"

EOF

# Install
pip install -e .
```


### 2.2 Project Structure After Integration

```
your_project/
├── app/
│   ├── main.py                          # FastAPI entry
│   ├── config.py                        # MCP config (NEW)
│   ├── module/
│   │   ├── mcp/                         # (NEW) MCP Integration
│   │   │   ├── __init__.py
│   │   │   ├── client_manager.py        # MultiServerMCPClient wrapper
│   │   │   ├── interceptors.py          # Tool interceptors (auth, retry)
│   │   │   ├── callbacks.py             # Progress/logging callbacks
│   │   │   └── servers/                 # Internal MCP server definitions
│   │   │       ├── math_server.py
│   │   │       ├── browser_server.py
│   │   │       └── drive_server.py
│   │   │
│   │   ├── multi_agents/                # Existing agents
│   │   │   ├── agent_entrypoint.py      # MODIFIED: Use MCP client
│   │   │   └── ...
│   │   │
│   │   └── workflows/                   # (NEW) LangGraph workflows
│   │       ├── __init__.py
│   │       ├── complex_analysis.py      # Use MCP tools
│   │       └── financial_report.py
│   │
│   └── services/
│       └── mcp_service.py               # (NEW) Service layer
│
├── config/
│   ├── mcp_servers.yaml                 # External server configs
│   ├── .env.example
│   └── .env (production)
│
├── tests/
│   └── test_mcp_integration.py
│
└── docker-compose.yml                    # For MCP servers
```


***

## 3. Triển Khai MCP Server Bên Ngoài

### 3.1 Step 1: Tạo External MCP Server (Finance Example)

**File: `mcp_servers/finance_server.py`** (Deployed separately)

```python
#!/usr/bin/env python3
"""
External Finance MCP Server
Deploy này trên remote server, chạy as standalone service
"""

from fastmcp import FastMCP
from typing import Optional
import httpx
import os

# Initialize FastMCP server
mcp = FastMCP("FinanceAPI")

# Database connection (example with environment variable)
FINANCE_API_BASE = os.getenv("FINANCE_API_BASE", "https://api.financeprovider.com")
API_KEY = os.getenv("FINANCE_API_KEY")

# ==================== TOOLS ====================

@mcp.tool()
async def get_stock_price(ticker: str, date: Optional[str] = None) -> dict:
    """
    Lấy giá cổ phiếu hiện tại hoặc lịch sử
    
    Args:
        ticker: Mã cổ phiếu (VD: VNM, FPT)
        date: Ngày (YYYY-MM-DD), nếu None thì lấy hiện tại
    
    Returns:
        dict: {price, change_percent, timestamp, volume}
    """
    async with httpx.AsyncClient() as client:
        params = {"ticker": ticker}
        if date:
            params["date"] = date
        
        response = await client.get(
            f"{FINANCE_API_BASE}/stocks/price",
            params=params,
            headers={"X-API-Key": API_KEY}
        )
        response.raise_for_status()
        return response.json()

@mcp.tool()
async def get_financial_statements(
    ticker: str, 
    year: int,
    statement_type: str = "income"  # income, balance_sheet, cash_flow
) -> dict:
    """
    Lấy báo cáo tài chính
    
    Args:
        ticker: Mã cổ phiếu
        year: Năm tài chính
        statement_type: Loại báo cáo
    
    Returns:
        dict: Structured financial data
    """
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{FINANCE_API_BASE}/financials/{ticker}",
            params={
                "year": year,
                "type": statement_type
            },
            headers={"X-API-Key": API_KEY}
        )
        response.raise_for_status()
        return response.json()

@mcp.tool()
async def calculate_valuation(
    ticker: str,
    method: str = "dcf"  # dcf, pe_ratio, peg
) -> dict:
    """
    Tính định giá cổ phiếu
    
    Args:
        ticker: Mã cổ phiếu
        method: Phương pháp định giá
    
    Returns:
        dict: Valuation result with fair_value, upside_downside, confidence
    """
    async with httpx.AsyncClient() as client:
        response = await client.post(
            f"{FINANCE_API_BASE}/valuation/calculate",
            json={
                "ticker": ticker,
                "method": method
            },
            headers={"X-API-Key": API_KEY}
        )
        response.raise_for_status()
        return response.json()

@mcp.tool()
async def search_analyst_reports(
    ticker: str,
    limit: int = 10
) -> list:
    """
    Tìm kiếm báo cáo phân tích
    
    Args:
        ticker: Mã cổ phiếu
        limit: Số báo cáo tối đa
    
    Returns:
        list: [{title, analyst, rating, date, url}]
    """
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{FINANCE_API_BASE}/reports/search",
            params={
                "ticker": ticker,
                "limit": limit
            },
            headers={"X-API-Key": API_KEY}
        )
        response.raise_for_status()
        return response.json()["reports"]

# ==================== RESOURCES ====================

@mcp.resource("file:///reports")
async def list_reports() -> str:
    """Danh sách các báo cáo có sẵn"""
    async with httpx.AsyncClient() as client:
        response = await client.get(
            f"{FINANCE_API_BASE}/reports/list",
            headers={"X-API-Key": API_KEY}
        )
        response.raise_for_status()
        reports = response.json()
        return "\n".join([f"- {r['name']}" for r in reports])

# ==================== MAIN ====================

if __name__ == "__main__":
    # Run with Streamable HTTP transport (cho production)
    import uvicorn
    
    app = mcp.mcp_asgi_app()
    
    uvicorn.run(
        app,
        host="0.0.0.0",  # Bind to all interfaces (use reverse proxy in production)
        port=8001,       # Custom port
        log_level="info"
    )
    
    # Hoặc dùng stdio transport cho local testing:
    # mcp.run(transport="stdio")
```


### 3.2 Step 2: Deploy External MCP Server

**File: `Dockerfile` (cho Finance MCP Server)**

```dockerfile
FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy server code
COPY mcp_servers/ .

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD python -c "import httpx; httpx.get('http://localhost:8001/health').raise_for_status()"

# Expose MCP endpoint
EXPOSE 8001

# Run server
CMD ["python", "-m", "uvicorn", "finance_server:app", \
     "--host", "0.0.0.0", "--port", "8001"]
```

**File: `docker-compose.yml` (Local + External Services)**

```yaml
version: '3.8'

services:
  # External Finance MCP Server (simulate remote)
  finance-mcp-server:
    build:
      context: ./mcp_servers
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    environment:
      FINANCE_API_BASE: "https://api.example.com"
      FINANCE_API_KEY: "${FINANCE_API_KEY}"
    networks:
      - mcp-network
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # External Compliance MCP Server
  compliance-mcp-server:
    build:
      context: ./mcp_servers/compliance
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    environment:
      COMPLIANCE_API_BASE: "https://regulatory-api.example.com"
      COMPLIANCE_API_KEY: "${COMPLIANCE_API_KEY}"
    networks:
      - mcp-network
    restart: always

  # Your main FastAPI application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # MCP Server URLs (externally accessible)
      MCP_FINANCE_URL: "http://finance-mcp-server:8001/mcp"
      MCP_COMPLIANCE_URL: "http://compliance-mcp-server:8002/mcp"
      # Internal API keys
      FINANCE_API_KEY: "${FINANCE_API_KEY}"
      COMPLIANCE_API_KEY: "${COMPLIANCE_API_KEY}"
    depends_on:
      finance-mcp-server:
        condition: service_healthy
      compliance-mcp-server:
        condition: service_healthy
    networks:
      - mcp-network
    restart: always

networks:
  mcp-network:
    driver: bridge
```


***

## 4. Integration với Existing Code

### 4.1 Step 1: Create MCP Client Manager

**File: `app/module/mcp/client_manager.py`** (Wrapper around MultiServerMCPClient)

```python
"""
MCP Client Manager - Central hub cho tất cả MCP servers
"""

from typing import Dict, List, Optional, Any
import os
import logging
from functools import lru_cache
from langchain_mcp_adapters.client import MultiServerMCPClient
from langchain_mcp_adapters.callbacks import Callbacks, CallbackContext
from langchain_mcp_adapters.interceptors import MCPToolCallRequest
from langchain_core.tools import BaseTool
import asyncio

logger = logging.getLogger(__name__)

class MCPClientManager:
    """
    Quản lý tất cả MCP connections (internal + external)
    
    Features:
    - Connection pooling
    - Error handling & retry
    - Tool caching
    - Interceptors for auth/context
    """
    
    def __init__(self):
        self._client: Optional[MultiServerMCPClient] = None
        self._tools_cache: Optional[List[BaseTool]] = None
        self._lock = asyncio.Lock()
    
    async def initialize(self) -> None:
        """Initialize MCP client with all servers"""
        if self._client is not None:
            return
        
        async with self._lock:
            if self._client is not None:
                return
            
            # Build server configurations
            servers_config = self._build_servers_config()
            
            # Create client with interceptors
            self._client = MultiServerMCPClient(
                servers=servers_config,
                tool_interceptors=[
                    self._auth_interceptor,
                    self._retry_interceptor,
                    self._context_interceptor,
                ],
                callbacks=Callbacks(
                    on_logging_message=self._on_logging_message,
                    on_progress=self._on_progress,
                ),
            )
            
            logger.info("MCP Client initialized successfully")
    
    def _build_servers_config(self) -> Dict[str, Dict[str, Any]]:
        """Build MCP server configurations from environment"""
        
        return {
            # ===== INTERNAL MCP SERVERS (Stdio) =====
            "math": {
                "transport": "stdio",
                "command": "python",
                "args": [
                    os.path.join(os.path.dirname(__file__), "servers/math_server.py")
                ],
            },
            "browser": {
                "transport": "stdio",
                "command": "python",
                "args": [
                    os.path.join(os.path.dirname(__file__), "servers/browser_server.py")
                ],
            },
            "drive": {
                "transport": "stdio",
                "command": "python",
                "args": [
                    os.path.join(os.path.dirname(__file__), "servers/drive_server.py")
                ],
            },
            
            # ===== EXTERNAL MCP SERVERS (HTTP) =====
            "finance": {
                "transport": "http",
                "url": os.getenv(
                    "MCP_FINANCE_URL",
                    "http://localhost:8001/mcp"
                ),
                "headers": {
                    "Authorization": f"Bearer {os.getenv('MCP_FINANCE_TOKEN', '')}",
                    "X-Request-ID": "{request_id}",  # Will be injected by interceptor
                },
            },
            "compliance": {
                "transport": "http",
                "url": os.getenv(
                    "MCP_COMPLIANCE_URL",
                    "http://localhost:8002/mcp"
                ),
                "headers": {
                    "Authorization": f"Bearer {os.getenv('MCP_COMPLIANCE_TOKEN', '')}",
                },
            },
        }
    
    # ==================== INTERCEPTORS ====================
    
    async def _auth_interceptor(
        self,
        request: MCPToolCallRequest,
        handler,
    ):
        """Inject authentication headers based on runtime context"""
        try:
            # Get user context from runtime
            runtime = getattr(request, 'runtime', None)
            if runtime and hasattr(runtime, 'context'):
                user_id = runtime.context.get('user_id')
                api_key = runtime.context.get('api_key')
                
                # Modify request with user-specific credentials
                if api_key and request.name == "finance_tool":
                    modified_request = request.override(
                        headers={
                            **request.headers,
                            "X-User-ID": user_id,
                            "X-API-Key": api_key,
                        }
                    )
                    return await handler(modified_request)
        except Exception as e:
            logger.warning(f"Auth interceptor error: {e}")
        
        return await handler(request)
    
    async def _retry_interceptor(
        self,
        request: MCPToolCallRequest,
        handler,
        max_retries: int = 3,
        base_delay: float = 1.0,
    ):
        """Retry failed requests with exponential backoff"""
        last_error = None
        
        for attempt in range(max_retries):
            try:
                return await handler(request)
            except Exception as e:
                last_error = e
                
                if attempt < max_retries - 1:
                    delay = base_delay * (2 ** attempt)
                    logger.warning(
                        f"Tool {request.name} failed (attempt {attempt + 1}/{max_retries}), "
                        f"retrying in {delay}s... Error: {e}"
                    )
                    await asyncio.sleep(delay)
                else:
                    logger.error(
                        f"Tool {request.name} failed after {max_retries} attempts"
                    )
        
        raise last_error
    
    async def _context_interceptor(
        self,
        request: MCPToolCallRequest,
        handler,
    ):
        """Inject runtime context into tool arguments"""
        # Add tracing context
        modified_request = request.override(
            args={
                **request.args,
                "_trace_id": getattr(request, 'trace_id', None),
                "_user_id": getattr(request, 'user_id', None),
            }
        )
        
        return await handler(modified_request)
    
    # ==================== CALLBACKS ====================
    
    async def _on_logging_message(self, params, context: CallbackContext):
        """Handle MCP server logging messages"""
        logger.log(
            level=getattr(logging, params.level.upper(), logging.INFO),
            msg=f"[{context.server_name}] {params.data}"
        )
    
    async def _on_progress(self, progress, total, message, context: CallbackContext):
        """Handle progress notifications from long-running tools"""
        percent = (progress / total * 100) if total else progress
        logger.info(
            f"Progress [{context.server_name}/{context.tool_name}]: "
            f"{percent:.1f}% - {message}"
        )
    
    # ==================== PUBLIC API ====================
    
    async def get_tools(self, server_name: Optional[str] = None) -> List[BaseTool]:
        """
        Get tools from MCP servers
        
        Args:
            server_name: Specific server name, or None for all
        
        Returns:
            List of LangChain BaseTool objects
        """
        await self.initialize()
        
        if server_name:
            return await self._client.get_tools(server_name=server_name)
        else:
            return await self._client.get_tools()
    
    async def execute_tool(
        self,
        tool_name: str,
        **kwargs
    ) -> Any:
        """Direct tool execution without LangChain"""
        tools = await self.get_tools()
        tool = next((t for t in tools if t.name == tool_name), None)
        
        if not tool:
            raise ValueError(f"Tool not found: {tool_name}")
        
        return await tool.ainvoke(kwargs)
    
    async def close(self) -> None:
        """Close all MCP connections"""
        if self._client:
            # MultiServerMCPClient doesn't have explicit close in v0.5
            # But you should implement cleanup if needed
            self._client = None
            self._tools_cache = None
            logger.info("MCP Client closed")
    
    async def health_check(self) -> Dict[str, bool]:
        """Check health of all MCP servers"""
        await self.initialize()
        
        health_status = {}
        
        for server_name in self._build_servers_config().keys():
            try:
                # Try to get tools from server
                tools = await self._client.get_tools(server_name=server_name)
                health_status[server_name] = len(tools) > 0
            except Exception as e:
                logger.error(f"Health check failed for {server_name}: {e}")
                health_status[server_name] = False
        
        return health_status


# Global singleton instance
_mcp_manager: Optional[MCPClientManager] = None

def get_mcp_manager() -> MCPClientManager:
    """Get global MCP manager instance"""
    global _mcp_manager
    if _mcp_manager is None:
        _mcp_manager = MCPClientManager()
    return _mcp_manager
```


### 4.2 Step 2: Modify Agent Entrypoint

**File: `app/module/multi_agents/agent_entrypoint.py`** (MODIFIED)

```python
"""
Modified agent entrypoint to use MCP Client
"""

from typing import Dict, List, Optional, Any
import logging
from app.module.mcp.client_manager import get_mcp_manager
from langchain.agents import create_react_agent
from langchain_openai import ChatOpenAI
from langgraph.graph import StateGraph, START, END
from typing_extensions import TypedDict
import asyncio

logger = logging.getLogger(__name__)

class FinAIAgentState(TypedDict):
    """State for FinAI multi-agent orchestration"""
    query: str
    user_id: str
    tools_available: List[Dict[str, Any]]
    analysis_results: Dict[str, Any]
    final_response: str
    error: Optional[str]

class FinAIAgentEntrypoint:
    """Updated entrypoint using MCP tools"""
    
    def __init__(self):
        self.mcp_manager = get_mcp_manager()
        self.llm = ChatOpenAI(model="gpt-4-turbo-preview")
    
    async def execute(
        self,
        query: str,
        user_id: str,
        context: Optional[Dict[str, Any]] = None
    ) -> Dict[str, Any]:
        """
        Execute financial AI query using MCP tools
        
        Args:
            query: User query
            user_id: User identifier
            context: Additional context (API keys, etc.)
        
        Returns:
            Analysis result with final response
        """
        
        # Initialize MCP client
        await self.mcp_manager.initialize()
        
        # Get available tools from all MCP servers
        all_tools = await self.mcp_manager.get_tools()
        
        # Filter tools based on query type
        relevant_tools = self._filter_tools_by_query(all_tools, query)
        
        # Create ReAct agent with MCP tools
        agent = create_react_agent(
            model=self.llm,
            tools=relevant_tools,
            prompt=self._get_system_prompt(user_id),
        )
        
        # Execute agent
        try:
            result = await agent.ainvoke(
                {
                    "messages": [
                        {
                            "role": "user",
                            "content": query,
                        }
                    ],
                    "user_id": user_id,
                    "context": context or {},
                }
            )
            
            return {
                "status": "success",
                "response": result.get("messages", [])[-1].content,
                "tools_used": self._extract_tools_used(result),
            }
        
        except Exception as e:
            logger.error(f"Agent execution failed: {e}")
            return {
                "status": "error",
                "error": str(e),
            }
    
    def _filter_tools_by_query(
        self,
        tools: List,
        query: str
    ) -> List:
        """Filter relevant tools based on query keywords"""
        
        # Simple keyword-based filtering
        keywords_to_tools = {
            "math": ["mcp_add", "mcp_subtract", "mcp_multiply"],
            "stock": ["get_stock_price", "get_financial_statements"],
            "drive": ["list_files", "read_file"],
            "browser": ["take_screenshot", "navigate"],
        }
        
        selected_tools = []
        query_lower = query.lower()
        
        for tool in tools:
            for keyword, tool_names in keywords_to_tools.items():
                if keyword in query_lower and tool.name in tool_names:
                    selected_tools.append(tool)
                    break
        
        # If no tools matched, return all (let agent decide)
        return selected_tools if selected_tools else tools
    
    def _get_system_prompt(self, user_id: str) -> str:
        """Get system prompt for agent"""
        return f"""You are a helpful financial AI assistant for user {user_id}.
        
You have access to various tools to help analyze financial data.
- Use math tools for calculations
- Use stock tools for market data
- Use drive tools for document retrieval
- Use browser tools for web scraping

Always explain your reasoning before using tools.
When unsure, ask clarifying questions.
"""
    
    def _extract_tools_used(self, result: Dict) -> List[str]:
        """Extract which tools were actually used"""
        tools_used = []
        
        for message in result.get("messages", []):
            if hasattr(message, 'tool_calls'):
                tools_used.extend([
                    call['tool'] for call in message.tool_calls
                ])
        
        return tools_used
```


### 4.3 Step 3: Create LangGraph Workflow (Optional for Complex Cases)

**File: `app/module/workflows/complex_analysis.py`** (Using MCP tools with LangGraph)

```python
"""
Complex Financial Analysis Workflow using LangGraph + MCP
"""

from typing import TypedDict, Optional, List, Any
import logging
from langgraph.graph import StateGraph, START, END
from langchain_openai import ChatOpenAI
from app.module.mcp.client_manager import get_mcp_manager

logger = logging.getLogger(__name__)

class AnalysisState(TypedDict):
    """State for complex financial analysis"""
    ticker: str
    user_id: str
    
    # Analysis stages
    stock_data: Optional[Dict[str, Any]]
    financial_statements: Optional[Dict[str, Any]]
    valuation: Optional[Dict[str, Any]]
    analyst_reports: Optional[List[Dict[str, Any]]]
    
    # Synthesis
    recommendation: Optional[str]
    confidence: float
    
    # Metadata
    errors: List[str]

class ComplexAnalysisWorkflow:
    """Orchestrate complex financial analysis using LangGraph + MCP"""
    
    def __init__(self):
        self.mcp_manager = get_mcp_manager()
        self.llm = ChatOpenAI(model="gpt-4-turbo-preview")
    
    def build_graph(self):
        """Build LangGraph workflow"""
        
        builder = StateGraph(AnalysisState)
        
        # Add nodes
        builder.add_node("fetch_stock_data", self.fetch_stock_data_node)
        builder.add_node("fetch_financials", self.fetch_financials_node)
        builder.add_node("calculate_valuation", self.calculate_valuation_node)
        builder.add_node("fetch_analyst_reports", self.fetch_analyst_reports_node)
        builder.add_node("synthesize", self.synthesize_node)
        builder.add_node("human_review", self.human_review_node)
        
        # Add edges
        builder.add_edge(START, "fetch_stock_data")
        builder.add_edge("fetch_stock_data", "fetch_financials")
        builder.add_edge("fetch_financials", "calculate_valuation")
        builder.add_edge("calculate_valuation", "fetch_analyst_reports")
        builder.add_edge("fetch_analyst_reports", "synthesize")
        
        # Conditional: if confidence < 0.7, go to human review
        builder.add_conditional_edges(
            "synthesize",
            self._should_review,
            {
                "review": "human_review",
                "end": END,
            }
        )
        builder.add_edge("human_review", END)
        
        return builder.compile()
    
    async def fetch_stock_data_node(self, state: AnalysisState) -> Dict:
        """Node: Fetch stock price data"""
        try:
            stock_data = await self.mcp_manager.execute_tool(
                "get_stock_price",
                ticker=state["ticker"]
            )
            return {"stock_data": stock_data}
        except Exception as e:
            logger.error(f"Failed to fetch stock data: {e}")
            return {
                "stock_data": None,
                "errors": state["errors"] + [f"Stock data fetch: {str(e)}"],
            }
    
    async def fetch_financials_node(self, state: AnalysisState) -> Dict:
        """Node: Fetch financial statements"""
        try:
            financials = await self.mcp_manager.execute_tool(
                "get_financial_statements",
                ticker=state["ticker"],
                year=2024,
                statement_type="income"
            )
            return {"financial_statements": financials}
        except Exception as e:
            logger.error(f"Failed to fetch financials: {e}")
            return {
                "financial_statements": None,
                "errors": state["errors"] + [f"Financials fetch: {str(e)}"],
            }
    
    async def calculate_valuation_node(self, state: AnalysisState) -> Dict:
        """Node: Calculate valuation"""
        try:
            valuation = await self.mcp_manager.execute_tool(
                "calculate_valuation",
                ticker=state["ticker"],
                method="dcf"
            )
            return {"valuation": valuation}
        except Exception as e:
            logger.error(f"Failed to calculate valuation: {e}")
            return {
                "valuation": None,
                "errors": state["errors"] + [f"Valuation: {str(e)}"],
            }
    
    async def fetch_analyst_reports_node(self, state: AnalysisState) -> Dict:
        """Node: Fetch analyst reports"""
        try:
            reports = await self.mcp_manager.execute_tool(
                "search_analyst_reports",
                ticker=state["ticker"],
                limit=5
            )
            return {"analyst_reports": reports}
        except Exception as e:
            logger.error(f"Failed to fetch reports: {e}")
            return {
                "analyst_reports": None,
                "errors": state["errors"] + [f"Reports fetch: {str(e)}"],
            }
    
    async def synthesize_node(self, state: AnalysisState) -> Dict:
        """Node: Synthesize all data into recommendation"""
        
        # Prepare context for LLM
        context = f"""
        Ticker: {state['ticker']}
        
        Stock Data: {state.get('stock_data', {})}
        
        Financial Statements: {state.get('financial_statements', {})}
        
        Valuation: {state.get('valuation', {})}
        
        Analyst Reports: {state.get('analyst_reports', [])}
        """
        
        prompt = f"""Based on the following financial data for {state['ticker']},
        provide an investment recommendation:
        
        {context}
        
        Format your response as:
        1. Summary of key metrics
        2. Investment recommendation (Buy/Hold/Sell)
        3. Confidence level (0-1)
        4. Key risks
        """
        
        try:
            response = await self.llm.ainvoke(prompt)
            
            # Extract recommendation and confidence
            recommendation = self._parse_recommendation(response.content)
            confidence = self._parse_confidence(response.content)
            
            return {
                "recommendation": recommendation,
                "confidence": confidence,
            }
        except Exception as e:
            logger.error(f"Synthesis failed: {e}")
            return {
                "recommendation": None,
                "confidence": 0.0,
                "errors": state["errors"] + [f"Synthesis: {str(e)}"],
            }
    
    async def human_review_node(self, state: AnalysisState) -> Dict:
        """Node: Request human review if confidence is low"""
        
        logger.warning(
            f"Low confidence ({state['confidence']}) for {state['ticker']}, "
            f"requesting human review"
        )
        
        # In production, this would integrate with approval workflow
        # For now, mark as reviewed
        return {
            "recommendation": f"[PENDING REVIEW] {state['recommendation']}",
        }
    
    def _should_review(self, state: AnalysisState) -> str:
        """Decide if human review is needed"""
        if state.get("confidence", 0) < 0.7:
            return "review"
        return "end"
    
    def _parse_recommendation(self, response: str) -> str:
        """Extract recommendation from LLM response"""
        for line in response.split('\n'):
            if "Buy" in line or "Hold" in line or "Sell" in line:
                return line.strip()
        return "Unknown"
    
    def _parse_confidence(self, response: str) -> float:
        """Extract confidence from LLM response"""
        # Simple regex-based extraction
        import re
        match = re.search(r'Confidence.*?(\d+\.?\d*)', response, re.IGNORECASE)
        if match:
            return float(match.group(1))
        return 0.5
    
    async def execute(self, ticker: str, user_id: str) -> Dict[str, Any]:
        """Execute the workflow"""
        
        graph = self.build_graph()
        
        initial_state = AnalysisState(
            ticker=ticker,
            user_id=user_id,
            stock_data=None,
            financial_statements=None,
            valuation=None,
            analyst_reports=None,
            recommendation=None,
            confidence=0.0,
            errors=[],
        )
        
        result = await graph.ainvoke(initial_state)
        
        return result
```


***

## 5. Production Deployment

### 5.1 Environment Configuration

**File: `.env.production`**

```bash
# FastAPI
FASTAPI_ENV=production
DEBUG=false
LOG_LEVEL=info

# LLM
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4-turbo-preview

# Internal MCP Servers (Stdio - local)
MCP_MATH_ENABLED=true
MCP_BROWSER_ENABLED=true
MCP_DRIVE_ENABLED=true

# External MCP Servers (HTTP - remote)
MCP_FINANCE_URL=https://mcp-finance.prod.example.com/mcp
MCP_FINANCE_TOKEN=bearer_token_here

MCP_COMPLIANCE_URL=https://mcp-compliance.prod.example.com/mcp
MCP_COMPLIANCE_TOKEN=bearer_token_here

# Security
MCP_VERIFY_SSL=true
MCP_REQUEST_TIMEOUT=30
MCP_MAX_RETRIES=3
MCP_RETRY_BACKOFF=2.0

# Database (for checkpointing if using LangGraph)
POSTGRES_URL=postgresql://user:pass@db:5432/finai_db
```


### 5.2 Kubernetes Deployment

**File: `k8s/deployment.yaml`**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finai-agent
  namespace: production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: finai-agent
  template:
    metadata:
      labels:
        app: finai-agent
    spec:
      serviceAccountName: finai-agent
      containers:
      - name: finai-agent
        image: your-registry/finai-agent:latest
        imagePullPolicy: Always
        
        ports:
        - containerPort: 8000
          name: http
        
        env:
        - name: FASTAPI_ENV
          value: "production"
        
        - name: MCP_FINANCE_URL
          valueFrom:
            configMapKeyRef:
              name: mcp-config
              key: finance-url
        
        - name: MCP_FINANCE_TOKEN
          valueFrom:
            secretKeyRef:
              name: mcp-secrets
              key: finance-token
        
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: ai-secrets
              key: openai-api-key
        
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
---
apiVersion: v1
kind: Service
metadata:
  name: finai-agent-svc
  namespace: production
spec:
  selector:
    app: finai-agent
  ports:
  - port: 80
    targetPort: 8000
    name: http
  type: LoadBalancer
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: finai-agent-hpa
  namespace: production
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: finai-agent
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
```


***

## 6. Monitoring \& Debugging

### 6.1 Health Check \& Observability

**File: `app/routes/health.py`**

```python
from fastapi import APIRouter, HTTPException
from app.module.mcp.client_manager import get_mcp_manager
import logging

router = APIRouter(prefix="/health", tags=["health"])
logger = logging.getLogger(__name__)

@router.get("/")
async def health_check():
    """Basic health check"""
    return {
        "status": "healthy",
        "service": "finai-agent",
    }

@router.get("/ready")
async def readiness_check():
    """Readiness check - include MCP servers"""
    try:
        mcp_manager = get_mcp_manager()
        mcp_health = await mcp_manager.health_check()
        
        # All servers must be reachable
        all_healthy = all(mcp_health.values())
        
        if not all_healthy:
            failing = [k for k, v in mcp_health.items() if not v]
            raise HTTPException(
                status_code=503,
                detail=f"MCP servers unavailable: {failing}"
            )
        
        return {
            "status": "ready",
            "mcp_servers": mcp_health,
        }
    
    except Exception as e:
        logger.error(f"Readiness check failed: {e}")
        raise HTTPException(status_code=503, detail=str(e))

@router.get("/mcp-status")
async def mcp_status():
    """Detailed MCP server status"""
    mcp_manager = get_mcp_manager()
    return await mcp_manager.health_check()
```


### 6.2 Observability Integration

**File: `app/observability/tracing.py`**

```python
from opentelemetry import trace, metrics
from opentelemetry.exporter.jaeger.thrift import JaegerExporter
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import BatchSpanProcessor
import logging

logger = logging.getLogger(__name__)

def setup_tracing():
    """Setup OpenTelemetry tracing for MCP calls"""
    
    jaeger_exporter = JaegerExporter(
        agent_host_name="localhost",
        agent_port=6831,
    )
    
    trace.set_tracer_provider(
        TracerProvider(active_span_processor=BatchSpanProcessor(jaeger_exporter))
    )
    
    logger.info("Tracing configured with Jaeger")

# Usage in MCP manager
tracer = trace.get_tracer(__name__)

async def execute_with_tracing(tool_name: str, **kwargs):
    """Execute MCP tool with OpenTelemetry tracing"""
    with tracer.start_as_current_span(f"mcp.tool.{tool_name}") as span:
        span.set_attribute("tool_name", tool_name)
        span.set_attribute("args", str(kwargs))
        
        # Execute tool
        result = await mcp_manager.execute_tool(tool_name, **kwargs)
        
        span.set_attribute("result", str(result))
        return result
```


***

## 📝 Tóm Tắt Triển Khai

### Checklist

- [ ] Install `langchain-mcp-adapters` và `fastmcp`
- [ ] Tạo external MCP servers (Finance, Compliance, etc.)
- [ ] Viết `MCPClientManager` wrapper
- [ ] Update `agent_entrypoint.py` để dùng MCP tools
- [ ] (Optional) Tạo LangGraph workflows cho complex cases
- [ ] Setup environment variables cho external servers
- [ ] Deploy MCP servers (Docker/Kubernetes)
- [ ] Integrate health checks vào FastAPI
- [ ] Setup observability (Jaeger, Prometheus)
- [ ] Test end-to-end in staging
- [ ] Deploy to production


### Expected Benefits

✅ **Scalability**: MCP servers can scale independently
✅ **Flexibility**: Mix local (stdio) + remote (HTTP) tools seamlessly
✅ **Maintainability**: Clear separation between agents \& tools
✅ **Observability**: Built-in MCP callbacks for logging/progress
✅ **Security**: Interceptor-based auth, context injection

***

Bạn có câu hỏi cụ thể nào về implementation không? Hoặc muốn tôi deep-dive vào bất kỳ phần nào?


---
### Ý tưởng chung

- **External MCP server** (Playwright MCP, GitHub MCP, Drive MCP npm package, …)  
  - Chạy như **process riêng** (thường là `node`/`npx`), expose tool qua **MCP protocol** (stdio hoặc HTTP).
- Muốn dùng trong **LangChain / LangGraph**:
  1. Dùng **MCP client** trong Python để connect đến server, `list_tools`, `call_tool`.
  2. **Wrap** các MCP tools đó thành **LangChain `StructuredTool`**.
  3. Dùng như tools bình thường trong **agent LangChain** hoặc **nodes trong LangGraph**.

Dưới đây là **demo tối giản**, dễ hiểu, có thể dùng production sau khi thêm retry/logging.

---

### 1. MCP client wrapper cho external server (vd: Playwright MCP)

Ví dụ: server Playwright là npm package `@modelcontextprotocol/server-playwright`.

```python
"""
External MCP Client Wrapper cho server Playwright (hoặc bất kỳ MCP server nào)
- Transport: stdio (npx / node)
"""

import asyncio
from typing import Any, Dict, List, Optional

from mcp import ClientSession, StdioServerParameters
from mcp.client.stdio import stdio_client


class ExternalMCPClient:
    """Client Python để nói chuyện với external MCP server (Playwright, GitHub, ...)."""

    def __init__(
        self,
        name: str,
        command: str,
        args: Optional[List[str]] = None,
        env: Optional[Dict[str, str]] = None,
        cwd: Optional[str] = None,
    ):
        self.name = name
        self.command = command
        self.args = args or []
        self.env = env or {}
        self.cwd = cwd

        self._session: Optional[ClientSession] = None
        self._read = None
        self._write = None
        self._ctx = None  # stdio context
        self._tools_cache: Optional[List[Dict[str, Any]]] = None

    async def _ensure_connected(self):
        """Connect đến MCP server nếu chưa connect."""
        if self._session is not None:
            return

        params = StdioServerParameters(
            command=self.command,
            args=self.args,
            env=self.env or None,
            cwd=self.cwd,
        )
        self._ctx = stdio_client(params)
        self._read, self._write = await self._ctx.__aenter__()
        self._session = ClientSession(self._read, self._write)
        await self._session.initialize()

    async def list_tools(self) -> List[Dict[str, Any]]:
        """Gọi `tools/list` và cache kết quả."""
        if self._tools_cache is not None:
            return self._tools_cache

        await self._ensure_connected()
        resp = await self._session.list_tools()
        tools = []
        for t in resp.tools:
            tools.append(
                {
                    "name": t.name,
                    "description": t.description or "",
                    "input_schema": t.inputSchema or {},
                }
            )
        self._tools_cache = tools
        return tools

    async def call_tool(self, name: str, arguments: Dict[str, Any]) -> Any:
        """Gọi `tools/call`."""
        await self._ensure_connected()
        result = await self._session.call_tool(name, arguments)

        if result.isError:
            # Trả về error có cấu trúc
            msg = result.content[0].text if result.content else "Unknown error"
            return {"success": False, "error": msg}

        # Gom text/data từ content
        text = ""
        for item in result.content or []:
            if hasattr(item, "text"):
                text += item.text
            elif hasattr(item, "data"):
                text += str(item.data)
        return {"success": True, "result": text or result.content}

    async def close(self):
        """Đóng kết nối."""
        if self._ctx is not None:
            await self._ctx.__aexit__(None, None, None)
            self._ctx = None
        self._session = None
        self._read = None
        self._write = None
```

Ví dụ dùng với Playwright MCP:

```python
playwright_client = ExternalMCPClient(
    name="playwright",
    command="npx",
    args=["-y", "@modelcontextprotocol/server-playwright"],
    env={},  # thêm token, config nếu server cần
)
```

---

### 2. Adapter: MCP tools → LangChain `StructuredTool`

```python
from typing import Any, Dict, List, Optional
from langchain_core.tools import StructuredTool
from langchain_core.pydantic_v1 import BaseModel as LCBaseModel, Field


class ExternalMCPToLangChainAdapter:
    """Convert tools từ ExternalMCPClient → LangChain StructuredTool."""

    def __init__(self, client: ExternalMCPClient):
        self.client = client
        self._tools: Optional[List[StructuredTool]] = None

    async def get_tools(self) -> List[StructuredTool]:
        if self._tools is not None:
            return self._tools

        mcp_tools = await self.client.list_tools()
        tools: List[StructuredTool] = []
        for t in mcp_tools:
            tools.append(self._to_structured_tool(t))
        self._tools = tools
        return tools

    def _to_structured_tool(self, mcp_tool: Dict[str, Any]) -> StructuredTool:
        name = mcp_tool["name"]
        description = mcp_tool["description"]
        schema = mcp_tool["input_schema"]

        # 1) JSON Schema → Pydantic model (simple version)
        props = schema.get("properties", {})
        required = set(schema.get("required", []))

        fields = {}
        for prop_name, prop_schema in props.items():
            json_type = prop_schema.get("type", "string")
            py_type = self._json_type_to_python(json_type)
            desc = prop_schema.get("description", "")

            if prop_name in required:
                fields[prop_name] = (py_type, Field(description=desc))
            else:
                fields[prop_name] = (Optional[py_type], Field(default=None, description=desc))

        model_name = f"{self.client.name}_{name}_Input"
        ArgsModel = type(model_name, (LCBaseModel,), fields)

        # 2) Hàm gọi MCP tool
        async def _call(**kwargs) -> str:
            res = await self.client.call_tool(name, kwargs)
            import json
            return json.dumps(res, ensure_ascii=False, indent=2)

        # 3) StructuredTool
        return StructuredTool(
            name=name,
            description=description,
            args_schema=ArgsModel,
            func=_call,
            coroutine=_call,
        )

    def _json_type_to_python(self, t: str):
        return {
            "string": str,
            "number": float,
            "integer": int,
            "boolean": bool,
            "array": list,
            "object": dict,
        }.get(t, str)
```

---

### 3. Dùng trong **LangChain Agent**

```python
import asyncio
from langchain_openai import ChatOpenAI
from langchain.agents import create_openai_tools_agent, AgentExecutor
from langchain.prompts import ChatPromptTemplate, MessagesPlaceholder


async def build_playwright_langchain_agent() -> AgentExecutor:
    # 1. Tạo MCP client cho Playwright
    client = ExternalMCPClient(
        name="playwright",
        command="npx",
        args=["-y", "@modelcontextprotocol/server-playwright"],
    )

    # 2. Convert tools → LangChain tools
    adapter = ExternalMCPToLangChainAdapter(client)
    tools = await adapter.get_tools()

    # 3. LLM
    llm = ChatOpenAI(model="gpt-4o-mini", temperature=0)

    # 4. Prompt
    prompt = ChatPromptTemplate.from_messages(
        [
            ("system", "Bạn là AI, có thể dùng Playwright MCP tools để duyệt web và trả lời."),
            ("human", "{input}"),
            MessagesPlaceholder("agent_scratchpad"),
        ]
    )

    # 5. Tạo agent
    agent = create_openai_tools_agent(llm, tools, prompt)

    # 6. Executor
    executor = AgentExecutor(agent=agent, tools=tools, verbose=True)
    return executor


async def demo_langchain():
    agent = await build_playwright_langchain_agent()
    result = await agent.ainvoke(
        {"input": "Mở trang https://example.com và cho tôi biết title là gì."}
    )
    print(result["output"])


# asyncio.run(demo_langchain())
```

---

### 4. Dùng trong **LangGraph**

```python
from typing import TypedDict, Annotated, Optional
from langgraph.graph import StateGraph, END
from langgraph.graph.message import add_messages
from langgraph.prebuilt import ToolNode, ToolExecutor
from langchain_openai import ChatOpenAI


class GraphState(TypedDict):
    messages: Annotated[list, add_messages]


async def build_playwright_langgraph_app():
    # MCP client + tools
    client = ExternalMCPClient(
        name="playwright",
        command="npx",
        args=["-y", "@modelcontextprotocol/server-playwright"],
    )
    adapter = ExternalMCPToLangChainAdapter(client)
    tools = await adapter.get_tools()

    llm = ChatOpenAI(model="gpt-4o-mini", temperature=0)
    llm_with_tools = llm.bind_tools(tools)
    tool_executor = ToolExecutor(tools)

    def agent_node(state: GraphState):
        msgs = state["messages"]
        resp = llm_with_tools.invoke(msgs)
        return {"messages": [resp]}

    async def tools_node(state: GraphState):
        msgs = state["messages"]
        last = msgs[-1]
        res = await tool_executor.ainvoke({"messages": [last]})
        return {"messages": res["messages"]}

    def should_continue(state: GraphState) -> str:
        last = state["messages"][-1]
        if getattr(last, "tool_calls", None):
            return "tools"
        return "end"

    graph = StateGraph(GraphState)
    graph.add_node("agent", agent_node)
    graph.add_node("tools", tools_node)
    graph.set_entry_point("agent")
    graph.add_conditional_edges(
        "agent",
        should_continue,
        {"tools": "tools", "end": END},
    )
    graph.add_edge("tools", "agent")
    app = graph.compile()
    return app


async def demo_langgraph():
    from langchain_core.messages import HumanMessage

    app = await build_playwright_langgraph_app()
    result = await app.ainvoke(
        {"messages": [HumanMessage(content="Dùng browser để mở https://example.com và đọc title.")]}
    )
    print(result["messages"][-1].content)


# asyncio.run(demo_langgraph())
```

---

### 5. Tóm tắt quy trình cho **mọi external MCP server** (Playwright, GitHub, …)

- **Bước 1**: Khởi chạy server (docker / `npx @modelcontextprotocol/server-xyz`).
- **Bước 2**: Tạo `ExternalMCPClient`:
  - Nếu server stdio → dùng `mcp.client.stdio`.
  - Nếu server HTTP (như Google Workspace MCP) → dùng `httpx` (giống `HTTPMCPServerWrapper` trong repo của bạn).
- **Bước 3**: `list_tools()` → convert thành `StructuredTool` (adapter).
- **Bước 4**:
  - LangChain: dùng `create_openai_tools_agent` + `AgentExecutor`.
  - LangGraph: bind tools vào LLM + `ToolExecutor` + `ToolNode`.

Nếu bạn muốn, mình có thể viết riêng một phiên bản **HTTP MCP client adapter** (giống Google Workspace MCP) nhưng rút gọn, để bạn cắm nhanh bất kỳ MCP server HTTP nào (không chỉ Playwright).

---
Đang kiểm tra cách deploy MCP server hiện tại trong codebase và các best practices:


[5 tools called]


Tổng hợp best practices để deploy MCP server chuẩn, dựa trên codebase hiện tại và industry standards:

## Best practices: Deploy MCP server chuẩn

### 1. Chọn transport: stdio vs HTTP

#### Option A: stdio (cho development/local, single-user)

Ưu điểm:
- Đơn giản, không cần network
- Phù hợp single-user, local development
- Ít overhead

Nhược điểm:
- Không scale được (1 process = 1 connection)
- Không phù hợp multi-user
- Khó monitor/debug

Khi nào dùng:
- Development
- Single-user desktop apps
- Testing

```python
# Client connect qua stdio
client = ExternalMCPClient(
    name="playwright",
    command="npx",
    args=["-y", "@modelcontextprotocol/server-playwright"],
)
```

#### Option B: HTTP (cho production, multi-user) — khuyến nghị

Ưu điểm:
- Stateless, scale được
- Multi-user support (bearer tokens)
- Dễ monitor, health check
- Load balancing được
- Phù hợp microservices

Nhược điểm:
- Cần HTTP server
- Phức tạp hơn một chút

Khi nào dùng:
- Production
- Multi-user systems
- Cloud deployment
- Microservices architecture

```python
# Server chạy HTTP mode
CMD ["uv", "run", "main.py", "--transport", "streamable-http"]
```

Khuyến nghị: Dùng HTTP cho production.

---

### 2. Deployment patterns

#### Pattern 1: Docker Compose (development/staging)

```yaml
# docker-compose-mcp.yml
version: '3.8'

services:
  # MCP Server với HTTP transport
  playwright-mcp:
    build:
      context: ./mcp-servers/playwright-mcp
      dockerfile: Dockerfile
    container_name: mcp-playwright
    ports:
      - "${PLAYWRIGHT_MCP_PORT:-8001}:8000"
    environment:
      - MCP_TRANSPORT=http
      - MCP_PORT=8000
      - LOG_LEVEL=INFO
      # Secrets từ .env
      - PLAYWRIGHT_BROWSER_PATH=/usr/bin/chromium
    restart: unless-stopped
    networks:
      - mcp-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # Resource limits
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

  # GitHub MCP Server
  github-mcp:
    build:
      context: ./mcp-servers/github-mcp
    container_name: mcp-github
    ports:
      - "${GITHUB_MCP_PORT:-8002}:8000"
    environment:
      - MCP_TRANSPORT=http
      - GITHUB_TOKEN=${GITHUB_TOKEN}  # Secret từ .env
    restart: unless-stopped
    networks:
      - mcp-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  mcp-network:
    driver: bridge
    name: mcp-network

# Volumes cho persistent data (nếu cần)
volumes:
  playwright-data:
```

Dockerfile mẫu:

```dockerfile
# Dockerfile cho MCP Server (ví dụ: Playwright)
FROM node:20-slim

WORKDIR /app

# Install Playwright browsers
RUN npx playwright install chromium --with-deps

# Install MCP server package
RUN npm install -g @modelcontextprotocol/server-playwright

# Expose port
EXPOSE 8000

# Health check script
COPY healthcheck.sh /healthcheck.sh
RUN chmod +x /healthcheck.sh

# Run với HTTP transport
CMD ["npx", "@modelcontextprotocol/server-playwright", "--transport", "http", "--port", "8000"]
```

#### Pattern 2: Kubernetes (production)

```yaml
# k8s/mcp-servers/playwright-mcp.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: playwright-mcp
  namespace: mcp-servers
spec:
  replicas: 3  # Scale horizontally
  selector:
    matchLabels:
      app: playwright-mcp
  template:
    metadata:
      labels:
        app: playwright-mcp
    spec:
      containers:
      - name: playwright-mcp
        image: your-registry/playwright-mcp:latest
        ports:
        - containerPort: 8000
        env:
        - name: MCP_TRANSPORT
          value: "http"
        - name: MCP_PORT
          value: "8000"
        - name: LOG_LEVEL
          value: "INFO"
        # Secrets từ Kubernetes Secret
        - name: PLAYWRIGHT_BROWSER_PATH
          value: "/usr/bin/chromium"
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: playwright-mcp-service
  namespace: mcp-servers
spec:
  selector:
    app: playwright-mcp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: ClusterIP  # Internal only
---
# Ingress (nếu cần expose public)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: playwright-mcp-ingress
  namespace: mcp-servers
spec:
  rules:
  - host: mcp-playwright.yourdomain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: playwright-mcp-service
            port:
              number: 80
```

#### Pattern 3: Serverless (AWS Lambda, Cloud Functions)

```python
# serverless_mcp_handler.py
import json
from mcp.server.fastmcp import FastMCP

# FastMCP cho serverless
mcp = FastMCP("playwright-mcp")

@mcp.tool()
async def navigate(url: str) -> str:
    """Navigate to URL."""
    # Implementation
    return f"Navigated to {url}"

# Lambda handler
def lambda_handler(event, context):
    """AWS Lambda handler."""
    # Parse MCP request từ event
    mcp_request = json.loads(event["body"])
    
    # Process request
    response = mcp.handle_request(mcp_request)
    
    return {
        "statusCode": 200,
        "body": json.dumps(response),
    }
```

---

### 3. Multi-user support (OAuth 2.1)

#### Architecture: Stateless HTTP + Bearer Tokens

```yaml
# docker-compose.yml
services:
  google-workspace-mcp:
    build: ./google-workspace-mcp
    environment:
      # ✅ Stateless mode cho multi-user
      - WORKSPACE_MCP_STATELESS_MODE=true
      - MCP_ENABLE_OAUTH21=true
      - WORKSPACE_MCP_PORT=8000
    # ✅ Không cần volumes (stateless)
    # ✅ Mỗi request tự inject token
```

Client code:

```python
# HTTP MCP Client với token injection
class HTTPMCPServerWrapper:
    async def _get_headers(self, user_id: Optional[str] = None) -> Dict[str, str]:
        headers = {"Content-Type": "application/json"}
        
        # ✅ Inject bearer token từ OAuth repository
        if user_id:
            token = await get_google_token_for_user(user_id)
            if token:
                headers["Authorization"] = f"Bearer {token}"
        
        return headers
```

---

### 4. Security best practices

#### A. Secrets management

```yaml
# ❌ KHÔNG làm thế này
environment:
  - GITHUB_TOKEN=ghp_abc123  # Hard-coded secret

# ✅ Làm thế này
environment:
  - GITHUB_TOKEN=${GITHUB_TOKEN}  # Từ .env hoặc secret manager
```

Dùng Kubernetes Secrets:

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: github-mcp-secrets
type: Opaque
stringData:
  GITHUB_TOKEN: "ghp_abc123"
---
env:
- name: GITHUB_TOKEN
  valueFrom:
    secretKeyRef:
      name: github-mcp-secrets
      key: GITHUB_TOKEN
```

#### B. Network isolation

```yaml
# ✅ Internal network only
networks:
  mcp-network:
    driver: bridge
    internal: true  # Không expose ra ngoài

# ✅ Service discovery qua service name
services:
  app:
    environment:
      - PLAYWRIGHT_MCP_URL=http://playwright-mcp:8000  # Internal DNS
```

#### C. Authentication & Authorization

```python
# ✅ Validate bearer token trong MCP server
async def validate_token(token: str) -> Optional[str]:
    """Validate token và return user_id."""
    # Verify JWT hoặc check với auth service
    payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
    return payload.get("user_id")

# ✅ Inject vào MCP request
headers = {
    "Authorization": f"Bearer {token}",
    "X-User-ID": user_id,  # Optional: explicit user context
}
```

---

### 5. Monitoring & observability

#### A. Health checks

```python
# healthcheck.py trong MCP server
from fastapi import FastAPI

app = FastAPI()

@app.get("/health")
async def health_check():
    """Health check endpoint."""
    return {
        "status": "healthy",
        "version": "1.0.0",
        "tools_count": len(discovered_tools),
    }
```

Docker healthcheck:

```yaml
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
  interval: 30s
  timeout: 10s
  retries: 3
  start_period: 40s
```

#### B. Logging

```python
# Structured logging
import structlog

logger = structlog.get_logger()

logger.info(
    "mcp_tool_called",
    tool_name="playwright_navigate",
    user_id=user_id,
    duration_ms=150,
    success=True,
)
```

#### C. Metrics (Prometheus)

```python
from prometheus_client import Counter, Histogram

tool_calls_total = Counter(
    "mcp_tool_calls_total",
    "Total tool calls",
    ["tool_name", "status"],
)

tool_duration = Histogram(
    "mcp_tool_duration_seconds",
    "Tool execution duration",
    ["tool_name"],
)

# Trong tool handler
with tool_duration.labels(tool_name="navigate").time():
    result = await execute_tool(...)
    tool_calls_total.labels(
        tool_name="navigate",
        status="success" if result else "error",
    ).inc()
```

---

### 6. Scaling strategies

#### A. Horizontal scaling (HTTP transport)

```yaml
# Kubernetes: Scale replicas
replicas: 5

# Load balancer
apiVersion: v1
kind: Service
spec:
  type: LoadBalancer
  sessionAffinity: None  # Stateless → no affinity needed
```

#### B. Connection pooling (client-side)

```python
import httpx

# ✅ Connection pool cho HTTP client
class HTTPMCPServerWrapper:
    def __init__(self, base_url: str):
        self._client = httpx.AsyncClient(
            base_url=base_url,
            limits=httpx.Limits(
                max_keepalive_connections=20,
                max_connections=100,
            ),
            timeout=30.0,
        )
```

#### C. Rate limiting

```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

@app.post("/mcp")
@limiter.limit("100/minute")  # Rate limit
async def handle_mcp_request(request: Request):
    # Process request
    pass
```

---

### 7. Deployment checklist

```markdown
## ✅ Pre-Deployment Checklist

### Infrastructure
- [ ] Docker image built và tested
- [ ] Health check endpoint implemented
- [ ] Logging configured (structured logs)
- [ ] Metrics exposed (Prometheus format)
- [ ] Secrets managed (không hard-code)

### Network
- [ ] Internal network configured
- [ ] Service discovery working
- [ ] Firewall rules set
- [ ] Load balancer configured (nếu cần)

### Security
- [ ] Authentication implemented (OAuth/Bearer tokens)
- [ ] Authorization checked (per-user permissions)
- [ ] Secrets từ secret manager
- [ ] Network isolation (internal only)

### Monitoring
- [ ] Health checks working
- [ ] Logs aggregated (ELK/CloudWatch)
- [ ] Metrics collected (Prometheus)
- [ ] Alerts configured (PagerDuty/Slack)

### Testing
- [ ] Unit tests passed
- [ ] Integration tests passed
- [ ] Load testing done
- [ ] Fallback tested (nếu có internal MCP)
```

---

### 8. Recommended architecture (production)

```
┌─────────────────────────────────────────────────────────┐
│                    Production Architecture                │
└─────────────────────────────────────────────────────────┘

                    ┌─────────────┐
                    │   Load      │
                    │  Balancer   │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐       ┌────▼────┐       ┌────▼────┐
   │ MCP     │       │ MCP     │       │ MCP     │
   │ Server  │       │ Server  │       │ Server  │
   │ (Pod 1) │       │ (Pod 2) │       │ (Pod 3) │
   └─────────┘       └─────────┘       └─────────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                    ┌──────▼──────┐
                    │   Redis     │
                    │  (Cache)    │
                    └─────────────┘
                           │
                    ┌──────▼──────┐
                    │ PostgreSQL  │
                    │  (Tokens)   │
                    └─────────────┘
```

---

### Tóm tắt: Best practices

1. Transport: HTTP cho production (stateless, scalable)
2. Deployment: Docker Compose (dev) → Kubernetes (prod)
3. Multi-user: Stateless mode + Bearer tokens
4. Security: Secrets management, network isolation, auth
5. Monitoring: Health checks, structured logs, metrics
6. Scaling: Horizontal scaling với load balancer
7. Testing: Health checks, integration tests, load testing

Khuyến nghị: Dùng HTTP transport + Docker/Kubernetes + OAuth 2.1 + health checks + monitoring. Đây là cách chuẩn nhất cho production.


### Ý ngắn gọn: Deploy bằng Docker Compose xong thì dùng như **1 HTTP service** bình thường

Bạn đã có sẵn flow chuẩn trong repo rồi (Google Workspace MCP là ví dụ hoàn chỉnh). Cách dùng sau khi `docker-compose` rất đơn giản, theo 3 bước:

---

### 1. Chạy MCP server bằng Docker Compose

Ví dụ với Google Workspace MCP:

```bash
cd deploy
docker-compose -f docker-compose-mcp.yml up -d google-workspace-mcp
```

Nó sẽ chạy MCP server ở `http://localhost:8000` (trong compose đã mapping port 8000).

---

### 2. Cấu hình app để trỏ tới MCP server

Trong `.env` của app Python:

```bash
GOOGLE_WORKSPACE_MCP_ENABLED=true
GOOGLE_WORKSPACE_MCP_URL=http://localhost:8000
GOOGLE_WORKSPACE_MCP_TIMEOUT=30
```

`app.common.config.Settings` sẽ đọc các biến này, và trong `mcp_init.py` bạn đã có:

```python
if settings.GOOGLE_WORKSPACE_MCP_ENABLED:
    from app.module.multi_agents.layer_3_action.internal_mcp_servers.http_mcp_wrapper import (
        HTTPMCPServerWrapper,
        get_google_token_for_user,
    )

    drive_server = HTTPMCPServerWrapper(
        name="google_workspace",
        base_url=settings.GOOGLE_WORKSPACE_MCP_URL,
        get_token_callback=get_google_token_for_user,
        timeout=settings.GOOGLE_WORKSPACE_MCP_TIMEOUT,
    )
else:
    drive_server = InternalDriveMCPServer()
```

→ Chỉ cần **bật flag + URL đúng**, app sẽ tự dùng external MCP server đang chạy trong Docker.

---

### 3. App/agent dùng MCP server như thế nào?

Với multi-agent hiện tại, flow đã wired sẵn:

- `init_mcp()`:
  - Tạo `HTTPMCPServerWrapper` (nếu ENABLED)
  - `registry.register_mcp_server("drive", drive_server)`
  - `adapter = MCPToolAdapter(bus=bus, registry=registry)`

- Agent (ví dụ `DriveAgent`) chỉ cần gọi:

```python
res = await self.mcp.execute(
    MCPToolRequest(
        agent_name=self.name,
        tool_server="drive",           # chính là HTTPMCPServerWrapper đang trỏ ra Docker MCP
        tool_name="drive_list_files",  # hoặc tên tool bạn muốn
        arguments={"folder_id": None},
    )
)
```

Không cần quan tâm Docker hay internal, vì:
- Docker MCP → HTTPMCPServerWrapper → ToolRegistry → MCPToolAdapter → Agent  
- Nếu tắt external (ENABLE=false) → fallback `InternalDriveMCPServer` cùng interface.

---

### 4. Nếu bạn muốn test gọi trực tiếp (không qua agent)

Ví dụ: gọi thẳng tool từ HTTPMCPServerWrapper:

```python
from app.module.multi_agents.layer_3_action.internal_mcp_servers.http_mcp_wrapper import (
    HTTPMCPServerWrapper,
    get_google_token_for_user,
)
from app.common.config import settings

server = HTTPMCPServerWrapper(
    name="google_workspace",
    base_url=settings.GOOGLE_WORKSPACE_MCP_URL,
    get_token_callback=get_google_token_for_user,
    timeout=settings.GOOGLE_WORKSPACE_MCP_TIMEOUT,
)

# Discover tools (HTTP POST /mcp method=tools/list)
tools = await server.discover_tools()
print([t.name for t in tools])

# Call 1 tool (HTTP POST /mcp method=tools/call)
result = await server.call_tool(
    tool_name="drive_list_files",
    arguments={"page_size": 5},
    user_id="user_a",  # để wrapper tự inject Bearer token
)
print(result)
```

---

### Tóm tắt

- **Deploy**: `docker-compose -f docker-compose-mcp.yml up -d` → MCP lắng nghe HTTP (port 8000).
- **Config app**: `GOOGLE_WORKSPACE_MCP_ENABLED=true`, `GOOGLE_WORKSPACE_MCP_URL=http://localhost:8000`.
- **Sử dụng**:
  - Multi-agent: qua `MCPToolAdapter` + `tool_server="drive"`.
  - Hoặc trực tiếp: dùng `HTTPMCPServerWrapper.call_tool(...)`.

Bạn chỉ cần nhớ: **Docker Compose chỉ làm nhiệm vụ chạy MCP server; code Python của bạn nói chuyện với nó như 1 HTTP MCP service qua `HTTPMCPServerWrapper`.**



