

![[../../New folder/3_Learning/attachments/Pasted image 20241106122454.png]]


---


Ok 👍. Dưới đây là **toàn bộ file Git Cheat Sheet** bạn đã gửi, mình đã gom lại thành một khối hoàn chỉnh, sắp xếp **từ dễ → khó**, nhưng giữ **nguyên vẹn toàn bộ text, comment, ký tự, cấu trúc code block**:

---

# 📘 Git Cheat Sheet (29/09/2025) — Sắp xếp từ dễ đến khó

---

# 1. Cất tạm file (stash)

```bash
git stash push -m "WIP: feature login"
# Hoặc viết đầy đủ 
git stash pop              # Pop stash gần nhất
git stash pop stash@{1}    # Pop một stash cụ thể
git stash apply            # Áp dụng nhưng không xóa stash

```

---

# 2. Git Pull

## 2.1 Commit hoặc stash code local trước khi pull

- **Commit**:
    

```bash
git add .
git commit -m "Save local changes"
git pull
```

→ Lưu lại công việc hiện tại, sau đó pull về. Nếu có xung đột thì git sẽ yêu cầu merge.

- **Stash** (nếu chưa muốn commit):
    

```bash
git stash
git pull
git stash pop
```

→ Lưu tạm thay đổi vào stash, pull xong thì lấy ra.

---

# 3. Các lệnh quay lại / reset commit

- Quay lại commit trước đó:
    

```bash
git reset --hard fff1206
```

- **Quay lại commit gần nhất (giữ hoặc xóa thay đổi):**
    

```bash
git reset --soft HEAD^   # Gỡ commit gần nhất nhưng giữ lại thay đổi (soft reset)
git reset --hard fff1206 # Gỡ commit gần nhất và loại bỏ luôn thay đổi (hard reset)
```

- `git reset --soft HEAD~1`: gỡ commit nhưng giữ thay đổi
    
- `git reset --hard HEAD`: xóa thay đổi local, quay lại commit gần nhất
    
- `git revert`: tạo commit mới đảo ngược commit cũ
    

**Khác nhau reset vs revert (ví dụ A→B→C→D):**

- `git reset --hard <commit>` → quay về A→B (cần force push)
    
- `git revert` → A→B→C→D→B’ (giữ lịch sử)
    

```bash
git reset --hard HEAD
git push --force
```

---

# 4. Git Config

- Cho phép đường dẫn dài (Windows):
    

```bash
git config --system core.longpaths true
```

---

# 5. Origin, Main, Master là gì?

- **`origin`**: Remote repository mặc định.
    
- **`main`**: Nhánh chính mặc định (hiện đại).
    
- **`master`**: Nhánh chính mặc định trước đây (đang dần thay bằng `main`).
    

Các lệnh thường gặp với `origin`:

- `git fetch origin`
    
- `git push origin main`
    

---

# 6. Đánh dấu phiên bản (Tag)

Ví dụ: demo, deploy 1.1, …

```bash
git tag -a v1_AutoQC_fromVisualizeLessonRobot_ -m "AutoQC System - Visualize Lesson Robot Integration ..."
git push origin v1_AutoQC_fromVisualizeLessonRobot
```

---

# 7. Xử lý xung đột trong VS Code

- Dấu hiệu: `<<<<<<<`, `=======`, `>>>>>>>`.
    
- Options:
    
    - Accept Current Change
        
    - Accept Incoming Change
        
    - Accept Both Changes
        
    - Compare Changes
        

---

# 8. Xóa nhánh local đã mất trên remote

## Windows (PowerShell)

```powershell
git branch -vv | Select-String ': gone]'
git branch -vv | ForEach-Object {
    if ($_ -match ': gone]') {
        $branch = ($_ -split '\s+')[1]
        git branch -D $branch
    }
}
```

## Linux / WSL

```bash
git branch -vv | grep ': gone]' | awk '{print $1}' | xargs git branch -D
```

---

# 9. Merge code cơ bản

```bash
git branch   # danh sách các nhánh cục bộ:
git branch -r  # kiểm tra nhánh remote

# Nếu nhánh chỉ tồn tại trên remote, bạn cần tải về:
git fetch origin cuong/update2024-legal2008txt 
git checkout -b cuong/update2024-legal2008txt origin/cuong/update2024-legal2008txt

# checkout sang main
git checkout main
git merge cuong/update2024_legal2008txt
```

Kéo main về nhánh hiện tại:

```bash
git pull origin main
git pull origin main --no-rebase   # Gộp commit, giữ nguyên lịch sử
git pull origin main --rebase      # Lịch sử sạch hơn
git pull origin main --ff-only     # Chỉ update nếu không có commit mới
```

Merge nhiều nhánh:

```bash
git checkout main
git merge cuong/update2024-legal2008txt
git merge DNCuong/UI

git checkout dev/DoanCuong
git pull origin main
```

## Không cần commit. Bạn muốn giữ bản cloud thì chỉ cần bỏ thay đổi local trước: 

```
(.locust_env) PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git pull origin
Updating fb9a0d2..eb9ac84
error: Your local changes to the following files would be overwritten by merge:
        src/app/core/config_settings.py
Please commit your changes or stash them before you merge.
Aborting
(.locust_env) PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git checkout -- src/app/core/config_settings.py
(.locust_env) PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git pull origin                    
Updating fb9a0d2..eb9ac84
Fast-forward
 Jenkinsfile                     | 86 +++++++++++++++++++++++++++++++++++++++++
 src/Dockerfile                  | 16 ++++----
 src/app/core/config_settings.py | 41 +++++++++++---------
 3 files changed, 116 insertions(+), 27 deletions(-)
 create mode 100644 Jenkinsfile
(.locust_env) PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025>
```

---

# 10. Git Pull & Merge chuẩn

```bash
git commit -m "Save work"
git pull origin main
# resolve conflict
git add .
git commit -m "Resolve merge conflicts"
git push
```

---

# 11. Bug & Bảo mật

- **Bug `GH013: Repository rule violations found for refs/heads/main`**  
    → GitHub phát hiện vi phạm (ví dụ đẩy secret).
    

**Cách xử lý:**

- Dùng `.gitignore`
    
- Xóa commit nhạy cảm:
    

```bash
git reset --soft HEAD~1
```

- Hoặc dùng `git filter-repo`:
    

```bash
pip install git-filter-repo
git filter-repo --path utils/langfuse_demo/integration_langgraph.ipynb --invert-paths --force
git push --force destination1 refs/heads/manual-refactor-agent-registry:refs/heads/manual-refactor-agent-registry
```

- Hoặc dùng `git filter-branch`:
    

```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch ver1_20250625/robot-ai-lesson/config.yml" \
  --prune-empty --tag-name-filter cat -- --all
```

|Tiêu chí|`git filter-repo`|`git filter-branch`|
|---|---|---|
|**Tốc độ**|⚡ Nhanh hơn 10-50x|🐢 Rất chậm|
|**An toàn**|✅ Tự động backup|⚠️ Dễ gây lỗi|
|**Built-in**|❌ Phải cài|✅ Có sẵn|
|**Cú pháp**|😊 Đơn giản|😵 Phức tạp|
|**Khuyến nghị**|✅ Recommended|⛔ Deprecated|

---

# 12. Gộp nhiều repo / merge nâng cao

## 12.1 Gộp repo nguồn vào repo đích

```bash
git clone https://github.com/username/source-repo.git
cd source-repo
git remote add destination https://github.com/username/destination-repo.git
git push --force destination origin/deploy:main
```

## 12.2 Hợp nhất lịch sử không liên quan

```bash
git pull --no-rebase destination main --allow-unrelated-histories
git merge other-branch --allow-unrelated-histories
git add .
git commit -m "Resolved merge conflicts"
```

## 12.3 Copy nhánh giữa 2 repo (GitKraken)

```bash


# GỘP 1 REPO VÀO 1 NHÁNH CỦA 1 REPO --- GỘP 1 REPO VÀO 1 NHÁNH CỦA 1 REPO SAU ĐÓ HỢP NHẤT, MERGER VÀO MAIN

# BẮT ĐẦU TỪ REPO NGUỒN. 

git clone https://github.com/username/source-repo.git
cd source-repo

git remote add destination https://github.com/Hngdcmnh/content-the-coach
git remote add destination https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025

# push the current branch origin/deploy to the destination branch main of the destination repo
# git push destination origin/deploy:main
# ghi đè
# git push --force destination origin/deploy:main

# or full ref name - - Lệnh này **cập nhật mạnh (force overwrite)** nhánh cùng tên trên remote `destination` để **khớp 100% với nội dung local**.
git push --force destination refs/heads/main:refs/heads/destination_branch
git push --force destination refs/heads/manual-refactor-agent-registry:refs/heads/manual-refactor-agent-registry
git push --force destination refs/heads/v2-1node-manual-refactor-robot-workflow-agent-registry:refs/heads/v2-1node-manual-refactor-robot-workflow-agent-registry

# đến đây là đẩy sang repo đích rồi 

# --- SANG REPO ĐÍCH / LÀ LÊN GITHUB MERGER---
git clone https://github.com/username/destination-repo.git
# **Kéo (pull) những thay đổi từ repo đích vào repo hiện tại**: 
# Điều này đảm bảo rằng bạn có tất cả các thay đổi mới nhất từ repo đích trước khi đẩy lên, tránh ghi đè.
git pull --no-rebase destination main --allow-unrelated-histories

git checkout main

# Lỗi "fatal: refusing to merge unrelated histories" xảy ra khi bạn cố gắng hợp nhất hai nhánh mà không có lịch sử chung. Điều này thường xảy ra khi hai nhánh được khởi tạo từ các kho lưu trữ khác nhau hoặc khi một nhánh được tạo mới mà không có bất kỳ commit nào từ nhánh gốc. Để giải quyết vấn đề này, bạn có thể sử dụng tùy chọn `--allow-unrelated-histories` để cho phép hợp nhất các lịch sử không liên quan:
git merge aPartGenspark_wantMerge --allow-unrelated-histories
# Xử lý xung đột nếu có, sau đó thêm và commit
git add .
git commit -m "Resolved merge conflicts"



```

Lúc này check remote bạn sẽ thấy: 

```bash
PS D:\GIT\robot-lesson-workflow> git remote -v
destination     https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025 (fetch)
destination     https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025 (push)
destination1    https://github.com/DoanNgocCuong/Refactored_CFO_Multi-AI-Agent_LangchainLanggraph_T10_2025 (fetch)
destination1    https://github.com/DoanNgocCuong/Refactored_CFO_Multi-AI-Agent_LangchainLanggraph_T10_2025 (push)
upstream        https://github.com/IsProjectX/robot-lesson-workflow.git (fetch)
upstream        https://github.com/IsProjectX/robot-lesson-workflow.git (push)
PS D:\GIT\robot-lesson-workflow> 
```
---

## Cách anh Thành em đã “chuyển nhà” repo local sang 1 GitHub repo mới, và push lần đầu nên nó tạo nhánh `main` mới trên repo đó. - ko mất history nhé 


```
PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git remote -v
origin  https://github.com/DoanNgocCuong/context-handling-bigmodule_PikaRobot_25112025 (fetch)
origin  https://github.com/DoanNgocCuong/context-handling-bigmodule_PikaRobot_25112025 (push)
PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git remote set-url origin https://github.com/IsProjectX/robot-context-handling.git
PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git push
Enumerating objects: 1077, done.
Counting objects: 100% (1077/1077), done.
Delta compression using up to 16 threads
Compressing objects: 100% (625/625), done.
Writing objects: 100% (1077/1077), 1.89 MiB | 19.00 MiB/s, done.
Total 1077 (delta 590), reused 723 (delta 380), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (590/590), done.
To https://github.com/IsProjectX/robot-context-handling.git
 * [new branch]      main -> main

PS D:\GIT\context-handling-bigmodule_PikaRobot_25112025> git remote -v                    
origin  https://github.com/IsProjectX/robot-context-handling.git (fetch)
origin  https://github.com/IsProjectX/robot-context-handling.git (push)
```

## Được rồi! Đây là các bước chi tiết để copy 4 nhánh giữ nguyên tên vào repo đã có sẵn:[^1]

### Các bước thực hiện

**Bước 1: Clone repo nguồn**

```bash
git clone https://github.com/IsProjectX/pika-mem0-enterprise.git
cd pika-mem0-enterprise
```

**Bước 2: Checkout tất cả các nhánh về local**

```bash
git checkout main
git checkout m0-woa-customize-memory
git checkout m1-mem0-SDK-self-hosted
git checkout m2-mem0-is-remote-service
```

**Bước 3: Thêm remote mới trỏ đến repo đích**

```bash
git remote add new-origin <URL_REPO_DICH>
```

**Bước 4: Push tất cả 4 nhánh với tên gốc**

```bash
git push new-origin main
git push new-origin m0-woa-customize-memory
git push new-origin m1-mem0-SDK-self-hosted
git push new-origin m2-mem0-is-remote-service
```

**Hoặc push tất cả cùng lúc:**

```bash
git push new-origin --all
```

Sau khi hoàn thành, repo đích sẽ có thêm 4 nhánh với tên giống hệt repo nguồn, kèm đầy đủ lịch sử commit.[^2][^1]
<span style="display:none">[^3]</span>

<div align="center">⁂</div>

[^1]: https://stackoverflow.com/questions/49621254/git-push-preserving-history-and-branches

[^2]: https://stackoverflow.com/questions/17371150/moving-git-repository-content-to-another-repository-preserving-history

[^3]: image.jpg

```
PS D:\GIT\pika-mem0-enterprise> & d:/GIT/pika-mem0-enterprise/.locust_env/Scripts/Activate.ps1   
(.locust_env) PS D:\GIT\pika-mem0-enterprise> git push new-origin --all
Enumerating objects: 370, done.
Counting objects: 100% (370/370), done.
Delta compression using up to 16 threads
Compressing objects: 100% (320/320), done.
Writing objects: 100% (370/370), 502.71 KiB | 5.91 MiB/s, done.
Total 370 (delta 50), reused 84 (delta 20), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (50/50), done.
To https://github.com/DoanNgocCuong/MiniProj_Agent1_LongTermMemory
 * [new branch]      m0-woa-customize-memory -> m0-woa-customize-memory
 * [new branch]      m1-mem0-SDK-self-hosted -> m1-mem0-SDK-self-hosted
 * [new branch]      m2-mem0-is-remote-service -> m2-mem0-is-remote-service
 * [new branch]      mem0-self-hosted -> mem0-self-hosted
 * [new branch]      real-mem0-self-hosted -> real-mem0-self-hosted
 * [new branch]      self-hosted-migration -> self-hosted-migration
 ! [rejected]        main -> main (fetch first)
error: failed to push some refs to 'https://github.com/DoanNgocCuong/MiniProj_Agent1_LongTermMemory'
hint: Updates were rejected because the remote contains work that you do not
hint: have locally. This is usually caused by another repository pushing to
hint: the same ref. If you want to integrate the remote changes, use
hint: 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.
(.locust_env) PS D:\GIT\pika-mem0-enterprise> git checkout mem0-supadatabase_SoftwareAndHardwareLongTermMemory_RAG_whenGraduation_T5_2025
error: pathspec 'mem0-supadatabase_SoftwareAndHardwareLongTermMemory_RAG_whenGraduation_T5_2025' did not match any file(s) known to git
(.locust_env) PS D:\GIT\pika-mem0-enterprise> git remote -v
new-origin      https://github.com/DoanNgocCuong/MiniProj_Agent1_LongTermMemory (fetch)
new-origin      https://github.com/DoanNgocCuong/MiniProj_Agent1_LongTermMemory (push)
origin  https://github.com/IsProjectX/pika-mem0-enterprise (fetch)
origin  https://github.com/IsProjectX/pika-mem0-enterprise (push)
(.locust_env) PS D:\GIT\pika-mem0-enterprise>
```


# 13. Ghi chú hình ảnh

![[../../New folder/3_Learning/attachments/Pasted image 20241106122454.png]]

# 14. Git Merge

Sang branch 2 => ấn `git merge branch2`

```
  
Sau khi git merge branch2  
  
create mode 100644 utils/refactored_index_Workflow_API30000_Web9433/utils_autoQC_logic.js  
create mode 100644 utils/refactored_index_Workflow_API30000_Web9433/utils_responseProcessor.js  
create mode 100644 utils/refactored_index_Workflow_API30000_Web9433/workflow.html  
delete mode 100644 uv.lock  
PS D:\GIT\robot-lesson-workflow> git push  
fatal: No configured push destination.  
Either specify the URL from the command-line or configure a remote repository using  
  
git remote add <name> <url>  
  
and then push using the remote name  
  
git push <name>  
  
PS D:\GIT\robot-lesson-workflow> git push origin main  
fatal: 'origin' does not appear to be a git repository  
fatal: Could not read from remote repository.  
  
Please make sure you have the correct access rights  
and the repository exists.  
PS D:\GIT\robot-lesson-workflow>
```

## 14.1 Ép dùng nội dung của một phía khi conflict

Khi đang merge và có conflict, có thể chọn toàn bộ nội dung từ một phía:

- Chọn toàn bộ từ branch đang merge vào (theirs):
    
    - Sau khi `git merge branch2` bị conflict:  
        `git checkout --theirs .`  
        `git add .`  
        `git commit`
        
- Chọn toàn bộ từ branch hiện tại (ours):
    
    - Sau khi conflict:  
        `git checkout --ours .`  
        `git add .`  
        `git commit`


## 14.2 Ép branch1 giống hệt branch2 (overwrite lịch sử)

Nếu mục tiêu là làm branch1 giống y chang branch2 (rất mạnh tay, cẩn thận vì lịch sử thay đổi):

- Đang ở branch1:
    
    - `git reset --hard branch2`
        
    - `git push -f origin branch1` (force push, nên dùng khi bạn hiểu rõ rủi ro với team).[](https://www.reddit.com/r/git/comments/bqx85v/how_do_i_overwrite_one_branch_with_another_branch/)​
        

Đây là cách “bắt buộc” nhất: branch1 sẽ trùng snapshot với branch2, bỏ qua khác biệt trên branch1.

## 14.3 cách xoá hét các nhánh git trên local ko xoá trên remote 

```
git switch main # Hoặc master nếu repo dùng tên cũ git branch | grep -v "main" | xargs git branch -D
```

## 14.4 Đẩy sau khi merge 
```
PS D:\GIT\robot-lesson-workflow> git remote -v
destination     https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025 (fetch)
destination     https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025 (push)
destination1    https://github.com/DoanNgocCuong/Refactored_CFO_Multi-AI-Agent_LangchainLanggraph_T10_2025 (fetch)
destination1    https://github.com/DoanNgocCuong/Refactored_CFO_Multi-AI-Agent_LangchainLanggraph_T10_2025 (push)
upstream        https://github.com/IsProjectX/robot-lesson-workflow.git (fetch)
upstream        https://github.com/IsProjectX/robot-lesson-workflow.git (push)
PS D:\GIT\robot-lesson-workflow> git push -f origin manual-refactor
fatal: 'origin' does not appear to be a git repository
fatal: Could not read from remote repository.

Please make sure you have the correct access rights
and the repository exists.
```

```
1. git reset --hard dev-mysql-v2-1node-manual-refactor-robot-workflow-agent-registry  # Ép manual-refactor giống branch kia
2. git log --oneline -5  # Verify HEAD = 1128674 [Small Update - UI]
3. git push -f upstream manual-refactor  # Force push thành công lên upstream
4. git switch main  # Dọn dẹp
5. git branch | grep -v "main" | xargs git branch -D  # Xóa local branches thừa

```

```
PS D:\GIT\robot-lesson-workflow> git branch
+ 2025-10-30-61yu-Dr2qM
+ 2025-10-30-ilv9-QvmUu
  agent-registry
  dev-mysql-v2-1node-manual-refactor-robot-workflow-agent-registry
  dev/fixchatbot
  dev/todoagent
+ feat-combine-sims-excel-EDlPv
+ feat-combine-sims-excel-QLGby
+ feat-excel-one-sheet-TsL2B
  main
* manual-refactor
  manual-refactor-agent-registry
  v2-1node-manual-refactor-robot-workflow-agent-registry
PS D:\GIT\robot-lesson-workflow> git log --oneline -5
1128674 (HEAD -> manual-refactor, upstream/dev-mysql-v2-1node-manual-refactor-robot-workflow-agent-registry, v2-1node-manual-refactor-robot-workflow-agent-registry, dev-mysql-v2-1node-manual-refactor-robot-workflow-agent-registry) [Small Update - UI]
3540986 [Small Update: Logic IMAGE LISTENING lấy của INTENT (câu của intent đã có câu hỏi của câu tiếp theo) / của IMAGE_LISTENING của cục QUESTION NEXT ] --- Logic chung hiện tại đang là : 
6069d8f [Small Update: Fix bug logic từ excel -> scenario ko ăn IMAGE_LISTENING ===> Nguyên nhân do update nhầm file lúc runtime (Đúng ra phải là file lúc ban đầu khởi tạo) --- Đang kiểm tra lại: dù đã tạo bot mới nhưng vẫn thiếu 'IMAGE_LISTENING'. Kiểm tra 'data_excel.json' và scenario được tạo:
bb6bab9 [Small Update: Add IMAGE_LISTENING of QUESTION to scenario -> to run time]
e1ac891 REPORT BENCHMARK : * Luông mới với gpt-oss-20b: GIẢM 1/2 SO VỚI LUỒNG CŨ (P95 giảm từ 1.3s -> 0.8s, P99 giảm từ 2.1s -> 1.2s) ---

PS D:\GIT\robot-lesson-workflow> git remote -v 
destination     https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025 (fetch)
destination     https://github.com/DoanNgocCuong/robot-lesson-workflow_stepUpEducation_22092025 (push)
destination1    https://github.com/DoanNgocCuong/Refactored_CFO_Multi-AI-Agent_LangchainLanggraph_T10_2025 (fetch)
destination1    https://github.com/DoanNgocCuong/Refactored_CFO_Multi-AI-Agent_LangchainLanggraph_T10_2025 (push)
upstream        https://github.com/IsProjectX/robot-lesson-workflow.git (fetch)
upstream        https://github.com/IsProjectX/robot-lesson-workflow.git (push)
PS D:\GIT\robot-lesson-workflow> git push -f upstream manual-refactor .\image\sssssssssssss^C
PS D:\GIT\robot-lesson-workflow> ^C
PS D:\GIT\robot-lesson-workflow> git push -f upstream manual-refactor 
Total 0 (delta 0), reused 0 (delta 0), pack-reused 0 (from 0)
To https://github.com/IsProjectX/robot-lesson-workflow.git
   25ea66c..1128674  manual-refactor -> manual-refactor
PS D:\GIT\robot-lesson-workflow> 
```


# 15. git 