# PRODUCTION QUALITY CHECKLIST
## Sai Lầm Thường Gặp & Best Practices Tương Ứng

**Theo MECE — 19 Khía Cạnh Cốt Lõi | Dành cho AI Engineer, DevOps, SRE, Tech Lead**
**Version 3.0 — March 2026**

---

## Cách Sử Dụng Tài Liệu

| Bước | Hành động |
|------|-----------|
| **1. Duyệt từng domain** | Lần lượt đi qua 19 domain, xem ASCII diagram để hiểu big picture |
| **2. Kiểm tra hệ thống** | Với mỗi sai lầm, kiểm tra xem hệ thống của bạn có mắc phải không |
| **3. Nhận diện dấu hiệu** | Nếu có dấu hiệu (cột Triệu chứng), đánh dấu là cần xử lý |
| **4. Đánh giá impact** | Đọc cột Tác động để ưu tiên xử lý |
| **5. Áp dụng best practice** | Triển khai best practice tương ứng, tham khảo Tool Stack |

**Tổng cộng: 72 sai lầm thường gặp với best practices tương ứng, bao phủ toàn diện 19 khía cạnh của Production Quality.**

---

## Phân Tích MECE (Mutually Exclusive, Collectively Exhaustive)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    19 DOMAINS — MECE ANALYSIS                               │
│                                                                             │
│   ME (Mutually Exclusive): Mỗi domain có PRIMARY FOCUS riêng biệt          │
│   CE (Collectively Exhaustive): Cover toàn bộ production lifecycle          │
└─────────────────────────────────────────────────────────────────────────────┘

  DESIGN TIME                    BUILD TIME                   RUN TIME
  ───────────                    ──────────                   ────────
  ┌───────────────┐             ┌───────────────┐           ┌───────────────┐
  │ I.  Architect.│             │ IV. Deploy/CI │           │ II. Reliabil. │
  │ IX. API Design│             │ VI. Testing   │           │ III.Observab. │
  │ VIII.Database │             │ XI. Code Qual.│           │ VII.Error Hnd.│
  └───────────────┘             └───────────────┘           │ XIV.Prod Read.│
                                                             └───────────────┘
  CROSS-CUTTING
  ─────────────
  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐
  │ V.  Security  │  │ X. Config/Sec.│  │ XII.Infra/K8s │  │XIII.Docs/Run. │
  └───────────────┘  └───────────────┘  └───────────────┘  └───────────────┘

  SPECIALIZED
  ───────────
  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐
  │XV. MLOps/AI   │  │XVI.Caching/Per│  │XVII.Async/MQ  │  │XVIII.Complian.│
  └───────────────┘  └───────────────┘  └───────────────┘  └───────────────┘
                     ┌───────────────┐
                     │XIX.Chaos Eng. │
                     └───────────────┘
```

### Ghi chú MECE — Overlap hợp lý

| Overlap | Giải thích tại sao tách riêng |
|---------|-------------------------------|
| VII (Error Handling) vs II (Reliability) | II focus patterns hệ thống (Circuit Breaker, Retry). VII focus coding practices (exception handling, error classification). |
| X (Secrets) vs V (Security) | V focus attack vectors & defense (OWASP, WAF). X focus config management & operational secrets. |
| XIII (Documentation) vs XIV (Production Readiness) | XIII focus day-to-day docs. XIV focus go-live gate & SLO/SLI. |
| XIX (Feature Flags) vs IV (Deployment) | IV focus pipeline & strategy. XIX focus proactive failure testing & progressive release. |

---

## Mục Lục

| # | Domain | Số items |
|---|--------|----------|
| I | Architecture & Design | 4 |
| II | Reliability & Resilience | 6 |
| III | Observability & Monitoring | 5 |
| IV | Deployment & CI/CD | 4 |
| V | Security | 7 |
| VI | Testing | 4 |
| VII | Error Handling | 3 |
| VIII | Database & Data Management | 4 |
| IX | API Design | 5 |
| X | Configuration & Secrets Management | 3 |
| XI | Code Quality & Maintainability | 3 |
| XII | Infrastructure & Containerization | 4 |
| XIII | Documentation & Runbooks | 3 |
| XIV | Production Readiness | 3 |
| XV | MLOps & AI Systems | 3 |
| XVI | Caching & Performance Optimization | 3 |
| XVII | Async Processing & Message Queues | 3 |
| XVIII | Compliance & Data Protection | 3 |
| XIX | Chaos Engineering & Resilience Testing | 2 |

---

## I. ARCHITECTURE & DESIGN

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                   ARCHITECTURE & DESIGN — BEST PRACTICES                    │
└─────────────────────────────────────────────────────────────────────────────┘

  PRODUCTION ARCHITECTURE DECISION TREE
  ═══════════════════════════════════════

  Team size?
    │
    ├── 1-3 devs ──────────────────────▶ MODULAR MONOLITH
    │                                      │
    ├── 4-10 devs ─────────────────────▶ MODULAR MONOLITH + Service-Oriented
    │                                      │
    └── 10+ devs, clear domain ────────▶ MICROSERVICES
                                           │
                                           ▼
  ┌──────────────────────────────────────────────────────────────────────────┐
  │                    TARGET ARCHITECTURE                                    │
  │                                                                          │
  │   ┌──────────┐     ┌──────────────┐     ┌──────────────┐               │
  │   │  Client  │────▶│ API Gateway  │────▶│  Service A   │               │
  │   │ (Web/App)│     │ (Kong/APIGW) │     │  (Domain 1)  │               │
  │   └──────────┘     │              │     └──────┬───────┘               │
  │                     │  - Auth      │            │                        │
  │                     │  - Rate Limit│     ┌──────┴───────┐               │
  │                     │  - Routing   │     │   DB (PG)    │               │
  │                     │  - Versioning│     └──────────────┘               │
  │                     │              │                                     │
  │                     │              │     ┌──────────────┐               │
  │                     │              │────▶│  Service B   │               │
  │                     └──────────────┘     │  (Domain 2)  │               │
  │                                          └──────┬───────┘               │
  │                                                 │                        │
  │                                          ┌──────┴───────┐               │
  │                                          │ Redis Cache  │               │
  │                                          └──────────────┘               │
  └──────────────────────────────────────────────────────────────────────────┘

  DATABASE SELECTION GUIDE
  ════════════════════════
  ┌────────────────┬────────────────────────┬─────────────────────────┐
  │ Use Case       │ Database               │ Khi nào                 │
  ├────────────────┼────────────────────────┼─────────────────────────┤
  │ Default        │ PostgreSQL             │ Hầu hết mọi trường hợp │
  │ Time-series    │ TimescaleDB/InfluxDB   │ IoT, metrics, logs      │
  │ Cache/Session  │ Redis                  │ Hot data, sessions      │
  │ Flexible schema│ MongoDB                │ Evolving schema         │
  │ Search         │ Elasticsearch          │ Full-text search        │
  │ High write     │ Cassandra/ScyllaDB     │ > 10K writes/sec        │
  │ Global scale   │ CockroachDB/Spanner    │ Multi-region, > 100M    │
  └────────────────┴────────────────────────┴─────────────────────────┘

  CAPACITY PLANNING
  ═════════════════
  Estimate DAU ──▶ Peak concurrency ──▶ Storage 12mo ──▶ Plan 3x headroom
                                                              │
                                                              ▼
                                                     Review hàng quý
```

### I.1 — Monolithic architecture cho hệ thống lớn, không có separation of concerns

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Deploy 1 tính năng nhỏ phải deploy toàn bộ. 1 lỗi nhỏ làm sập toàn hệ thống. Team conflict khi merge code. |
| **Tác động** | Downtime toàn hệ thống khi deploy. Không thể scale từng phần độc lập. Velocity giảm dần theo thời gian. |
| **Best Practice** | Bắt đầu với **Modular Monolith**, tách module rõ ràng. Chỉ chuyển sang Microservices khi team > 10 người và domain boundaries rõ. Áp dụng **Strangler Fig Pattern** để migration dần dần. |

### I.2 — Chọn sai database cho use case

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Query chậm dần khi data lớn. Index không hiệu quả. Storage tăng nhanh bất thường. |
| **Tác động** | Latency tăng, user experience tệ. Chi phí infra tăng 3-5x. Re-architecture tốn kém. |
| **Best Practice** | **PostgreSQL** làm default choice. **TimescaleDB** cho time-series. **Redis** cho caching/sessions. **MongoDB** cho flexible schema. Đánh giá read/write ratio, data model trước khi chọn. |

### I.3 — Không có API Gateway, client gọi trực tiếp từng service

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Client phải biết địa chỉ của nhiều services. Authentication logic bị duplicate. Không có rate limiting thống nhất. |
| **Tác động** | Security gap nghiêm trọng. Khó quản lý và monitor. Breaking changes ảnh hưởng trực tiếp client. |
| **Best Practice** | Dùng **API Gateway** (Kong, AWS API Gateway) làm single entry point. Centralized authentication, rate limiting, request routing. API versioning thống nhất. |

### I.4 — Thiếu capacity planning, không đánh giá scalability từ đầu

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Hệ thống chậm vào giờ cao điểm. CPU/Memory thường xuyên > 80%. Database connection pool cạn kiệt. |
| **Tác động** | Service degradation hoặc downtime vào peak hours. Mất khách hàng. Emergency scaling tốn kém gấp 3-5x. |
| **Best Practice** | Ước tính DAU và peak concurrency. Tính storage growth rate 12 tháng. Plan **3x headroom** trên expected peak. Review capacity hàng quý. |

---

## II. RELIABILITY & RESILIENCE

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                RELIABILITY & RESILIENCE — BEST PRACTICES                    │
└─────────────────────────────────────────────────────────────────────────────┘

  RESILIENCE PATTERNS STACK (áp dụng từ trong ra ngoài)
  ═════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────────────────┐
  │  Layer 5: DISASTER RECOVERY                                            │
  │  3-2-1 Backup | DR Drill hàng quý | RTO/RPO defined                   │
  ├─────────────────────────────────────────────────────────────────────────┤
  │  Layer 4: BULKHEAD (Failure Isolation)                                 │
  │  Tách connection pools | Thread pool isolation | Resource limits       │
  ├─────────────────────────────────────────────────────────────────────────┤
  │  Layer 3: FALLBACK (Graceful Degradation)                              │
  │  Cached data | Default value | Simplified response | Feature flags     │
  ├─────────────────────────────────────────────────────────────────────────┤
  │  Layer 2: CIRCUIT BREAKER (Ngắt mạch)                                 │
  │  Closed ──▶ Open ──▶ Half-Open | fail_max=5 | reset_timeout=60s       │
  ├─────────────────────────────────────────────────────────────────────────┤
  │  Layer 1: RETRY + TIMEOUT (Nền tảng)                                   │
  │  Exponential Backoff + Jitter | Max 3 retries | Connect 3-5s Read 30s │
  └─────────────────────────────────────────────────────────────────────────┘

  REQUEST FLOW QUA RESILIENCE STACK
  ═════════════════════════════════

  Client Request
      │
      ▼
  ┌──────────┐  timeout   ┌──────────────┐  circuit  ┌──────────────┐
  │  Timeout  │──expired──▶│ Circuit Brkr │──open────▶│   Fallback   │
  │  3-30s    │            │ Closed/Open  │           │ Cache/Default│
  └────┬─────┘            └──────┬───────┘           └──────────────┘
       │ ok                      │ closed
       ▼                         ▼
  ┌──────────┐  fail      ┌──────────────┐
  │ External │──────────▶ │    Retry     │
  │ Service  │            │ 1s→2s→4s     │
  └──────────┘            │ max 3 times  │
                          └──────────────┘

  DISASTER RECOVERY: 3-2-1 RULE
  ══════════════════════════════
  3 copies ──▶ 2 different media ──▶ 1 offsite
      │              │                    │
      ▼              ▼                    ▼
   Primary      Local backup         Cloud/Remote
   Database     (SSD/NAS)            (S3/GCS)

  Test restore HÀNG THÁNG | DR drill HÀNG QUÝ | RTO/RPO per service

  TOOLS
  ═════
  ┌────────────────┬────────────────────────────────────────────┐
  │ Pattern        │ Libraries                                  │
  ├────────────────┼────────────────────────────────────────────┤
  │ Retry          │ tenacity (Py), polly (.NET), resilience4j  │
  │ Circuit Breaker│ pybreaker (Py), resilience4j (Java)        │
  │ Timeout        │ httpx timeout, aiohttp timeout             │
  │ Bulkhead       │ K8s resource limits, thread pools          │
  │ Fallback       │ Feature flags (LaunchDarkly, Unleash)      │
  │ DR/Backup      │ pg_dump, AWS Backup, Velero (K8s)          │
  └────────────────┴────────────────────────────────────────────┘
```

### II.1 — Không có Retry logic với Exponential Backoff

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Request thất bại ngay khi gặp transient error. Log đầy lỗi timeout/connection refused nhưng chỉ xảy ra ngắn. |
| **Tác động** | User thấy lỗi không cần thiết. Tỷ lệ lỗi cao hơn thực tế. Mất trust của user. |
| **Best Practice** | Implement retry với **exponential backoff + jitter** (1s, 2s, 4s...). Giới hạn max 3 lần retry. Chỉ retry transient errors (5xx, timeout). Dùng thư viện: `tenacity` (Python), `polly` (.NET). |

### II.2 — Không có Circuit Breaker, để cascading failures lan tỏa

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | 1 service down kéo theo nhiều service khác down. Request bị treo chờ timeout. Connection pool cạn kiệt trên nhiều services. |
| **Tác động** | **Cascading failure** — toàn bộ hệ thống sập chỉ vì 1 dependency down. Recovery time rất lâu (hàng giờ). |
| **Best Practice** | Implement Circuit Breaker với 3 states: **Closed/Open/Half-Open**. Config: `fail_max=5`, `reset_timeout=60s`. Return cached data hoặc default value khi circuit open. Dùng `pybreaker`, `resilience4j`, `Polly`. |

### II.3 — Không đặt Timeout cho external calls

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Request bị treo vô thời hạn. Worker processes bị occupied. Hệ thống chậm dần rồi không phản hồi. |
| **Tác động** | **Resource exhaustion** — hết connection, hết thread. Hệ thống ngưng phản hồi hoàn toàn. Cần restart để phục hồi. |
| **Best Practice** | LUÔN đặt timeout cho mọi external call (DB, API, cache). **Connect timeout: 3-5s**. **Read timeout: 5-30s** tùy use case. Dùng deadline propagation trong microservices. |

### II.4 — Không có Fallback strategy khi dependency fail

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Khi 1 service/API down, feature liên quan mất hoàn toàn. User nhận lỗi 500 thay vì degraded experience. |
| **Tác động** | User experience bị phá hủy hoàn toàn. Mất revenue từ feature bị ảnh hưởng. User chuyển sang đối thủ. |
| **Best Practice** | Thiết kế **Graceful Degradation**: trả về cached data, default value, hoặc simplified response. Ưu tiên: Cache > Default > Error message có ý nghĩa. Feature flags để tắt feature có vấn đề. |

### II.5 — Không có Bulkhead Pattern, không có failure isolation

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | 1 endpoint bị overload làm ảnh hưởng toàn bộ API. Slow queries ảnh hưởng các request khác. |
| **Tác động** | Không có fault isolation. 1 vấn đề nhỏ có thể làm sập toàn bộ hệ thống. |
| **Best Practice** | Tách connection pools theo service/dependency. Thread pool isolation cho critical paths. Rate limiting per-endpoint. Kubernetes: resource limits per pod. |

### II.6 — Backup không được test định kỳ, DR plan chỉ có trên giấy

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Chưa bao giờ restore thử từ backup. DR plan viết từ lâu chưa cập nhật. RTO/RPO không ai biết là bao nhiêu. |
| **Tác động** | Khi sự cố thật sự: backup có thể bị hỏng, restore thất bại, mất data. RTO thực tế gấp 5-10x dự kiến. |
| **Best Practice** | Test restore backup **hàng tháng**. Chạy DR drill **hàng quý**. Định nghĩa RTO/RPO rõ ràng cho từng service. Backup theo quy tắc **3-2-1**: 3 bản sao, 2 loại media, 1 offsite. |

---

## III. OBSERVABILITY & MONITORING

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                 PRODUCTION OBSERVABILITY STACK — 6 TẦNG                     │
│                                                                             │
│  "Không thể fix thứ không nhìn thấy"                                       │
└─────────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════════
 TẦNG 6: RUNBOOKS & INCIDENT RESPONSE          ← Con người hành động
═══════════════════════════════════════════════════════════════════════════════

  Detect ──▶ Diagnose ──▶ Fix ──▶ Verify ──▶ Prevent

  Tools:   Runbook templates (Markdown/Confluence)
           PagerDuty / Opsgenie (on-call rotation)
           Postmortem templates
  Output:  MTTR giảm, không ai phải nhớ trong đầu cách fix

───────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
 TẦNG 5: ALERTING                               ← Máy phát hiện sớm
═══════════════════════════════════════════════════════════════════════════════

  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────┐
  │  Error rate > 5%  │    │  P95 > 10s       │    │  Cost spike 2x   │
  │  trong 5 phút     │    │  trong 5 phút     │    │  so với avg       │
  └────────┬─────────┘    └────────┬─────────┘    └────────┬─────────┘
           │                       │                       │
           └───────────────────────┼───────────────────────┘
                                   ▼
                          ┌────────────────┐
                          │  Slack / Email  │
                          │  + Runbook link │
                          └────────────────┘

  Quy tắc: Alert SYMPTOM (error rate) không phải CAUSE (CPU cao)
  Severity: P1 (pager) | P2 (Slack) | P3 (ticket)
  Tools:   Alertmanager (Prometheus) | Grafana Alerting | PagerDuty

───────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
 TẦNG 4: METRICS & DASHBOARDS                   ← Đo lường liên tục
═══════════════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────────────┐
  │                         Dashboard                                   │
  │                                                                     │
  │  RED Metrics (mỗi service):                                        │
  │  ┌───────────┐  ┌───────────┐  ┌───────────────────┐              │
  │  │   Rate    │  │  Errors   │  │    Duration        │              │
  │  │  req/sec  │  │  error %  │  │  p50, p95, p99     │              │
  │  └───────────┘  └───────────┘  └───────────────────┘              │
  │                                                                     │
  │  USE Metrics (mỗi resource):     Business Metrics:                 │
  │  - Utilization (CPU, Mem)         - Lessons/day                    │
  │  - Saturation (queue depth)       - Conversion rate                │
  │  - Errors (disk, network)         - Revenue/hour                   │
  └─────────────────────────────────────────────────────────────────────┘

  Max 7-10 panels / dashboard | Tách layer: Business > Service > Infra
  Tools:   Grafana + Prometheus + Mimir | Datadog | Kibana (ELK)

───────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
 TẦNG 3: DISTRIBUTED TRACING                    ← Request đi qua đâu?
═══════════════════════════════════════════════════════════════════════════════

  User Request
    │
    ▼
  ┌──────┐ 120ms  ┌──────────────┐ 45ms  ┌─────────┐ 2100ms  ┌─────┐
  │ API  │───────▶│ Orchestration │──────▶│ LLM Call │────────▶│ TTS │
  └──────┘        └──────────────┘        └─────────┘         └─────┘
                         │                     ▲
                         │ 30ms                │ ← Bottleneck!
                         ▼                     │
                  ┌──────────────┐             │
                  │   Emotion    │─────────────┘
                  │   Service    │
                  └──────────────┘

  trace_id: abc-123 (propagate qua HTTP header xuyên tất cả services)
  Standard: OpenTelemetry (OTEL) ← vendor-neutral

  ┌─────────────────────────────┬──────────────────────────────┐
  │ Infrastructure Tracing      │ LLM Tracing                  │
  │ Jaeger, Tempo, Zipkin       │ Langfuse, LangSmith          │
  │ Datadog APM (SaaS)          │ Helicone (SaaS)              │
  └─────────────────────────────┴──────────────────────────────┘

───────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
 TẦNG 2: LOG AGGREGATION                        ← Thu thập về 1 nơi
═══════════════════════════════════════════════════════════════════════════════

  Service A ──┐
  Service B ──┼──▶ [Collector] ──▶ [Storage + Index] ──▶ [Query UI]
  Service C ──┤
  Service D ──┘

  Option 1: Grafana Loki    ★ RECOMMENDED (nhẹ, K8s native, LogQL)
  Option 2: ELK Stack       (full-text search, phù hợp fintech)
  Option 3: SigNoz          (all-in-one Logs+Traces+Metrics)

───────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════════
 TẦNG 1: STRUCTURED LOGGING                     ← Foundation (bắt buộc)
═══════════════════════════════════════════════════════════════════════════════

  ❌ print("API call success, model=gpt-4, duration=450ms")
  ✅ logger.info("api.request.success", model="gpt-4", duration_ms=450)

  Output: {"event":"api.request.success","model":"gpt-4","duration_ms":450}

  Quy tắc: JSON format | dot.separated event names | request_id xuyên suốt
  Tools:   structlog (Py) | std logging + JSON | loguru (scripts)

═══════════════════════════════════════════════════════════════════════════════

  BEST PRACTICE STACK COMBINATIONS
  ┌───────────────────────────────────────────────────────────────────────┐
  │ ★ Open Source "LGTM Stack":                                          │
  │   Loki + Grafana + Tempo + Mimir + Alertmanager + Langfuse          │
  │                                                                      │
  │ ★ SaaS:                                                              │
  │   Datadog (all-in-one) + Langfuse (LLM tracing)                     │
  │                                                                      │
  │ ★ All-in-One OSS:                                                    │
  │   SigNoz + Langfuse                                                  │
  └───────────────────────────────────────────────────────────────────────┘

  TRIỂN KHAI THEO THỨ TỰ:
  Tầng 1 → Tầng 2 → Tầng 3 → Tầng 4 → Tầng 5 → Tầng 6
  (1 ngày)  (1-2d)   (2-3d)   (1-2d)   (1d)     (ongoing)
  Tổng: ~2-3 tuần cho full production observability stack
```

### III.1 — Không có monitoring từ đầu, chỉ setup khi có sự cố

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không biết hệ thống đang có vấn đề cho đến khi user báo. Debug bằng cách đọc log thủ công. Không có metrics nào được track. |
| **Tác động** | MTTR (Mean Time To Resolve) rất cao. Không phát hiện vấn đề sớm. Reactive thay vì proactive. |
| **Best Practice** | Setup monitoring **TRƯỚC** khi go live. Track **4 Golden Signals**: Latency, Traffic, Errors, Saturation. Dùng Prometheus + Grafana. Đặt alert cho mỗi signal. |

### III.2 — Alert Fatigue — quá nhiều alert không quan trọng

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Hàng trăm alerts mỗi ngày. Team bắt đầu ignore alerts. Alert channel bị mute. |
| **Tác động** | Alert thật sự quan trọng bị bỏ lỡ. Incident không được xử lý kịp thời. On-call burnout. |
| **Best Practice** | Chỉ alert cho **symptom** (error rate cao) không phải cause (CPU cao). Phân loại severity: P1 (pager), P2 (Slack), P3 (ticket). Review và clean up alerts hàng tháng. Mỗi alert phải có runbook. |

### III.3 — Log không có structure, không tập trung

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Log nằm rải rác trên nhiều server. Không thể search/filter hiệu quả. Thiếu context (request_id, user_id). |
| **Tác động** | Debug mất hàng giờ thay vì phút. Không thể correlate events across services. Không thể tạo alert từ logs. |
| **Best Practice** | **Structured logging** (JSON format). **Centralized log** (ELK Stack, Loki). **Correlation ID** (request_id) qua mọi service. Log levels đúng: DEBUG/INFO/WARN/ERROR/FATAL. |

### III.4 — Không có Distributed Tracing

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không biết request đi qua những service nào. Không xác định được bottleneck ở đâu. Debug cross-service mất rất nhiều thời gian. |
| **Tác động** | Incident resolution chậm gấp 5-10x. Performance issues khó xác định root cause. Không có visibility vào hệ thống. |
| **Best Practice** | Implement **OpenTelemetry** cho tracing. Propagate trace context qua mọi service call. Dùng **Jaeger/Zipkin** để visualize traces. Sample rate: 100% cho errors, 1-10% cho normal traffic. |

### III.5 — Dashboard quá nhiều metrics, không ai hiểu

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Dashboard có 50+ panels. Không ai biết panel nào quan trọng. Mỗi lần có sự cố vẫn phải hỏi senior. |
| **Tác động** | Incident response chậm. Thông tin quan trọng bị chìm. Decision making kém. |
| **Best Practice** | Tối đa **7-10 panels** trên 1 dashboard. Tách dashboard theo layer: Business > Service > Infrastructure. **RED method** cho services (Rate, Errors, Duration). **USE method** cho resources (Utilization, Saturation, Errors). |

---

## IV. DEPLOYMENT & CI/CD

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                  DEPLOYMENT & CI/CD — BEST PRACTICES                        │
└─────────────────────────────────────────────────────────────────────────────┘

  CI/CD PIPELINE (AUTOMATED — ZERO TOUCH)
  ════════════════════════════════════════

  Developer                   CI Pipeline                      Production
  ─────────                   ───────────                      ──────────
  git push ──▶ ┌──────────────────────────────────────────┐
               │  1. BUILD                                 │
               │     Docker multi-stage build              │
               │     Cache dependencies                    │
               ├──────────────────────────────────────────┤
               │  2. TEST                                  │
               │     Unit ──▶ Integration ──▶ E2E          │
               │     Coverage gate >= 80%                  │
               ├──────────────────────────────────────────┤
               │  3. SECURITY SCAN                         │
               │     SAST (SonarQube) + Dependency (Snyk)  │
               │     Secrets detection (gitleaks)          │
               ├──────────────────────────────────────────┤
               │  4. DEPLOY                                │  ──▶ Production
               │     Canary 1-5% ──▶ 25% ──▶ 100%        │
               │     Auto-rollback if error_rate > 5%      │
               ├──────────────────────────────────────────┤
               │  5. MONITOR                               │
               │     Health check + Error rate + Latency   │
               │     5 phút observation window             │
               └──────────────────────────────────────────┘

  DEPLOYMENT STRATEGIES
  ═════════════════════

  ┌─────────────────┬──────────────────────────────────────────────────┐
  │ Strategy        │ Khi nào dùng                                     │
  ├─────────────────┼──────────────────────────────────────────────────┤
  │ Canary          │ Default choice. Deploy 1-5% trước, monitor,     │
  │   1%──▶100%     │ tăng dần. Best cho hầu hết trường hợp.         │
  ├─────────────────┼──────────────────────────────────────────────────┤
  │ Blue-Green      │ Cần instant rollback. 2 environments,           │
  │   Blue ⇄ Green  │ switch traffic. Tốn 2x resources.              │
  ├─────────────────┼──────────────────────────────────────────────────┤
  │ Rolling         │ K8s default. Update pods dần dần.               │
  │   ████░░░░      │ Zero downtime, ít resource overhead.            │
  ├─────────────────┼──────────────────────────────────────────────────┤
  │ Feature Flags   │ Tách deployment ≠ release. Deploy code          │
  │   🏳️ ON/OFF     │ lên prod nhưng chưa bật cho users.             │
  └─────────────────┴──────────────────────────────────────────────────┘

  ROLLBACK STRATEGY
  ═════════════════
  Error rate > threshold ──▶ Auto-rollback ──▶ Alert team
                                  │
                            Giữ 3 versions trước
                            DB migration có DOWN script
                            Test rollback thường xuyên

  ENVIRONMENT PARITY (12-Factor)
  ══════════════════════════════
  ┌──────────┐    ┌──────────┐    ┌──────────┐
  │   Dev    │ ═══│ Staging  │ ═══│   Prod   │  ← PHẢI GIỐNG NHAU
  │          │    │ (mirror) │    │          │
  └──────────┘    └──────────┘    └──────────┘
  IaC (Terraform) + Docker ──▶ Đảm bảo consistency

  TOOLS
  ═════
  Pipeline: GitHub Actions | GitLab CI | Jenkins | CircleCI
  IaC:      Terraform | Pulumi | AWS CDK
  Deploy:   ArgoCD (K8s) | AWS CodeDeploy | Spinnaker
  Flags:    LaunchDarkly | Unleash | Flagsmith
```

### IV.1 — Deploy thủ công (SSH vào server, pull code, restart)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Deploy mất hàng giờ. Quy trình khác nhau tùy người. Config sai giữa các lần deploy. |
| **Tác động** | Human error cao. Không thể rollback nhanh. Downtime mỗi khi deploy. Không scale được team. |
| **Best Practice** | CI/CD pipeline tự động: **build > test > security scan > deploy > monitor**. Pipeline as Code (Jenkinsfile, GitHub Actions). Zero-touch deployment — không ai cần SSH vào server. |

### IV.2 — Deploy tất cả traffic cùng lúc (Big Bang deployment)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Mỗi khi deploy là hồi hộp. Lỗi ảnh hưởng 100% users ngay lập tức. Rollback là phương án cuối cùng. |
| **Tác động** | 100% users bị ảnh hưởng nếu có lỗi. Revenue loss lớn. Reputation damage. |
| **Best Practice** | **Canary Deployment**: deploy cho 1-5% users trước, monitor, rồi tăng dần. **Blue-Green**: 2 environments, switch traffic. **Rolling Deployment** cho Kubernetes. **Feature Flags** để tách deployment và release. |

### IV.3 — Không có rollback strategy hoặc rollback không được test

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Khi có lỗi, team mất hàng giờ để fix forward. Không ai biết cách rollback. Database migration không có reverse. |
| **Tác động** | Downtime kéo dài. Panic trong team. Có thể làm vấn đề tồi tệ hơn. |
| **Best Practice** | **Automated rollback** trigger khi error rate vượt threshold. Rollback được test thường xuyên. Database migration luôn có rollback script. Giữ 3 version trước để rollback. |

### IV.4 — Environment Drift — Staging khác Production

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Code chạy tốt trên Staging nhưng lỗi trên Prod. Config, data, infra khác nhau giữa các môi trường. |
| **Tác động** | Bugs chỉ xuất hiện trên Production. Confidence khi deploy thấp. Testing không có giá trị. |
| **Best Practice** | **Infrastructure as Code** (Terraform/Pulumi). Docker để đảm bảo consistency. Staging mirror Production config (sanitized data). **12-Factor App**: Dev/Prod Parity. |

---

## V. SECURITY

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      SECURITY — DEFENSE IN DEPTH                            │
└─────────────────────────────────────────────────────────────────────────────┘

  7 LAYERS BẢO VỆ (từ ngoài vào trong)
  ═════════════════════════════════════

  Internet
    │
    ▼
  ┌─────────────────────────────────────────────────────────────────────────┐
  │ Layer 7: NETWORK SECURITY                                              │
  │ WAF (CloudFlare/AWS WAF) | DDoS Protection | VPC + Private Subnets    │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ Layer 6: ENCRYPTION                                                    │
  │ HTTPS/TLS 1.3 everywhere | mTLS service-to-service | AES-256 at rest  │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ Layer 5: SECURITY HEADERS                                              │
  │ HSTS | CSP | X-Frame-Options: DENY | CORS whitelist (no wildcard *)   │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ Layer 4: AUTH & AUTHZ                                                  │
  │ OAuth 2.0 + OIDC | JWT (15-30min) + Refresh | RBAC | MFA for admins  │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ Layer 3: INPUT VALIDATION                                              │
  │ Parameterized queries (NO string concat) | Server-side validation     │
  │ Output encoding (XSS) | Content Security Policy                       │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ Layer 2: SECRETS MANAGEMENT                                            │
  │ Vault / AWS SM | Runtime injection | Auto rotation | No .env in Git   │
  ├─────────────────────────────────────────────────────────────────────────┤
  │ Layer 1: SECURITY IN CI/CD                                             │
  │ SAST (SonarQube) | DAST | Dependency scan (Snyk) | Secrets detect    │
  └─────────────────────────────────────────────────────────────────────────┘

  OWASP TOP 10 COVERAGE MAP
  ═════════════════════════
  ┌─────────────────────────┬───────────────────────────────────────────┐
  │ OWASP Risk              │ Covered by checklist item                 │
  ├─────────────────────────┼───────────────────────────────────────────┤
  │ A01: Broken Access Ctrl │ V.3 Auth/AuthZ                           │
  │ A02: Crypto Failures    │ V.6 Encryption                           │
  │ A03: Injection          │ V.2 Input Validation                     │
  │ A04: Insecure Design    │ I.1-I.4 Architecture                     │
  │ A05: Security Misconfig │ V.5 Headers, X.1 Config                  │
  │ A06: Vuln Components    │ V.4 Security Scanning                    │
  │ A07: Auth Failures      │ V.3 Auth/AuthZ                           │
  │ A08: Data Integrity     │ IV.3 Rollback, V.4 Scanning              │
  │ A09: Logging Failures   │ III.3 Structured Logging                 │
  │ A10: SSRF               │ V.2 Input Validation, V.7 Network        │
  └─────────────────────────┴───────────────────────────────────────────┘

  THREAT MODELING: STRIDE
  ═══════════════════════
  S — Spoofing      ──▶ Strong auth (V.3)
  T — Tampering     ──▶ Input validation + integrity checks (V.2)
  R — Repudiation   ──▶ Audit logging (III.3)
  I — Info Disclosure ──▶ Encryption + headers (V.5, V.6)
  D — Denial of Svc ──▶ Rate limit + WAF (V.7, IX.3)
  E — Elev. of Priv ──▶ RBAC + least privilege (V.3)
```

### V.1 — Hardcode secrets trong source code (API keys, passwords, tokens)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Secrets nằm trong .env file được commit lên Git. Grep source code thấy credentials. Secrets giống nhau giữa dev và prod. |
| **Tác động** | Data breach nghiêm trọng. Compliance violation (GDPR, PCI-DSS). Bị hacker khai thác từ leaked secrets trên GitHub. |
| **Best Practice** | **Secrets Manager** (HashiCorp Vault, AWS Secrets Manager). Secrets inject lúc runtime qua env vars. Secrets rotation tự động. Git hooks để ngăn commit secrets (`gitleaks`, `trufflehog`). |

### V.2 — Không có input validation, dễ bị SQL Injection/XSS

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | User input được đưa thẳng vào SQL query. Không có sanitization cho HTML output. Error messages lộ database structure. |
| **Tác động** | **SQL Injection**: mất toàn bộ database. **XSS**: đánh cắp session của user. Data breach, legal liability. |
| **Best Practice** | **Parameterized queries / ORM** (KHÔNG BAO GIỜ string concatenation). Input validation ở cả client và server. Output encoding để chống XSS. Content Security Policy headers. |

### V.3 — Authentication/Authorization yếu hoặc thiếu

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không có rate limiting cho login. JWT không có expiry hoặc expiry quá dài. RBAC không được enforce nhất quán. |
| **Tác động** | Brute force attacks thành công. Unauthorized access vào sensitive data. Privilege escalation. |
| **Best Practice** | **OAuth 2.0 / OpenID Connect**. JWT với short expiry (15-30 phút) + refresh token. RBAC enforce ở API Gateway level. **MFA** cho admin accounts. Rate limiting cho auth endpoints. |

### V.4 — Không có Security Scanning trong CI/CD pipeline

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Dependencies có vulnerabilities đã biết. Code có security flaws không ai biết. Secrets bị commit mà không ai phát hiện. |
| **Tác động** | Supply chain attacks. Known vulnerabilities bị exploit. Zero-day exposure cao. |
| **Best Practice** | **SAST** (SonarQube) trong CI pipeline. **Dependency scanning** (Snyk, Dependabot). **DAST** cho staging environment. **Secrets detection** (gitleaks) làm pre-commit hook. |

### V.5 — Thiếu Security Headers (HSTS, CSP, CORS)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Response headers không có HSTS, CSP, X-Frame-Options. CORS cho phép mọi origin (*). Browser console cảnh báo missing security headers. |
| **Tác động** | XSS attacks qua missing CSP. Clickjacking qua missing X-Frame-Options. Man-in-the-middle qua missing HSTS. CORS misconfiguration cho phép unauthorized origins. |
| **Best Practice** | **Helmet.js** (Express) hoặc middleware tương đương. HSTS: `max-age=31536000; includeSubDomains`. CSP: whitelist chỉ trusted sources. CORS: explicit origin list, KHÔNG dùng wildcard (*) trên production. X-Frame-Options: DENY hoặc SAMEORIGIN. |

### V.6 — Thiếu Encryption at Rest và in Transit

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Database không mã hóa. HTTP thay vì HTTPS. Sensitive data truyền plaintext giữa services. Backup không mã hóa. |
| **Tác động** | Data breach nếu disk bị đánh cắp. Man-in-the-middle attacks. Compliance violation (PCI-DSS, GDPR). |
| **Best Practice** | **HTTPS bắt buộc** mọi nơi (TLS 1.3). Encryption at rest cho database (AES-256). mTLS cho service-to-service communication. Encrypt backups. Certificate management tự động (Let's Encrypt, AWS ACM). |

### V.7 — Thiếu Network Security (VPC, Security Groups, WAF)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Database accessible từ Internet. Không có firewall rules. Không có WAF trước web app. |
| **Tác động** | DDoS attacks làm sập hệ thống. Data exfiltration từ database. Unauthorized access vào internal services. |
| **Best Practice** | **VPC** với private subnets cho DB/internal services. **Security Groups** với least-privilege rules. **WAF** trước public-facing services. DDoS protection (CloudFlare, AWS Shield). Rate limiting ở nhiều layers. |

---

## VI. TESTING

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       TESTING — BEST PRACTICES                              │
└─────────────────────────────────────────────────────────────────────────────┘

  TESTING PYRAMID
  ═══════════════

                        ┌─────────┐
                        │  E2E    │  10%  ← Ít nhất, chậm nhất, đắt nhất
                        │ Cypress │       Chỉ critical user flows
                        │Playwrght│       (signup, login, purchase)
                       ┌┴─────────┴┐
                       │Integration │  20%  ← API + DB + Service interactions
                       │ TestContain│       Real dependencies
                       │ ers + Jest │       Moderate speed
                      ┌┴───────────┴┐
                      │  Unit Tests  │  70%  ← Nhiều nhất, nhanh nhất
                      │  pytest/jest │       Isolated functions
                      │  mock deps   │       Milliseconds per test
                      └──────────────┘

  TESTING IN CI/CD PIPELINE
  ═════════════════════════

  git push ──▶ Lint ──▶ Unit ──▶ Integration ──▶ E2E ──▶ Deploy
                 │        │          │              │
                 │        │          │              └── chỉ critical paths
                 │        │          └── TestContainers/real DB
                 │        └── parallel, < 5 phút
                 └── auto-fix formatting

  TEST QUALITY (không chỉ coverage)
  ═════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │  Coverage >= 80% (critical paths)                                │
  │  + Happy path tests                                              │
  │  + Sad path tests (error cases)                                  │
  │  + Edge cases (unicode, empty, large values)                     │
  │  + Mutation testing (đo chất lượng test thật sự)                │
  └──────────────────────────────────────────────────────────────────┘

  PERFORMANCE TESTING
  ═══════════════════
  Load Test ──▶ Tìm capacity      Tools: k6, Locust, JMeter
  Stress Test ──▶ Tìm breaking pt  Budget: p95 < 200ms, p99 < 500ms
  Soak Test ──▶ Tìm memory leaks  Run: trước mỗi major release

  TEST DATA STRATEGY
  ══════════════════
  ❌ Production data (PII risk, GDPR violation)
  ✅ Factory pattern: Faker, FactoryBot
  ✅ Sanitized production data (anonymized)
  ✅ Edge case data: unicode, empty, max-length
```

### VI.1 — Không có automated tests, chỉ manual testing

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Mỗi lần thay đổi code phải test thủ công. Regression bugs xuất hiện liên tục. Test mất nhiều ngày. |
| **Tác động** | Release cycle chậm (tuần -> tháng). Regression bugs nhiều. Confidence khi deploy thấp. Team mệt mỏi. |
| **Best Practice** | **Testing Pyramid**: 70% Unit, 20% Integration, 10% E2E. Automated tests chạy trong CI pipeline. Test coverage target: >= 80% cho critical paths. TDD cho complex business logic. |

### VI.2 — Không có Load/Performance Testing

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không biết hệ thống chịu được bao nhiêu users. Performance issues chỉ phát hiện khi traffic cao. Không có baseline performance. |
| **Tác động** | Hệ thống sập khi có traffic spike (marketing campaign, viral event). Latency tăng, users bỏ đi. Emergency scaling chi phí cao. |
| **Best Practice** | Load test trước mỗi major release. Stress test để tìm breaking point. **k6, Locust, JMeter** cho performance testing. Đặt performance budget: **p95 < 200ms, p99 < 500ms**. |

### VI.3 — Test data dùng production data (có PII) hoặc test data quá đơn giản

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Test database có real user emails, phone numbers. Test không cover edge cases vì data quá clean. Compliance violation risk. |
| **Tác động** | GDPR/CCPA violation — phạt lên đến 4% doanh thu. Data breach từ test environment. Tests không phát hiện bugs thật sự. |
| **Best Practice** | **Factory pattern** cho test data (Faker, FactoryBot). Sanitize production data trước khi dùng cho test. Test data phải cover edge cases: unicode, empty strings, large values. Cleanup test data sau mỗi run. |

### VI.4 — Test Coverage chỉ là con số, không đo lường chất lượng test

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Coverage 90% nhưng vẫn có nhiều bugs. Tests chỉ check happy path. Mutation testing score thấp. |
| **Tác động** | False confidence từ coverage cao. Critical bugs vẫn lọt qua. Tests không catch regressions. |
| **Best Practice** | Focus coverage cho critical business logic. Test cả happy path, sad path, edge cases. **Mutation testing** để đo chất lượng test. Review test quality trong code review. |

---

## VII. ERROR HANDLING

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING — BEST PRACTICES                          │
└─────────────────────────────────────────────────────────────────────────────┘

  ERROR FLOW ARCHITECTURE
  ═══════════════════════

  Exception xảy ra
       │
       ▼
  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
  │  Classify    │────▶│  Handle      │────▶│  Report      │
  │              │     │              │     │              │
  │ Transient?   │     │ Retry?       │     │ Log (struct) │
  │ Client err?  │     │ Fallback?    │     │ Alert?       │
  │ Server err?  │     │ Propagate?   │     │ Sentry/Bug.? │
  └──────────────┘     └──────────────┘     └──────────────┘

  ERROR CLASSIFICATION
  ═══════════════════
  ┌────────────────┬──────────────────┬────────────────────────────┐
  │ Type           │ HTTP Code        │ Action                     │
  ├────────────────┼──────────────────┼────────────────────────────┤
  │ Transient      │ 5xx, timeout     │ RETRY (exponential backoff)│
  │ Client Error   │ 4xx              │ Return error, NO retry     │
  │ Server Error   │ 5xx persistent   │ Log + Alert + Circuit Break│
  │ Unrecoverable  │ Fatal            │ Log + Alert + Graceful stop│
  └────────────────┴──────────────────┴────────────────────────────┘

  ERROR RESPONSE FORMAT (cho API)
  ═══════════════════════════════
  ❌ cho User:  Stack trace, DB schema, internal paths
  ✅ cho User:  { "error": { "code": "PAYMENT_FAILED",
                              "message": "Payment could not be processed",
                              "request_id": "req-abc-123" } }
  ✅ Internal:  Full stack trace + context in structured log

  ANTI-PATTERNS
  ═════════════
  ❌ try { ... } catch { }              ← Silent failure (NEVER!)
  ❌ catch (Exception e) { retry(); }   ← Retry everything (even 400)
  ❌ throw new Exception(e.toString())  ← Lost stack trace
  ✅ catch (TransientError e) { retry(backoff); }
  ✅ catch (ClientError e) { return 4xx with message; }
  ✅ catch (Error e) { log(e, context); alert(); throw; }

  TOOLS
  ═════
  Error Tracking:  Sentry | Bugsnag | Raygun
  Logging:         structlog (Py) | Winston (Node) | slog (Go)
```

### VII.1 — Silent failures — nuốt exception không log/alert

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | `try/catch` với empty catch block. Errors không xuất hiện trong log. Data sai mà không ai biết. |
| **Tác động** | Vấn đề tích tụ theo thời gian. Data corruption không phát hiện. Khi bùng nổ thì đã quá muộn để fix. |
| **Best Practice** | **KHÔNG BAO GIỜ** empty catch block. Log mọi exception với đầy đủ context. Phân loại errors: recoverable vs unrecoverable. Alert cho unrecoverable errors. |

### VII.2 — Error messages lộ thông tin hệ thống (stack trace, DB schema)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | User thấy stack trace khi có lỗi. Error response chứa database table names. Internal service URLs lộ ra ngoài. |
| **Tác động** | Information disclosure cho attackers. User experience tệ. Security vulnerability. |
| **Best Practice** | Generic error message cho user ("Something went wrong"). Chi tiết error chỉ trong internal logs. Structured error response: `{code, message, request_id}`. Error tracking với **Sentry/Bugsnag**. |

### VII.3 — Không phân biệt error types, xử lý tất cả như nhau

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Retry cho mọi loại error (cả 400 Bad Request). Không có error classification. Log level giống nhau cho mọi error. |
| **Tác động** | Retry storm cho non-retryable errors. Resource waste. Khó debug vì không phân loại. |
| **Best Practice** | Phân loại: **Transient** (retry), **Client** (return 4xx), **Server** (log + alert + retry). Custom exception classes cho từng loại. Error codes có hệ thống để lookup. |

---

## VIII. DATABASE & DATA MANAGEMENT

```
┌─────────────────────────────────────────────────────────────────────────────┐
│               DATABASE & DATA MANAGEMENT — BEST PRACTICES                   │
└─────────────────────────────────────────────────────────────────────────────┘

  DATABASE ARCHITECTURE
  ═════════════════════

  Application
      │
      ▼
  ┌──────────────┐     ┌──────────────────────────────────────┐
  │ Connection   │     │         Database Cluster              │
  │ Pool         │     │                                      │
  │              │     │  ┌──────────┐    ┌──────────────┐    │
  │ Size =       │────▶│  │  Primary │───▶│  Replica 1   │    │
  │ (CPU*2)+disk │     │  │  (Write) │    │  (Read)      │    │
  │              │     │  └──────────┘    └──────────────┘    │
  │ + timeout    │     │       │          ┌──────────────┐    │
  │ + idle check │     │       └─────────▶│  Replica 2   │    │
  │ + health chk │     │                  │  (Read)      │    │
  └──────────────┘     │                  └──────────────┘    │
                       └──────────────────────────────────────┘

  QUERY OPTIMIZATION FLOW
  ═══════════════════════

  Slow query detected
       │
       ▼
  EXPLAIN ANALYZE ──▶ Full table scan? ──Yes──▶ ADD INDEX
       │                                          │
       │              N+1 problem? ──Yes──▶ Eager Loading / JOIN
       │                                          │
       │              Connection exhausted? ──▶ Tune Pool Config
       │
       ▼
  Monitor slow query log ──▶ Dashboard ──▶ Alert if p95 > threshold

  INDEX STRATEGY
  ═════════════
  ┌────────────────────────┬──────────────────────────────────────┐
  │ Query Pattern          │ Index Type                           │
  ├────────────────────────┼──────────────────────────────────────┤
  │ WHERE col = ?          │ B-tree index (default)               │
  │ WHERE col1=? AND col2=?│ Composite index (col1, col2)         │
  │ WHERE col LIKE '%text%'│ GIN / Full-text index                │
  │ ORDER BY col           │ B-tree index on col                  │
  │ JSON field queries     │ GIN index on JSONB                   │
  └────────────────────────┴──────────────────────────────────────┘

  MIGRATION STRATEGY (Zero-Downtime)
  ══════════════════════════════════
  1. Backup database         ← LUÔN LUÔN backup trước
  2. Run migration on staging ← Test trước
  3. Apply UP migration       ← Có DOWN script sẵn
  4. Deploy new code          ← Backward compatible
  5. Verify                   ← Health check
  6. Rollback if needed       ← DOWN script

  TOOLS
  ═════
  ORM:       SQLAlchemy (Py) | Prisma (Node) | GORM (Go)
  Migration: Alembic | Flyway | Prisma Migrate
  Monitor:   pg_stat_statements | slow_query_log | Datadog DBM
```

### VIII.1 — Thiếu hoặc sai database indexes

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Query chậm dần khi data lớn. Full table scan trong slow query log. CPU database cao bất thường. |
| **Tác động** | Latency tăng từ ms lên seconds. Database bị overload. User experience suy giảm nghiêm trọng. |
| **Best Practice** | Index cho mọi WHERE, JOIN, ORDER BY column. Composite index cho multi-column queries. **EXPLAIN ANALYZE** để verify query plan. Monitor slow queries thường xuyên. |

### VIII.2 — N+1 Query Problem

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | 1 trang load phát sinh 100+ DB queries. Log thấy lặp lại cùng 1 query nhiều lần. Response time tăng theo số lượng items. |
| **Tác động** | Latency tăng tuyến tính với data. Database bị overload. Page load > 5s. |
| **Best Practice** | **Eager loading / JOIN** thay vì lazy loading. Batch queries (`WHERE id IN (...)`). DataLoader pattern cho GraphQL. ORM: `select_related`/`prefetch_related` (Django), `include` (Rails). |

### VIII.3 — Connection Pool không được config đúng

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | "Too many connections" errors. Connections không được release. New connections mất nhiều thời gian. |
| **Tác động** | Database unreachable. Toàn bộ hệ thống bị ảnh hưởng. Cascading failures. |
| **Best Practice** | Pool size = **(CPU cores * 2) + number_of_disks**. Max pool size phù hợp với DB max_connections. Connection timeout và idle timeout. Health check cho connections. Monitor pool utilization. |

### VIII.4 — Database migration không có rollback, chạy trực tiếp trên prod

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Migration thất bại để database ở trạng thái không nhất quán. Không có cách quay lại. Schema và code không đồng bộ. |
| **Tác động** | Data corruption. Downtime kéo dài. Mất data. |
| **Best Practice** | Mỗi migration có **UP và DOWN** script. Test migration trên staging trước. Blue-Green migration cho zero-downtime. **Backup TRƯỚC khi migrate**. Flyway/Alembic/Prisma Migrate. |

---

## IX. API DESIGN

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      API DESIGN — BEST PRACTICES                            │
└─────────────────────────────────────────────────────────────────────────────┘

  API ARCHITECTURE
  ════════════════

  Client
    │
    ▼
  ┌──────────────────────────────────────────────────────────────────────┐
  │                        API GATEWAY                                   │
  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌────────────┐  │
  │  │  Auth   │ │  Rate   │ │ Version │ │ Request  │ │   Load     │  │
  │  │  Check  │ │  Limit  │ │ Route   │ │ Validate │ │  Balance   │  │
  │  └─────────┘ └─────────┘ └─────────┘ └──────────┘ └────────────┘  │
  └────────────────────────────┬─────────────────────────────────────────┘
                               │
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
        ┌──────────┐    ┌──────────┐    ┌──────────┐
        │ /v1/users│    │/v1/orders│    │ /v1/pay  │
        │ Service  │    │ Service  │    │ Service  │
        └──────────┘    └──────────┘    └──────────┘

  API DESIGN CHECKLIST
  ════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ ✅ Versioning    : /v1/resource (URL) or Accept header          │
  │ ✅ Pagination    : Cursor-based, default 20-50, max 200         │
  │ ✅ Rate Limiting : Per client/API key, 429 + Retry-After        │
  │ ✅ Idempotency   : Idempotency-Key header for POST/PUT          │
  │ ✅ Health Check  : GET /health (liveness) + GET /ready (readiness)│
  │ ✅ Error Format  : { code, message, request_id }                │
  │ ✅ Documentation : OpenAPI/Swagger, auto-generated              │
  └──────────────────────────────────────────────────────────────────┘

  PAGINATION PATTERNS
  ═══════════════════
  ❌ Offset-based: /users?page=500&limit=20  ← Chậm khi data lớn
  ✅ Cursor-based: /users?after=abc123&limit=20  ← O(1) performance

  Response: { "data": [...], "next_cursor": "xyz789", "has_more": true }

  RATE LIMITING TIERS
  ═══════════════════
  ┌────────────────┬──────────────┬──────────────────────────┐
  │ Tier           │ Limit        │ Response                 │
  ├────────────────┼──────────────┼──────────────────────────┤
  │ Free           │ 100/min      │ 429 + Retry-After: 60    │
  │ Pro            │ 1000/min     │ 429 + Retry-After: 10    │
  │ Enterprise     │ 10000/min    │ 429 + Retry-After: 5     │
  └────────────────┴──────────────┴──────────────────────────┘
  Algorithm: Token bucket hoặc Sliding window

  IDEMPOTENCY FLOW
  ════════════════
  Client ──▶ POST /orders  (Idempotency-Key: uuid-123)
                │
                ▼
  Server: Key exists? ──Yes──▶ Return cached result (no re-process)
                │
               No
                │
                ▼
         Process ──▶ Store result with key ──▶ Return result
         TTL: 24h for idempotency keys

  HEALTH CHECK ENDPOINTS
  ═════════════════════
  GET /health  (liveness)  ──▶ 200 {"status":"ok"}
  GET /ready   (readiness) ──▶ 200 {"status":"ok","db":"up","cache":"up"}
                           ──▶ 503 {"status":"degraded","db":"down"}
```

### IX.1 — Không có API versioning

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Thay đổi API làm client bị lỗi. Không thể update API mà không break backward compatibility. Client và server phải deploy cùng lúc. |
| **Tác động** | Breaking changes ảnh hưởng mọi client. Không thể evolve API. Tight coupling giữa client và server. |
| **Best Practice** | **URL versioning** (`/v1/`, `/v2/`) hoặc Header versioning. Deprecation policy rõ ràng (3-6 tháng). Backward compatibility mặc định. API changelog cho mỗi version. |

### IX.2 — Không có Pagination, trả về tất cả data

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | API trả về 10,000+ records trong 1 response. Response size rất lớn (MB). Client bị chậm/crash khi load. |
| **Tác động** | Memory overflow ở cả server và client. Network bandwidth lãng phí. Terrible UX. |
| **Best Practice** | **Cursor-based pagination** cho large datasets. Default page size: 20-50. Max page size: 100-200. Include total count và next cursor trong response. |

### IX.3 — Không có Rate Limiting

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | 1 client có thể gửi vô hạn requests. Không có bảo vệ trước API abuse. Resource không được chia sẻ công bằng. |
| **Tác động** | DDoS dễ dàng. 1 client có thể làm sập hệ thống. Chi phí infra tăng không kiểm soát. |
| **Best Practice** | Rate limiting per client/API key. Multiple tiers: 100/min cho free, 1000/min cho paid. Return **429 Too Many Requests** với Retry-After header. Token bucket hoặc sliding window algorithm. |

### IX.4 — Không có Idempotency cho write APIs

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Retry request tạo duplicate records. User click nút 2 lần tạo 2 orders. Payment bị charge 2 lần khi network timeout. |
| **Tác động** | Duplicate transactions. Data inconsistency. Financial loss. User trust giảm. |
| **Best Practice** | **Idempotency Key** trong request header cho POST/PUT. Server check key trước khi xử lý — nếu đã xử lý thì trả về kết quả cũ. TTL cho idempotency keys (24h). Database unique constraints cho business logic dedup. |

### IX.5 — Không có Health Check Endpoints chuẩn

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Load balancer không biết service nào healthy. K8s probe chỉ check port open, không check dependencies. Không phân biệt liveness vs readiness. |
| **Tác động** | Traffic route đến unhealthy instances. Cascading failures khi dependency down. False positive health status. |
| **Best Practice** | **GET /health** (liveness): simple check, app is alive. **GET /ready** (readiness): check DB, cache, critical dependencies. Return `200` hoặc `503` với chi tiết từng dependency. Không include sensitive info trong response. |

---

## X. CONFIGURATION & SECRETS MANAGEMENT

```
┌─────────────────────────────────────────────────────────────────────────────┐
│            CONFIGURATION & SECRETS MANAGEMENT — BEST PRACTICES              │
└─────────────────────────────────────────────────────────────────────────────┘

  CONFIG & SECRETS FLOW
  ═════════════════════

  ┌──────────────────────────────────────────────────────────────────────┐
  │                    CONFIG SOURCES (priority order)                    │
  │                                                                      │
  │  1. CLI args          ← Highest priority (override everything)       │
  │  2. ENV variables     ← Runtime injection (K8s, Docker)             │
  │  3. Config service    ← Centralized (Consul, AWS Parameter Store)   │
  │  4. Config file       ← Default values (YAML/TOML)                  │
  │  5. Code defaults     ← Lowest priority (fallback)                   │
  └──────────────────────────────────────────────────────────────────────┘

  SECRETS LIFECYCLE
  ═════════════════

  Create ──▶ Store ──▶ Distribute ──▶ Rotate ──▶ Revoke
    │          │           │            │          │
    ▼          ▼           ▼            ▼          ▼
  Generate  Vault/SM   Runtime      Auto 30-90d  Immediate
  strong    encrypted  ENV inject   no redeploy  on breach
  random    audit log  never file

  ❌ .env in Git | ❌ Hardcode | ❌ Same secret dev/prod | ❌ No rotation
  ✅ Vault/AWS SM | ✅ Runtime inject | ✅ Auto rotate | ✅ Audit log

  CONFIG VALIDATION (Fail Fast)
  ═════════════════════════════

  App starts ──▶ Load config ──▶ Validate schema ──▶ Check connectivity
                                      │                     │
                                   Invalid?              Failed?
                                      │                     │
                                      ▼                     ▼
                                 FAIL FAST            FAIL FAST
                                 (don't start)        (don't start)

  TOOLS
  ═════
  Secrets:   HashiCorp Vault | AWS Secrets Manager | GCP Secret Manager
  Config:    Consul | AWS Parameter Store | etcd
  Detection: gitleaks (pre-commit) | trufflehog | detect-secrets
  Validation: pydantic (Py) | joi (Node) | viper (Go)
```

### X.1 — Configuration Drift — config khác nhau giữa environments mà không ai biết

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | "It works on my machine" là câu nói thường xuyên. Staging pass nhưng Prod fail. Không có single source of truth cho config. |
| **Tác động** | Bugs chỉ xuất hiện trên Production. Debug mất thời gian. Confidence thấp. |
| **Best Practice** | **Infrastructure as Code** (Terraform). Config tách khỏi code, inject qua env vars. Centralized config service (Consul, AWS Parameter Store). Config validation khi startup. |

### X.2 — Secrets trong config files, .env files commit lên Git

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | .env file trong Git repo. Secrets giống nhau giữa environments. Không có secrets rotation. |
| **Tác động** | Secrets bị leak. Không thể revoke mà không deploy lại. Compliance violation. |
| **Best Practice** | **Secrets Manager** (Vault, AWS SM). .env **KHÔNG BAO GIỜ** commit lên Git (.gitignore). Secrets rotation tự động (30-90 ngày). Audit log cho mỗi lần access secrets. |

### X.3 — Không có config validation, ứng dụng start với config sai

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Ứng dụng start thành công nhưng fail khi xử lý request đầu tiên. Config sai chỉ bị phát hiện khi có traffic. |
| **Tác động** | Delayed failure — khó debug. Partial failures. Downtime sau deploy. |
| **Best Practice** | Validate tất cả config khi startup (**fail fast**). Schema validation cho config files. Health check endpoint kiểm tra config. Config changes thông qua CI/CD, không manual. |

---

## XI. CODE QUALITY & MAINTAINABILITY

```
┌─────────────────────────────────────────────────────────────────────────────┐
│              CODE QUALITY & MAINTAINABILITY — BEST PRACTICES                │
└─────────────────────────────────────────────────────────────────────────────┘

  CODE QUALITY GATES IN CI PIPELINE
  ═════════════════════════════════

  git push ──▶ ┌─────────────────────────────────────────────────────┐
               │  QUALITY GATES (auto-enforce)                       │
               │                                                     │
               │  Gate 1: LINTER + FORMATTER                        │
               │          ESLint/Ruff + Prettier/Black               │
               │          Auto-fix trên commit (pre-commit hooks)    │
               │                                                     │
               │  Gate 2: STATIC ANALYSIS                            │
               │          SonarQube: complexity, duplication, smells  │
               │          Max cyclomatic complexity: 20/function      │
               │          Max function length: 50 lines               │
               │                                                     │
               │  Gate 3: CODE REVIEW                                │
               │          >= 1 reviewer | PR < 400 lines             │
               │          Checklist: func, perf, security, tests     │
               │                                                     │
               │  Gate 4: TEST COVERAGE                              │
               │          >= 80% critical paths                      │
               │          Mutation score check                        │
               └─────────────────────────────────────────────────────┘

  TECHNICAL DEBT MANAGEMENT
  ═════════════════════════

  ┌────────────────────────────────────────────────────────────┐
  │              Technical Debt Quadrant                        │
  │                                                            │
  │  Deliberate ─────────────────────────── Inadvertent        │
  │       │                                      │             │
  │  Reckless: "No time for      Reckless: "What's             │
  │   design, just ship"          a design pattern?"            │
  │       │                                      │             │
  │  Prudent: "Ship now,         Prudent: "Now we              │
  │   deal with consequences"     know better"                 │
  │                                                            │
  └────────────────────────────────────────────────────────────┘

  Strategy: 15-20% sprint capacity cho tech debt
  Track:    Backlog items + priority
  Measure:  Code churn | Defect density | Lead time
  Review:   Refactor incrementally, NO big-bang rewrite

  CODE QUALITY METRICS
  ════════════════════
  ┌────────────────────────┬─────────────┬──────────────────────┐
  │ Metric                 │ Target      │ Tool                 │
  ├────────────────────────┼─────────────┼──────────────────────┤
  │ Cyclomatic Complexity  │ < 20/func   │ SonarQube, radon     │
  │ Code Coverage          │ >= 80%      │ pytest-cov, istanbul │
  │ Code Duplication       │ < 5%        │ SonarQube, jscpd     │
  │ Maintainability Index  │ > 65        │ SonarQube             │
  │ PR Size                │ < 400 lines │ GitHub settings       │
  └────────────────────────┴─────────────┴──────────────────────┘
```

### XI.1 — Cyclomatic Complexity cao, function quá dài (>50 dòng)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Functions có nhiều nested if/else. Khó đọc, khó hiểu code. Bug fix tạo ra bugs mới. |
| **Tác động** | Maintenance cost tăng theo thời gian. Onboarding developer mới mất nhiều thời gian. Technical debt tích tụ. |
| **Best Practice** | Max **20 cyclomatic complexity** per function. Single Responsibility Principle. Extract method refactoring. **SonarQube** để đo lường và enforce. |

### XI.2 — Không có Code Review process

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Code được merge trực tiếp vào main. Bugs chỉ được phát hiện bởi users. Không có knowledge sharing. |
| **Tác động** | Bug rate cao. Code quality giảm. Team không học từ nhau. Security vulnerabilities lọt qua. |
| **Best Practice** | Bắt buộc **Pull Request** với >= 1 reviewer. Code review checklist: functionality, readability, performance, security, tests. Automated checks (linter, formatter) trước review. Small PRs (< 400 lines). |

### XI.3 — Technical Debt không được track và giải quyết

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | TODO comments khắp nơi trong code. Workarounds chồng chéo. Refactoring bị delay liên tục. |
| **Tác động** | Velocity giảm 20-40% mỗi năm. Developer frustration tăng. Innovation bị cản trở. |
| **Best Practice** | Track tech debt trong backlog với priority. Dành **15-20% sprint capacity** cho tech debt. Refactor incrementally, không big-bang rewrite. Measure: code churn, defect density, lead time. |

---

## XII. INFRASTRUCTURE & CONTAINERIZATION

```
┌─────────────────────────────────────────────────────────────────────────────┐
│            INFRASTRUCTURE & CONTAINERIZATION — BEST PRACTICES               │
└─────────────────────────────────────────────────────────────────────────────┘

  KUBERNETES PRODUCTION ARCHITECTURE
  ══════════════════════════════════

  ┌──────────────────────────────────────────────────────────────────────┐
  │                     Kubernetes Cluster                               │
  │                                                                      │
  │  ┌─────────────── Namespace: production ──────────────────────┐     │
  │  │                                                             │     │
  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │     │
  │  │  │ Pod A   │  │ Pod A   │  │ Pod B   │  │ Pod B   │      │     │
  │  │  │ replica1│  │ replica2│  │ replica1│  │ replica2│      │     │
  │  │  │         │  │         │  │         │  │         │      │     │
  │  │  │ CPU: req│  │ CPU: req│  │ CPU: req│  │ CPU: req│      │     │
  │  │  │  /limit │  │  /limit │  │  /limit │  │  /limit │      │     │
  │  │  │ Mem: req│  │ Mem: req│  │ Mem: req│  │ Mem: req│      │     │
  │  │  │  /limit │  │  /limit │  │  /limit │  │  /limit │      │     │
  │  │  │         │  │         │  │         │  │         │      │     │
  │  │  │ Probes: │  │ Probes: │  │ Probes: │  │ Probes: │      │     │
  │  │  │ liveness│  │ liveness│  │ liveness│  │ liveness│      │     │
  │  │  │ ready   │  │ ready   │  │ ready   │  │ ready   │      │     │
  │  │  │ startup │  │ startup │  │ startup │  │ startup │      │     │
  │  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘      │     │
  │  │                                                             │     │
  │  │  ResourceQuota: CPU 8 cores, Mem 16Gi                      │     │
  │  │  LimitRange: min 100m CPU, max 2 CPU per container         │     │
  │  └─────────────────────────────────────────────────────────────┘     │
  └──────────────────────────────────────────────────────────────────────┘

  DOCKER IMAGE OPTIMIZATION
  ═════════════════════════

  ❌ Bad:  FROM ubuntu:22.04        ← 77MB base, + deps = 1GB+
           RUN apt install python3
           COPY . .

  ✅ Good: Multi-stage build
  ┌─────────────────────┐     ┌──────────────────────┐
  │ Stage 1: Builder    │     │ Stage 2: Production   │
  │ Full SDK + deps     │────▶│ Distroless/Alpine     │
  │ Build artifacts     │     │ Only runtime + binary │
  │ ~ 1GB               │     │ < 100MB target        │
  └─────────────────────┘     └──────────────────────┘

  CONTAINER SECURITY
  ══════════════════
  ✅ USER non-root in Dockerfile
  ✅ Read-only filesystem (readOnlyRootFilesystem: true)
  ✅ No privilege escalation (allowPrivilegeEscalation: false)
  ✅ Image scanning: Trivy / Snyk Container
  ✅ .dockerignore: .git, node_modules, .env, secrets

  K8S PROBES
  ══════════
  ┌──────────────┬───────────────────────────────────────────────────┐
  │ Probe        │ Purpose                                           │
  ├──────────────┼───────────────────────────────────────────────────┤
  │ Startup      │ App finished booting? (slow apps)                │
  │ Liveness     │ App alive? (simple, fast, no deps check)         │
  │ Readiness    │ App ready for traffic? (check DB, cache, deps)   │
  └──────────────┴───────────────────────────────────────────────────┘
  Tune: initialDelaySeconds, periodSeconds, failureThreshold

  TOOLS
  ═════
  Container: Docker | Podman | BuildKit
  Orchestration: Kubernetes | ECS | Nomad
  IaC: Terraform | Pulumi | AWS CDK
  Scanning: Trivy | Snyk | Grype
  GitOps: ArgoCD | Flux
```

### XII.1 — Docker images quá lớn, không optimize

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Image size > 1GB. Build time > 10 phút. Pull image mất nhiều thời gian. |
| **Tác động** | Deploy chậm. Storage cost cao. Scale-out chậm vì pull image lâu. |
| **Best Practice** | **Multi-stage builds**. Alpine/Distroless base images. `.dockerignore` file. Layer caching: dependencies trước, code sau. Target image **< 100MB**. |

### XII.2 — Container chạy với root privileges

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Dockerfile không có USER instruction. Container có thể access host filesystem. Security scan báo privilege escalation risk. |
| **Tác động** | Container escape attack. Host compromise. Data breach toàn hệ thống. |
| **Best Practice** | **USER non-root** trong Dockerfile. Read-only filesystem khi có thể. Security context trong K8s: `runAsNonRoot`, `readOnlyRootFilesystem`. Image scanning với **Trivy/Snyk**. |

### XII.3 — Kubernetes không có Resource Limits

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | 1 pod chiếm hết resources của node. Noisy neighbor problem. OOMKilled events thường xuyên. |
| **Tác động** | Không có resource isolation. Các pod khác bị ảnh hưởng. Node instability. |
| **Best Practice** | **Requests và Limits** cho mọi container. Requests = typical usage, Limits = max allowed. LimitRange và ResourceQuota cho namespace. Monitor resource utilization để tune. |

### XII.4 — Liveness/Readiness Probe không đúng hoặc thiếu

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Pod bị kill liên tục (CrashLoopBackOff). Traffic được route đến pod chưa sẵn sàng. Health check không phản ánh trạng thái thật. |
| **Tác động** | Service disruption. Request failures. Pod restart loops. |
| **Best Practice** | **Readiness probe**: kiểm tra app sẵn sàng nhận traffic (check DB, cache). **Liveness probe**: kiểm tra app còn sống (đơn giản, nhanh). **Startup probe** cho apps khởi động chậm. Tune timing: `initialDelaySeconds`, `periodSeconds`. |

---

## XIII. DOCUMENTATION & RUNBOOKS

```
┌─────────────────────────────────────────────────────────────────────────────┐
│               DOCUMENTATION & RUNBOOKS — BEST PRACTICES                     │
└─────────────────────────────────────────────────────────────────────────────┘

  DOCUMENTATION ECOSYSTEM
  ═══════════════════════

  ┌─────────────────────────────────────────────────────────────────────────┐
  │                       Documentation Layers                              │
  │                                                                         │
  │  ┌─────────────────┐                                                   │
  │  │ Architecture    │  ADRs, C4 diagrams, system context                │
  │  │ Decision Records│  → Stored in docs/adr/ (version controlled)       │
  │  └────────┬────────┘                                                   │
  │           │                                                             │
  │  ┌────────▼────────┐                                                   │
  │  │ API Docs        │  OpenAPI/Swagger, auto-generated from code        │
  │  │ (Auto-generated)│  → Always up-to-date, examples for every endpoint │
  │  └────────┬────────┘                                                   │
  │           │                                                             │
  │  ┌────────▼────────┐                                                   │
  │  │ Runbooks /      │  Per-alert playbooks: symptom → diagnosis → fix   │
  │  │ Playbooks       │  → Linked from every alert notification           │
  │  └────────┬────────┘                                                   │
  │           │                                                             │
  │  ┌────────▼────────┐                                                   │
  │  │ README /        │  Getting started, local setup, contribution guide │
  │  │ Onboarding      │  → Target: new member productive in < 1 week     │
  │  └─────────────────┘                                                   │
  └─────────────────────────────────────────────────────────────────────────┘

  RUNBOOK TEMPLATE
  ════════════════
  ┌──────────────────────────────────────────────────────────────────────┐
  │  ## Alert: [Alert Name]                                             │
  │  **Severity:** P1/P2/P3                                             │
  │  **Service:** [service-name]                                        │
  │                                                                      │
  │  ### Symptoms                                                        │
  │  - Error rate > X% for Y minutes                                    │
  │                                                                      │
  │  ### Diagnosis Steps                                                 │
  │  1. Check dashboard: [link]                                         │
  │  2. Check logs: `kubectl logs -l app=X --tail=100`                 │
  │  3. Check dependencies: [health endpoints]                          │
  │                                                                      │
  │  ### Resolution                                                      │
  │  - If [condition A]: [action A]                                     │
  │  - If [condition B]: [action B]                                     │
  │                                                                      │
  │  ### Escalation                                                      │
  │  - After 15 min: ping @team-lead                                    │
  │  - After 30 min: escalate to @on-call-manager                      │
  └──────────────────────────────────────────────────────────────────────┘

  DOCS-AS-CODE PRINCIPLE
  ═════════════════════
  Documentation sống CÙNG source code
  Review docs TRONG PR process
  Auto-generate API docs TỪ code
  Update sau MỖI incident (runbooks)
```

### XIII.1 — Documentation outdated hoặc không tồn tại

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | README viết từ 2 năm trước. Architecture diagram không khớp với thực tế. New member mất 2-3 tuần để onboard. |
| **Tác động** | Onboarding chậm. Knowledge silo. Sai lầm khi không hiểu hệ thống. |
| **Best Practice** | **Docs-as-Code**: documentation sống cùng source code. **Architecture Decision Records** (ADRs). Auto-generate API docs từ code (Swagger/OpenAPI). Review docs trong PR process. |

### XIII.2 — Không có Runbooks/Playbooks cho incident response

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Mỗi khi có sự cố, team làm từng bước từ đầu. On-call không biết bắt đầu từ đâu. Resolution time phụ thuộc vào ai đang trực. |
| **Tác động** | MTTR cao và không ổn định. Stress cho on-call. Có thể làm vấn đề tồi tệ hơn do panic. |
| **Best Practice** | Runbook cho mỗi alert: symptom, diagnosis steps, resolution steps, escalation path. Template chuẩn cho mỗi runbook. Review và update sau mỗi incident. Liên kết runbook với alert. |

### XIII.3 — API documentation thiếu hoặc không chính xác

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Client developers phải đọc source code để hiểu API. API behavior khác với document. Không có examples. |
| **Tác động** | Integration chậm và nhiều lỗi. Developer experience tệ. Client team frustrated. |
| **Best Practice** | **OpenAPI/Swagger** specification. Auto-generate từ code annotations. Examples cho mỗi endpoint. Changelog cho mỗi thay đổi. Postman collection cho testing. |

---

## XIV. PRODUCTION READINESS

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                PRODUCTION READINESS — BEST PRACTICES                        │
└─────────────────────────────────────────────────────────────────────────────┘

  PRODUCTION READINESS REVIEW (PRR) — GO/NO-GO GATE
  ══════════════════════════════════════════════════

  Feature complete ──▶ PRR Checklist ──▶ Sign-off ──▶ Go Live
                            │
                            ▼
  ┌──────────────────────────────────────────────────────────────────────┐
  │  PRR CHECKLIST (bắt buộc pass TẤT CẢ trước go-live)               │
  │                                                                      │
  │  □ Monitoring & Alerting                                             │
  │    - Dashboard cho 4 Golden Signals (Latency, Traffic, Errors, Sat) │
  │    - Alerts configured với runbook links                             │
  │                                                                      │
  │  □ SLO/SLI/SLA Defined                                              │
  │    - SLIs: latency p99, error rate, availability                    │
  │    - SLOs: 99.9% availability, p99 < 200ms                         │
  │    - Error budget calculated                                         │
  │                                                                      │
  │  □ On-Call & Incident Process                                        │
  │    - On-call rotation set up (PagerDuty/Opsgenie)                   │
  │    - Incident Commander role defined                                 │
  │    - Communication channels ready (#incident)                        │
  │                                                                      │
  │  □ Capacity & Performance                                            │
  │    - Load tested to 3x expected peak                                │
  │    - Auto-scaling configured                                         │
  │    - Performance budgets defined                                     │
  │                                                                      │
  │  □ Security Review                                                   │
  │    - Secrets in Vault/SM (not in code)                              │
  │    - Auth/AuthZ verified                                             │
  │    - Security scan passed                                            │
  │                                                                      │
  │  □ Deployment & Rollback                                             │
  │    - Canary/Blue-Green strategy configured                          │
  │    - Rollback tested                                                 │
  │    - Feature flags for new features                                  │
  │                                                                      │
  │  □ Backup & DR                                                       │
  │    - Backup verified (restore tested)                                │
  │    - RTO/RPO documented                                              │
  │                                                                      │
  │  □ Documentation                                                     │
  │    - Runbooks written for all alerts                                 │
  │    - API docs up-to-date                                             │
  │    - Architecture diagram current                                    │
  └──────────────────────────────────────────────────────────────────────┘

  SLO / SLI / SLA / ERROR BUDGET
  ═══════════════════════════════

  SLI (chỉ số)        SLO (mục tiêu)         SLA (cam kết)
  ────────────        ──────────────          ─────────────
  Latency p99         < 200ms                 < 500ms + penalty
  Error rate          < 0.1%                  < 1% + SLA credit
  Availability        99.95%                  99.9% + refund

  Error Budget = 100% - SLO
  ─────────────────────────
  SLO 99.9% ──▶ Error budget = 0.1% ──▶ ~43 min downtime/month
  Budget còn ──▶ Ship features nhanh
  Budget hết ──▶ STOP shipping, fix reliability

  INCIDENT MANAGEMENT PROCESS
  ═══════════════════════════
  Detect ──▶ Alert ──▶ Triage ──▶ Incident Commander
                                       │
                           ┌───────────┼───────────┐
                           ▼           ▼           ▼
                      Communicate   Diagnose    Mitigate
                      (Slack #inc)  (Traces,    (Rollback,
                                    Logs)       Restart)
                                       │
                                       ▼
                              Resolve ──▶ Post-mortem (blameless)
                                              │
                                              ▼
                                         Action items tracked
```

### XIV.1 — Không có Production Readiness Review (PRR) trước khi go live

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Launch rồi mới phát hiện thiếu monitoring. Không có on-call schedule. Không có SLO/SLA. |
| **Tác động** | Incident đầu tiên không được xử lý tốt. User experience tệ ngay từ đầu. Reputation damage sớm. |
| **Best Practice** | **PRR Checklist** bắt buộc trước go-live. Cover: monitoring, alerting, on-call, runbooks, capacity, security, backup, rollback. Sign-off từ Engineering Lead và SRE. |

### XIV.2 — SLO/SLI/SLA không được định nghĩa hoặc không realistic

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không biết "hệ thống ổn định" nghĩa là gì. Không có ngưỡng để quyết định rollback. Không có error budget. |
| **Tác động** | Không có cơ sở để đánh giá chất lượng. Không cân bằng được reliability vs velocity. Arguments giữa dev và ops. |
| **Best Practice** | Định nghĩa **SLIs** (chỉ số đo): latency p99, error rate, availability. Đặt **SLOs** (mục tiêu): 99.9% availability, p99 < 200ms. **SLA** = SLO + hậu quả (thưởng/phạt). **Error budget** = 100% - SLO để quyết định invest vào reliability hay features. |

### XIV.3 — Không có Incident Management Process

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Khi có sự cố, không ai biết ai chịu trách nhiệm. Communication hỗn loạn. Post-mortem không được làm. |
| **Tác động** | Resolution chậm. Lỗi lặp lại. Team stress và demotivated. |
| **Best Practice** | **Incident Commander** role rõ ràng. Communication channel (Slack #incident). Severity levels (P1-P4) với response time. **Blameless post-mortem** sau mỗi P1/P2. Action items được track đến khi hoàn thành. |

---

## XV. MLOps & AI SYSTEMS

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                   MLOps & AI SYSTEMS — BEST PRACTICES                       │
└─────────────────────────────────────────────────────────────────────────────┘

  ML PRODUCTION PIPELINE
  ═════════════════════

  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐
  │  Data    │──▶│ Training │──▶│ Registry │──▶│ Serving  │──▶│ Monitor  │
  │ Pipeline │   │ Pipeline │   │ (Version)│   │ (Infer.) │   │ (Drift)  │
  └──────────┘   └──────────┘   └──────────┘   └──────────┘   └──────────┘
       │              │              │              │              │
       ▼              ▼              ▼              ▼              ▼
  Validate data  Track experi-  Model+Data+   Optimize:      Data drift
  Quality check  ments, hyper-  Hyperparams   Quantization   Concept drift
  Schema check   params, metrics versioned    Batching       A/B test
                                              Caching        Retraining

  MODEL DEPLOYMENT STRATEGY
  ═════════════════════════

  New Model Ready
       │
       ▼
  Shadow Deploy ──▶ Compare with production model (no user impact)
       │
       ▼
  Canary Deploy ──▶ 1-5% traffic ──▶ Monitor metrics
       │
       ▼
  Full Rollout ──▶ Monitor for drift ──▶ Auto-retrain if threshold hit

  MODEL VERSIONING (Lineage Tracking)
  ═══════════════════════════════════

  ┌──────────────────────────────────────────────────────────────────────┐
  │  Data v1.2 ──▶ Training run #47 ──▶ Model v3.1 ──▶ Deployed prod  │
  │  │                │                    │                             │
  │  │ dataset hash   │ hyperparams        │ metrics                    │
  │  │ feature schema │ training time      │ accuracy: 94.2%            │
  │  │ size: 50K rows │ GPU: A100 x 2      │ latency p99: 45ms         │
  └──────────────────────────────────────────────────────────────────────┘

  MONITORING FOR AI SYSTEMS
  ═════════════════════════
  ┌────────────────────┬────────────────────────────────────────────────┐
  │ Monitor            │ How                                            │
  ├────────────────────┼────────────────────────────────────────────────┤
  │ Data Drift         │ PSI (Population Stability Index), KS-test     │
  │ Concept Drift      │ Accuracy tracking over time windows           │
  │ Model Latency      │ p50/p95/p99, tokens/sec for LLMs             │
  │ Model Cost         │ $/request, tokens/request for LLM APIs       │
  │ Output Quality     │ Human eval sampling, automated scoring        │
  └────────────────────┴────────────────────────────────────────────────┘

  TOOLS
  ═════
  Registry:    MLflow | Weights & Biases | Neptune
  Serving:     TensorRT | ONNX Runtime | vLLM | TGI
  LLM Tracing: Langfuse | LangSmith | Helicone
  Monitoring:  Evidently AI | WhyLabs | NannyML
```

### XV.1 — Không monitor Model Drift — model suy giảm theo thời gian

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Model accuracy giảm dần mà không ai biết. Predictions ngày càng sai. User feedback xấu đi. |
| **Tác động** | Business decisions dựa trên dự đoán sai. Revenue giảm. User mất niềm tin vào AI features. |
| **Best Practice** | Monitor **data drift** (PSI, KS-test) và **concept drift**. A/B testing cho model updates. Shadow deployment trước khi thay thế. Retraining pipeline tự động khi drift vượt threshold. |

### XV.2 — Model Serving không có optimization, latency cao

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Inference time > 1s. GPU utilization thấp. Không có batching. |
| **Tác động** | User experience tệ. Cost/request cao. Không scale được. |
| **Best Practice** | Model optimization: **quantization, pruning, distillation**. Batching inference requests. Caching cho frequent predictions. GPU sharing/Multi-model serving. **TensorRT/ONNX Runtime**. |

### XV.3 — Model Versioning hỗn loạn, không biết model nào đang chạy trên prod

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không biết model được train từ data nào. Không thể reproduce results. Rollback model là nightmare. |
| **Tác động** | Compliance risk (không explain được model). Không thể debug model issues. Không thể rollback. |
| **Best Practice** | **Model Registry** (MLflow, Weights & Biases). Version: model + data + hyperparams + metrics. Staged deployment: staging > canary > production. **Lineage tracking**: data -> training -> model -> deployment. |

---

## XVI. CACHING & PERFORMANCE OPTIMIZATION

```
┌─────────────────────────────────────────────────────────────────────────────┐
│           CACHING & PERFORMANCE OPTIMIZATION — BEST PRACTICES               │
└─────────────────────────────────────────────────────────────────────────────┘

  MULTI-LAYER CACHING ARCHITECTURE
  ════════════════════════════════

  Client Request
       │
       ▼
  ┌──────────────┐  HIT   Return immediately (< 5ms)
  │ Layer 1: CDN │──────▶ Static assets: images, CSS, JS, fonts
  │ (CloudFlare) │        Cache-Control: public, max-age=31536000
  └──────┬───────┘
         │ MISS
         ▼
  ┌──────────────┐  HIT   Return from memory (< 1ms)
  │ Layer 2: App │──────▶ Hot data: user sessions, config, frequently read
  │ Cache (Redis)│        TTL: 5-15 min (hot) / 1-24h (static)
  └──────┬───────┘
         │ MISS
         ▼
  ┌──────────────┐  HIT   Return from DB cache
  │ Layer 3: DB  │──────▶ Query results, materialized views
  │ Query Cache  │
  └──────┬───────┘
         │ MISS
         ▼
  ┌──────────────┐
  │   Database   │  Fetch from disk, populate cache layers
  └──────────────┘

  CACHE INVALIDATION STRATEGIES
  ═════════════════════════════
  ┌─────────────────┬──────────────────────────────────────────────────┐
  │ Strategy        │ Khi nào dùng                                     │
  ├─────────────────┼──────────────────────────────────────────────────┤
  │ TTL             │ Data ít thay đổi, acceptable stale window        │
  │ Write-Through   │ Critical data (price, inventory). Update cache   │
  │                 │ đồng thời khi write DB.                          │
  │ Event-driven    │ Publish event khi data change ──▶ invalidate key │
  │ Cache-Aside     │ App check cache, miss ──▶ fetch DB ──▶ set cache │
  └─────────────────┴──────────────────────────────────────────────────┘

  PERFORMANCE OPTIMIZATION
  ════════════════════════
  Frontend:  Lazy loading | Code splitting | Image WebP/AVIF | Bundle < 200KB
  Backend:   Connection pooling | Async I/O | Batch operations | N+1 fix
  DB:        Indexes | Read replicas | Query optimization | Partitioning
  Network:   CDN | Compression (gzip/brotli) | HTTP/2 | Keep-alive

  KPIs
  ════
  Cache hit ratio: target > 80%
  TTFB: < 200ms
  LCP (Largest Contentful Paint): < 2.5s
  Bundle size: < 200KB (gzipped)
```

### XVI.1 — Không có Caching Strategy, mọi request đều hit database

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Mọi API call đều query database. Response time cao dù data ít thay đổi. Database CPU luôn cao. Cùng 1 query bị gọi hàng nghìn lần/phút. |
| **Tác động** | Latency cao không cần thiết. Database overload. Chi phí infra tăng. User experience tệ. |
| **Best Practice** | **Multi-layer caching**: CDN (static assets) > Application cache (Redis/Memcached) > Database query cache. Cache-aside pattern cho read-heavy data. TTL phù hợp: hot data 5-15 phút, static data 1-24 giờ. **Cache invalidation strategy** rõ ràng (TTL, event-based, write-through). |

### XVI.2 — Cache Invalidation không đúng, data cũ (stale data)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | User thấy data cũ sau khi update. Admin thay đổi config nhưng không có effect. Price/inventory sai do cache. |
| **Tác động** | Data inconsistency. Business logic sai. User confusion và complaints. Có thể gây financial loss (wrong price). |
| **Best Practice** | **Write-through cache** cho critical data (update cache khi write DB). **Event-driven invalidation** (publish event khi data change). Cache tags/keys có version. Monitoring cache hit ratio (target > 80%). Graceful fallback khi cache miss. |

### XVI.3 — Không dùng CDN cho static assets, mọi thứ serve từ origin

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Images, CSS, JS load chậm cho users xa server. Origin server bandwidth cao. TTFB (Time to First Byte) > 500ms cho static files. |
| **Tác động** | Page load chậm = user bounce rate tăng. Bandwidth cost cao. Origin server overload cho static content. |
| **Best Practice** | **CDN** (CloudFlare, CloudFront, Fastly) cho static assets. Cache headers: `Cache-Control: public, max-age=31536000, immutable` cho hashed assets. Image optimization: WebP/AVIF format, responsive sizes. Lazy loading cho below-the-fold images. |

---

## XVII. ASYNC PROCESSING & MESSAGE QUEUES

```
┌─────────────────────────────────────────────────────────────────────────────┐
│           ASYNC PROCESSING & MESSAGE QUEUES — BEST PRACTICES                │
└─────────────────────────────────────────────────────────────────────────────┘

  ASYNC ARCHITECTURE
  ══════════════════

  ┌──────────┐   ┌───────────────┐   ┌──────────┐   ┌──────────────┐
  │  Client  │──▶│  API Server   │──▶│  Queue   │──▶│   Worker     │
  │          │   │               │   │ (Broker) │   │  (Consumer)  │
  │          │◀──│ 202 Accepted  │   │          │   │              │
  └──────────┘   │ + job_id      │   │ RabbitMQ │   │ Process task │
                 └───────────────┘   │ SQS      │   │ Update status│
                                     │ Kafka    │   │ Notify client│
                                     └────┬─────┘   └──────────────┘
                                          │ FAIL
                                          ▼
                                     ┌──────────┐
                                     │   DLQ    │  Dead Letter Queue
                                     │          │  Failed messages go here
                                     │ Monitor  │  Alert + inspect + replay
                                     │ + Alert  │
                                     └──────────┘

  KHI NÀO DÙNG ASYNC
  ═══════════════════
  ✅ Email / SMS / Push notifications
  ✅ PDF generation, Image processing
  ✅ Data aggregation, Report generation
  ✅ Webhook delivery
  ✅ Long-running computations (> 5s)
  ❌ User-facing real-time operations (use sync)

  MESSAGE QUEUE SELECTION
  ═══════════════════════
  ┌──────────────┬──────────────────────────────────────────────────────┐
  │ Tool         │ Best for                                             │
  ├──────────────┼──────────────────────────────────────────────────────┤
  │ RabbitMQ     │ Task queues, routing, pub/sub. General purpose.     │
  │ AWS SQS      │ Serverless, auto-scale, managed. AWS ecosystem.     │
  │ Kafka        │ Event streaming, high throughput, log. > 100K msg/s │
  │ Redis Streams│ Lightweight, already have Redis. Simple pub/sub.     │
  │ Celery       │ Python task queue (wraps RabbitMQ/Redis).           │
  └──────────────┴──────────────────────────────────────────────────────┘

  GRACEFUL SHUTDOWN FLOW
  ═════════════════════

  SIGTERM received
       │
       ▼
  1. Stop accepting NEW requests/messages
       │
       ▼
  2. Finish IN-FLIGHT requests (grace period: 30s)
       │
       ▼
  3. Close connections (DB, cache, queue)
       │
       ▼
  4. Exit cleanly (exit code 0)

  K8s: terminationGracePeriodSeconds: 30
  Pre-stop hook: drain connections
  Health check: return unhealthy BEFORE shutdown
```

### XVII.1 — Xử lý mọi thứ synchronous, không có async processing

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | API response time > 5s vì phải chờ email gửi, PDF generate, etc. User bị block chờ long-running tasks. Timeout errors cho heavy operations. |
| **Tác động** | UX tệ — user phải chờ. Timeout cho long tasks. Resource blocked, throughput thấp. |
| **Best Practice** | **Message Queue** (RabbitMQ, SQS, Kafka) cho async tasks. Pattern: API nhận request > publish message > return 202 Accepted > worker process async. Dùng cho: email, notifications, PDF generation, image processing, data aggregation. |

### XVII.2 — Message Queue không có Dead Letter Queue (DLQ) và retry

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Failed messages biến mất. Không biết message nào fail và tại sao. Không thể replay failed messages. |
| **Tác động** | Data loss — messages bị mất vĩnh viễn. Khó debug failures. Business transactions bị drop silently. |
| **Best Practice** | **Dead Letter Queue** cho mọi queue. Max retry 3-5 lần với backoff. DLQ monitoring và alerting. Tooling để inspect và replay DLQ messages. Idempotent consumers để safe retry. |

### XVII.3 — Không có Graceful Shutdown, mất in-flight requests khi deploy

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Deploy gây 502/503 errors tạm thời. In-flight requests bị drop. Queue messages bị mất khi worker restart. |
| **Tác động** | Request failures mỗi lần deploy. Data inconsistency do half-processed requests. User nhìn thấy errors. |
| **Best Practice** | **Graceful shutdown**: stop accepting new requests > finish in-flight requests > close connections > exit. `SIGTERM` handler với grace period (30s). K8s: `terminationGracePeriodSeconds`. Pre-stop hook để drain connections. Health check return unhealthy trước khi shutdown. |

---

## XVIII. COMPLIANCE & DATA PROTECTION

```
┌─────────────────────────────────────────────────────────────────────────────┐
│             COMPLIANCE & DATA PROTECTION — BEST PRACTICES                   │
└─────────────────────────────────────────────────────────────────────────────┘

  COMPLIANCE FRAMEWORK
  ════════════════════

  ┌─────────────────────────────────────────────────────────────────────────┐
  │                        DATA PROTECTION LAYERS                           │
  │                                                                         │
  │  ┌────────────────────────────────────────────────────────────────┐    │
  │  │ Layer 4: GOVERNANCE                                            │    │
  │  │ Data mapping | Retention policies | DPA with vendors           │    │
  │  │ Regular audits | SOC 2 / ISO 27001 readiness                   │    │
  │  ├────────────────────────────────────────────────────────────────┤    │
  │  │ Layer 3: ACCESS CONTROL                                        │    │
  │  │ RBAC | Audit trail | Access reviews quarterly                  │    │
  │  │ Principle of least privilege | MFA                              │    │
  │  ├────────────────────────────────────────────────────────────────┤    │
  │  │ Layer 2: DATA HANDLING                                         │    │
  │  │ PII encryption | Log redaction | Consent tracking              │    │
  │  │ Right to erasure | Data anonymization                          │    │
  │  ├────────────────────────────────────────────────────────────────┤    │
  │  │ Layer 1: DOCUMENTATION                                         │    │
  │  │ Data inventory | Processing records | Incident response plan   │    │
  │  │ Change management | Vendor risk assessment                     │    │
  │  └────────────────────────────────────────────────────────────────┘    │
  └─────────────────────────────────────────────────────────────────────────┘

  GDPR / CCPA KEY REQUIREMENTS
  ════════════════════════════
  ┌────────────────────────┬──────────────────────────────────────────────┐
  │ Requirement            │ Implementation                               │
  ├────────────────────────┼──────────────────────────────────────────────┤
  │ Right to access        │ API to export user data (JSON/CSV)           │
  │ Right to erasure       │ DELETE /users/{id} + cascade to all systems  │
  │ Consent management     │ Track consent per purpose, revocable         │
  │ Data portability       │ Standard format export (JSON, CSV)           │
  │ Breach notification    │ 72-hour notification process                 │
  │ Privacy by design      │ PII encrypted, minimized collection          │
  └────────────────────────┴──────────────────────────────────────────────┘

  LOG REDACTION
  ═════════════
  ❌ {"user":"john@email.com", "card":"4111-1111-1111-1111", "action":"pay"}
  ✅ {"user":"j***@***.com", "card":"****-****-****-1111", "action":"pay"}

  Auto-mask: email, phone, CC#, SSN, tokens
  Allowlist fields (chỉ log fields được phép)
  Retention limits cho logs chứa user data

  SOC 2 READINESS PATH
  ════════════════════
  Start with Type I (point-in-time) ──▶ Progress to Type II (6-12 months)
  Tools: Vanta | Drata | Secureframe (compliance automation)
```

### XVIII.1 — Không tuân thủ GDPR/CCPA, không có data privacy controls

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không có consent management. Không thể delete user data khi requested. Log chứa PII (email, phone, IP). Không biết data nào đang lưu ở đâu. |
| **Tác động** | GDPR phạt tới **4% global revenue** hoặc 20M EUR. CCPA phạt $7,500/violation. Lawsuit từ users. Reputation damage. |
| **Best Practice** | **Data mapping**: biết data nào, ở đâu, ai access. Right to erasure (delete endpoint). Consent tracking. PII encryption. Data retention policies. Privacy by design. **DPA** (Data Processing Agreement) với vendors. |

### XVIII.2 — Log và monitoring chứa sensitive data (PII)

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Log chứa passwords, credit card numbers, SSN. Error messages bao gồm user PII. Monitoring dashboards hiển thị sensitive data. |
| **Tác động** | Compliance violation. Internal data breach nếu logs bị access bởi unauthorized person. Audit failure. |
| **Best Practice** | **Log redaction**: auto-mask PII patterns (email, phone, CC#). Structured logging với allowlist fields. Pino/Winston redact option: `redact: ['req.headers.authorization', 'body.password', '*.email']`. Audit log access controls. Retention limits cho logs chứa user data. |

### XVIII.3 — Không có SOC 2/ISO 27001 readiness, thiếu security controls documentation

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Không có access control documentation. Không track ai access data gì. Không có change management process. Khách hàng enterprise hỏi SOC 2 report mà không có. |
| **Tác động** | Mất deal enterprise. Audit failure. Không đủ điều kiện bán cho regulated industries (healthcare, finance). |
| **Best Practice** | Bắt đầu với **SOC 2 Type I** (point-in-time). Audit trail cho mọi data access. Access reviews hàng quý. Change management documented. Incident response plan. Vendor risk assessment. Tools: Vanta, Drata, Secureframe cho automation. |

---

## XIX. CHAOS ENGINEERING & RESILIENCE TESTING

```
┌─────────────────────────────────────────────────────────────────────────────┐
│          CHAOS ENGINEERING & RESILIENCE TESTING — BEST PRACTICES            │
└─────────────────────────────────────────────────────────────────────────────┘

  CHAOS ENGINEERING MATURITY MODEL
  ════════════════════════════════

  Level 1: Manual ──▶ Level 2: Automated ──▶ Level 3: Continuous
  (kill pod thử)       (scheduled chaos)       (chaos in CI/CD)

  CHAOS EXPERIMENT FLOW
  ═════════════════════

  1. Define steady state  ──▶  "Error rate < 1%, p99 < 200ms"
       │
       ▼
  2. Hypothesize          ──▶  "If Redis down, system uses fallback"
       │
       ▼
  3. Inject failure       ──▶  Kill Redis pod / Add 500ms latency
       │
       ▼
  4. Observe              ──▶  Monitor dashboards, error rate, latency
       │
       ▼
  5. Learn & Fix          ──▶  Fix weaknesses found
       │
       ▼
  6. Repeat               ──▶  Increase scope over time

  CHAOS EXPERIMENTS BY CATEGORY
  ═════════════════════════════
  ┌──────────────────────┬──────────────────────────────────────────────┐
  │ Category             │ Experiments                                   │
  ├──────────────────────┼──────────────────────────────────────────────┤
  │ Infrastructure       │ Kill pod/node, fill disk, CPU stress         │
  │ Network              │ Add latency, packet loss, DNS failure        │
  │ Application          │ Kill dependency, corrupt cache, full queue   │
  │ Data                 │ Simulate DB failover, backup restore         │
  │ Regional             │ Simulate AZ failure, region failover         │
  └──────────────────────┴──────────────────────────────────────────────┘

  Bắt đầu nhỏ ──▶ single pod ──▶ single service ──▶ multi-service ──▶ region

  FEATURE FLAGS ARCHITECTURE
  ═════════════════════════

  ┌──────────────────────────────────────────────────────────────────────┐
  │                     Feature Flag System                              │
  │                                                                      │
  │  ┌────────────┐   ┌────────────────┐   ┌───────────────────────┐   │
  │  │ Flag Store │──▶│  Evaluation    │──▶│ Result                │   │
  │  │ (DB/Cache) │   │  Engine        │   │                       │   │
  │  │            │   │                │   │ user_id: 123          │   │
  │  │ feature_x: │   │ Rules:         │   │ feature_x: ON         │   │
  │  │  enabled   │   │ - 5% rollout   │   │ feature_y: OFF        │   │
  │  │  rules     │   │ - beta users   │   │                       │   │
  │  │  variants  │   │ - region: US   │   │                       │   │
  │  └────────────┘   └────────────────┘   └───────────────────────┘   │
  └──────────────────────────────────────────────────────────────────────┘

  PROGRESSIVE ROLLOUT
  ═══════════════════
  Deploy code ──▶ Flag OFF ──▶ 1% ON ──▶ 5% ──▶ 25% ──▶ 100%
                                  │
                          Monitor metrics
                          Auto-kill if error > threshold

  FLAG LIFECYCLE
  ══════════════
  Create flag ──▶ Develop ──▶ Test ──▶ Rollout ──▶ 100% ──▶ CLEANUP
                                                               │
                                                   Remove flag from code
                                                   (Avoid flag debt!)

  TOOLS
  ═════
  Chaos:  Gremlin | Chaos Monkey | Litmus (K8s) | Toxiproxy
  Flags:  LaunchDarkly | Unleash | Flagsmith | OpenFeature
  GameDay: Quarterly exercises, increasing scope
```

### XIX.1 — Không chủ động test failure scenarios, chỉ chờ sự cố xảy ra

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Chưa bao giờ thử kill 1 service xem hệ thống phản ứng thế nào. Không biết hệ thống chịu được bao nhiêu load. Mỗi incident đều là "lần đầu gặp". |
| **Tác động** | Surprises liên tục khi có sự cố. Không biết weak points ở đâu. Recovery time không dự đoán được. |
| **Best Practice** | **Chaos Engineering**: chủ động inject failures trong controlled environment. Bắt đầu nhỏ: kill 1 pod, add latency, fail 1 dependency. Tools: **Gremlin, Chaos Monkey, Litmus**. GameDay exercises hàng quý. Steadily increase scope: single service > multi-service > region failover. |

### XIX.2 — Không có Feature Flags, deploy = release cho 100% users

| | Chi tiết |
|---|---|
| **Triệu chứng / Dấu hiệu** | Mỗi deploy đều là all-or-nothing. Không thể tắt feature có bug mà không rollback toàn bộ. Không thể A/B test features. Dark launch không thể thực hiện. |
| **Tác động** | Blast radius luôn là 100% users. Rollback là cách duy nhất để fix. Không thể progressive rollout. Time-to-fix chậm. |
| **Best Practice** | **Feature Flag system** (LaunchDarkly, Unleash, Flagsmith, hoặc in-house). Tách deployment (code lên prod) khỏi release (feature bật cho users). Progressive rollout: 1% > 5% > 25% > 100%. Kill switch cho mọi feature mới. Cleanup flags sau khi feature stable (tránh flag debt). |

---

## TỔNG KẾT & HƯỚNG DẪN ƯU TIÊN

```
┌─────────────────────────────────────────────────────────────────────────────┐
│              PRODUCTION QUALITY — IMPLEMENTATION ROADMAP                     │
└─────────────────────────────────────────────────────────────────────────────┘

  TUẦN 1-2 (P1 — Critical)                  TUẦN 3-4 (P2 — High)
  ═══════════════════════                    ═════════════════════
  □ V.  Security                             □ III. Observability
    - Secrets management                       - Monitoring + Alerting
    - Input validation                         - Structured logging
    - Auth/AuthZ                               - Distributed tracing
  □ II. Reliability                          □ IV. Deployment
    - Retry + Timeout                          - CI/CD pipeline
    - Circuit Breaker                          - Canary deployment
  □ VII. Error Handling                      □ VIII. Database
    - Error classification                     - Indexes + N+1 fix
    - Structured error response                - Connection pool
  □ XVIII. Compliance                        □ XVI. Caching
    - PII protection                           - Redis cache layer
    - Log redaction                            - CDN for static assets

  THÁNG 2 (P3 — Medium)                     THÁNG 3 (P4 — Standard)
  ══════════════════════                     ═══════════════════════
  □ VI. Testing                              □ XI. Code Quality
    - Automated test pyramid                   - Linter + static analysis
    - Performance testing                      - Code review process
  □ IX. API Design                           □ XII. Infrastructure
    - Versioning + Pagination                  - Docker optimization
    - Rate limiting + Idempotency              - K8s resource limits
  □ X.  Configuration                        □ XIII. Documentation
    - Centralized config                       - Runbooks
    - Config validation                        - API docs (OpenAPI)
  □ XVII. Async Processing
    - Message queue + DLQ
    - Graceful shutdown

  THÁNG 4-6 (P5 — Enhancement)
  ════════════════════════════
  □ XV.  MLOps (nếu có AI features)
  □ XIV. Production Readiness (PRR checklist)
  □ I.   Architecture review
  □ XIX. Chaos Engineering + Feature Flags
```

### Thứ tự ưu tiên triển khai

| Ưu tiên | Domain | Timeline |
|---------|--------|----------|
| **P1 - Critical** | Security, Reliability, Error Handling, Compliance & Data Protection | Tuần 1-2 |
| **P2 - High** | Observability, Deployment, Database, Caching & Performance | Tuần 3-4 |
| **P3 - Medium** | Testing, API Design, Configuration, Async & Message Queues | Tháng 2 |
| **P4 - Standard** | Code Quality, Infrastructure, Documentation | Tháng 3 |
| **P5 - Enhancement** | MLOps, Production Readiness, Architecture, Chaos Engineering | Tháng 4-6 |

### Nguyên tắc vàng

1. **Bắt đầu từ những thứ đang gây đau (pain points) nhất cho team.**
2. **Không cố làm tất cả cùng lúc** — iterate từng domain một.
3. **Mỗi sprint dành 15-20% capacity** cho việc cải thiện production quality.
4. **Review checklist này mỗi quý** để cập nhật và đánh giá lại.
5. **Production Quality là hành trình liên tục, không phải đích đến.**

---

*Tài liệu được tổng hợp từ: Production Quality Handbook (100 trang), Production Risk Handbook (1250 trang), Dev Engineering Super-Skill, và các CKP Production Quality/Risk.*

*Version 3.0 — March 2026*